---
phase: 16-feriados-brasileiros
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/brazilian_holidays.rb
  - app/helpers/application_helper.rb
  - test/lib/brazilian_holidays_test.rb
autonomous: true
requirements:
  - FERI-01

must_haves:
  truths:
    - "BrazilianHolidays.for(2026) retorna hash com pelo menos uma entrada"
    - "BrazilianHolidays.for(9999) retorna {} sem levantar exceção"
    - "BrazilianHolidays.for(2026)[Date.new(2026,1,1)] retorna 'Ano Novo'"
    - "brazilian_holiday_for(date) retorna nil para data sem feriado"
    - "brazilian_holiday_for(date) retorna String para data com feriado"
  artifacts:
    - path: "app/lib/brazilian_holidays.rb"
      provides: "Module BrazilianHolidays com constante HOLIDAYS e método .for(year)"
      contains: "module BrazilianHolidays"
    - path: "app/helpers/application_helper.rb"
      provides: "Helper de view brazilian_holiday_for(date)"
      contains: "def brazilian_holiday_for"
    - path: "test/lib/brazilian_holidays_test.rb"
      provides: "Testes unitários do module BrazilianHolidays"
      contains: "class BrazilianHolidaysTest"
  key_links:
    - from: "app/helpers/application_helper.rb"
      to: "app/lib/brazilian_holidays.rb"
      via: "BrazilianHolidays.for(date.year)[date]"
      pattern: "BrazilianHolidays\\.for"
---

<objective>
Criar o módulo Ruby puro `BrazilianHolidays` com lista hardcoded de feriados nacionais e comemorativos de marketing para 2025–2027, e expor o helper `brazilian_holiday_for(date)` no `ApplicationHelper`. Criar testes unitários cobrindo FERI-01.

Purpose: Prover a fonte de dados de feriados para ambos os calendários (admin e cliente) sem banco de dados nem API externa. O módulo é a única fonte de verdade para nomes e datas de feriados nesta fase.
Output: app/lib/brazilian_holidays.rb, app/helpers/application_helper.rb (modificado), test/lib/brazilian_holidays_test.rb
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-feriados-brasileiros/16-CONTEXT.md
@.planning/phases/16-feriados-brasileiros/16-RESEARCH.md
@.planning/phases/16-feriados-brasileiros/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar módulo BrazilianHolidays com lista hardcoded 2025–2027 e testes unitários</name>
  <files>app/lib/brazilian_holidays.rb, test/lib/brazilian_holidays_test.rb</files>
  <read_first>
    - .planning/phases/16-feriados-brasileiros/16-RESEARCH.md — Pattern 1 (estrutura completa do módulo com todas as datas verificadas, anti-patterns, pitfalls 1 e 5)
    - .planning/phases/16-feriados-brasileiros/16-CONTEXT.md — D-01 (interface pública obrigatória: HOLIDAYS constante frozen, .for(year), fetch com default {})
    - .planning/phases/16-feriados-brasileiros/16-PATTERNS.md — seção app/lib/brazilian_holidays.rb (convenções: sem require Date, sem autoload manual, .freeze em cada sub-hash)
    - test/test_helper.rb — padrão de require e fixtures :all para replicar corretamente
    - test/models/arte_test.rb — padrão ActiveSupport::TestCase, assert_equal, assert_nil, assert_instance_of
  </read_first>
  <behavior>
    - BrazilianHolidays.for(2026) retorna Hash com pelo menos 10 entradas
    - BrazilianHolidays.for(9999) retorna {} sem exceção
    - BrazilianHolidays.for(2026)[Date.new(2026,1,1)] == "Ano Novo"
    - BrazilianHolidays.for(2026)[Date.new(2026,6,4)] == "Corpus Christi"
    - BrazilianHolidays.for(2026)[Date.new(2026,11,27)] == "Black Friday"
    - BrazilianHolidays.for(2025)[Date.new(2025,5,11)] == "Dia das Mães"
    - BrazilianHolidays.for(2026)[Date.new(2026,3,10)] é nil (dia sem feriado)
    - [2025, 2026, 2027].each { |y| assert BrazilianHolidays.for(y).any? }
  </behavior>
  <action>
    Criar diretório app/lib/ com mkdir -p (não existe no projeto — ver Pitfall 1 do RESEARCH.md).

    Criar app/lib/brazilian_holidays.rb como módulo Ruby puro. A estrutura HOLIDAYS é uma constante hash aninhada: chave externa é Integer (ano), chave interna é Date.new(year,month,day), valor é String com nome canônico do UI-SPEC.md. Chamar .freeze em cada sub-hash de ano e no hash externo. Implementar def self.for(year) usando HOLIDAYS.fetch(year, {}) — sem rescue, sem nil-check extra.

    Usar exatamente os nomes canônicos do UI-SPEC.md Copywriting Contract. Para 12/out usar "Ap. / Crianças" (14 chars). Incluir os dois dias de Carnaval por ano (segunda e terça). Todas as datas móveis verificadas estão no RESEARCH.md tabela "Datas Móveis Verificadas (2025–2027)".

    Criar diretório test/lib/ com mkdir -p. Criar test/lib/brazilian_holidays_test.rb com class BrazilianHolidaysTest < ActiveSupport::TestCase. Escrever testes RED primeiro (sem require do módulo ainda feito), confirmar falha, depois implementar o módulo para verde. Cobrir todos os 8 behaviors listados acima.

    Não colocar require "date" — Rails/bundler já provê Date. Não colocar def setup se não há estado de instância. Usar fixtures :all não interfere com testes do module (sem ActiveRecord).

    Executar bin/rails test test/lib/brazilian_holidays_test.rb ao final — deve passar 0 failures, 0 errors.
  </action>
  <verify>
    <automated>bin/rails test test/lib/brazilian_holidays_test.rb</automated>
  </verify>
  <done>bin/rails test test/lib/brazilian_holidays_test.rb verde com 0 failures, 0 errors. app/lib/brazilian_holidays.rb existe e contém HOLIDAYS frozen com entradas para 2025, 2026 e 2027. BrazilianHolidays.for(9999) retorna {} sem exceção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar helper brazilian_holiday_for ao ApplicationHelper</name>
  <files>app/helpers/application_helper.rb</files>
  <read_first>
    - app/helpers/application_helper.rb — ler o arquivo completo; entender a estrutura atual do módulo ApplicationHelper, localização do método client_color e include Pagy::Frontend
    - .planning/phases/16-feriados-brasileiros/16-PATTERNS.md — seção app/helpers/application_helper.rb (padrão exato de inserção: método público, sem private, sem rescue, retorno direto)
    - .planning/phases/16-feriados-brasileiros/16-CONTEXT.md — Claude's Discretion (nome do método: brazilian_holiday_for, delegação: BrazilianHolidays.for(date.year)[date])
  </read_first>
  <behavior>
    - ApplicationHelper#brazilian_holiday_for(Date.new(2026,1,1)) retorna "Ano Novo"
    - ApplicationHelper#brazilian_holiday_for(Date.new(2026,3,10)) retorna nil
    - ApplicationHelper#brazilian_holiday_for(Date.new(9999,1,1)) retorna nil sem exceção
  </behavior>
  <action>
    Abrir app/helpers/application_helper.rb e inserir o método def brazilian_holiday_for(date) dentro do módulo ApplicationHelper, imediatamente após o método client_color existente. O método deve ter uma única linha de corpo: BrazilianHolidays.for(date.year)[date]. Sem variável intermediária, sem rescue, sem comentário de documentação (padrão do projeto).

    O método não precisa de test file próprio — o comportamento end-to-end é verificado pelos testes de integração do Plan 02. Contudo, confirmar que bin/rails test passa sem erros no helper por meio do test suite completo.

    Executar bin/rails test após a modificação para garantir que nenhum teste existente quebrou.
  </action>
  <verify>
    <automated>bin/rails test</automated>
  </verify>
  <done>bin/rails test verde (0 failures, 0 errors). app/helpers/application_helper.rb contém def brazilian_holiday_for(date) seguido de BrazilianHolidays.for(date.year)[date]. O método está dentro do módulo ApplicationHelper, após client_color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | date vem de grid_dates (range calculado internamente); não é input de usuário |
| helper → module | Integer year derivado de Date#year; nenhum input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | BrazilianHolidays module | accept | Dados são públicos (feriados nacionais); nenhuma informação sensível exposta |
| T-16-02 | Tampering | app/lib/brazilian_holidays.rb (HOLIDAYS constante) | mitigate | .freeze em cada sub-hash e no hash externo — mutação em runtime impossível |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Fase não instala dependências externas — Package Legitimacy Audit confirma sem gems novas |

Avaliação ASVS L1: Fase não introduz endpoints, sessões, autenticação, consultas ao banco ou input do usuário. Superfície de ataque: zero. Única medida ativa é o .freeze na constante HOLIDAYS.
</threat_model>

<verification>
Após execução dos dois tasks:

1. bin/rails test test/lib/brazilian_holidays_test.rb — todos os casos verdes
2. bin/rails test — suite completa verde (sem regressão)
3. Grep: grep -n "def brazilian_holiday_for" app/helpers/application_helper.rb — retorna resultado
4. Grep: grep -n "module BrazilianHolidays" app/lib/brazilian_holidays.rb — retorna resultado
5. Grep: grep -c "\.freeze" app/lib/brazilian_holidays.rb — retorna >= 4 (cada sub-hash + hash externo)
</verification>

<success_criteria>
- app/lib/brazilian_holidays.rb existe com BrazilianHolidays.for(year) retornando Hash{Date => String} para 2025, 2026, 2027 e {} para outros anos
- app/helpers/application_helper.rb contém def brazilian_holiday_for(date)
- test/lib/brazilian_holidays_test.rb cobre datas fixas, datas móveis, ano sem cobertura e retorno nil para data sem feriado
- bin/rails test verde, 0 failures, 0 errors
</success_criteria>

<output>
Criar .planning/phases/16-feriados-brasileiros/16-01-SUMMARY.md quando concluído
</output>
