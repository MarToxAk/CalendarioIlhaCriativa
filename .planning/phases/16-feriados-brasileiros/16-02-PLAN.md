---
phase: 16-feriados-brasileiros
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - app/views/client/home/_month_calendar.html.erb
  - app/views/admin/calendar/_calendar_grid.html.erb
  - test/controllers/client/home_controller_test.rb
  - test/controllers/admin/calendar_controller_test.rb
autonomous: false
requirements:
  - FERI-02
  - FERI-03

must_haves:
  truths:
    - "Calendário do cliente renderiza span.text-red-400 com 'Tiradentes' para 2026-04"
    - "Calendário do admin renderiza span.text-red-400 com 'Tiradentes' para 2026-04"
    - "Dias sem feriado não têm span extra — nenhum elemento vazio adicionado"
    - "Nome de feriado aparece abaixo do número do dia e acima dos chips de artes"
    - "Dia atual que seja feriado exibe o nome do feriado (span fora do if/else do número)"
  artifacts:
    - path: "app/views/client/home/_month_calendar.html.erb"
      provides: "Span de feriado inserido após o bloco if/else do número do dia"
      contains: "brazilian_holiday_for"
    - path: "app/views/admin/calendar/_calendar_grid.html.erb"
      provides: "Span de feriado inserido após o bloco if/else do número do dia"
      contains: "brazilian_holiday_for"
    - path: "test/controllers/client/home_controller_test.rb"
      provides: "Teste de integração FERI-02: response.body inclui Tiradentes para month=2026-04"
      contains: "Tiradentes"
    - path: "test/controllers/admin/calendar_controller_test.rb"
      provides: "Teste de integração FERI-03: response.body inclui Tiradentes para month=2026-04"
      contains: "Tiradentes"
  key_links:
    - from: "app/views/client/home/_month_calendar.html.erb"
      to: "app/helpers/application_helper.rb"
      via: "brazilian_holiday_for(date)"
      pattern: "brazilian_holiday_for"
    - from: "app/views/admin/calendar/_calendar_grid.html.erb"
      to: "app/helpers/application_helper.rb"
      via: "brazilian_holiday_for(date)"
      pattern: "brazilian_holiday_for"
---

<objective>
Inserir o span de feriado nas views ERB dos dois calendários (cliente e admin) e adicionar testes de integração verificando que o nome do feriado aparece no body da resposta HTML.

Purpose: Completar FERI-02 e FERI-03 — tornar os feriados visíveis nos dois calendários. O módulo e o helper criados no Plan 01 são a fonte de dados; este plano apenas os conecta às views.
Output: _month_calendar.html.erb e _calendar_grid.html.erb modificados com span de feriado; testes de integração verdes para ambos os calendários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-feriados-brasileiros/16-CONTEXT.md
@.planning/phases/16-feriados-brasileiros/16-RESEARCH.md
@.planning/phases/16-feriados-brasileiros/16-PATTERNS.md
@.planning/phases/16-feriados-brasileiros/16-UI-SPEC.md
@.planning/phases/16-feriados-brasileiros/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inserir span de feriado nas duas views de calendário e adicionar testes de integração</name>
  <files>
    app/views/client/home/_month_calendar.html.erb,
    app/views/admin/calendar/_calendar_grid.html.erb,
    test/controllers/client/home_controller_test.rb,
    test/controllers/admin/calendar_controller_test.rb
  </files>
  <read_first>
    - app/views/client/home/_month_calendar.html.erb — ler o arquivo completo; identificar as linhas exatas do bloco if date == Date.today ... else ... end do número do dia; o span de feriado vai APÓS o end desse bloco e ANTES do each de artes
    - app/views/admin/calendar/_calendar_grid.html.erb — ler o arquivo completo; mesmo padrão, mas usa Time.zone.today em vez de Date.today; span de feriado APÓS end do if/else e ANTES de visible = artes_do_dia.first(3)
    - .planning/phases/16-feriados-brasileiros/16-PATTERNS.md — seções _month_calendar.html.erb e _calendar_grid.html.erb (ponto de inserção preciso, snippet ERB exato, Pitfall 3: span fora do if/else)
    - .planning/phases/16-feriados-brasileiros/16-UI-SPEC.md — Anatomy Holiday Cell (ordem vertical: número → feriado → chips) e classes Tailwind exatas: text-xs text-red-400 block truncate leading-snug mt-0.5
    - test/controllers/client/home_controller_test.rb — ler o arquivo completo; identificar o padrão de helper sign_in_as_client, o path usado (client_root_path), e como passar o param month
    - test/controllers/admin/calendar_controller_test.rb — ler o arquivo completo; identificar helper de autenticação (sign_in_as), path admin_calendar_path, param month
  </read_first>
  <behavior>
    TDD — escrever testes primeiro (RED), depois inserir o ERB (GREEN):

    FERI-02 (cliente):
    - GET client_root_path(token: @client.access_token, month: "2026-04") retorna 200
    - response.body inclui "Tiradentes" (feriado em 21/04/2026)
    - response.body inclui "Páscoa" (feriado em 05/04/2026)

    FERI-03 (admin):
    - GET admin_calendar_path(month: "2026-04") autenticado retorna 200
    - response.body inclui "Tiradentes"
    - response.body inclui "Páscoa"

    Regressão (ambos os calendários):
    - response.body não contém "brazilianholiday" ou "undefined method" — ausência de erros de template
  </behavior>
  <action>
    PASSO 1 — Testes RED:
    Em test/controllers/client/home_controller_test.rb, adicionar teste com descrição "exibe nome de feriado no calendário do cliente". Usar o padrão de autenticação existente no arquivo (sign_in_as_client ou equivalente encontrado na leitura). GET com month: "2026-04". Assert assert_response :success e assert_includes response.body, "Tiradentes" e assert_includes response.body, "Páscoa". Executar bin/rails test test/controllers/client/home_controller_test.rb — deve falhar (RED).

    Em test/controllers/admin/calendar_controller_test.rb, adicionar teste "exibe nome de feriado no calendário admin" com o padrão de autenticação admin existente, GET com month: "2026-04", assert_response :success e assert_includes response.body, "Tiradentes". Executar bin/rails test test/controllers/admin/calendar_controller_test.rb — deve falhar (RED).

    PASSO 2 — Implementação GREEN (views ERB):
    Modificar app/views/client/home/_month_calendar.html.erb: localizar o end que fecha o bloco if date == Date.today ... else ... end do número do dia. Inserir imediatamente após esse end (linha em branco + bloco):
      <% if (holiday = brazilian_holiday_for(date)) %>
        <span class="text-xs text-red-400 block truncate leading-snug mt-0.5">
          <%= truncate(holiday, length: 15) %>
        </span>
      <% end %>
    O bloco deve ficar antes do each de artes. Não inserir dentro de nenhum branch do if/else do número (Pitfall 3 do RESEARCH.md).

    Modificar app/views/admin/calendar/_calendar_grid.html.erb: mesma lógica. O if/else usa Time.zone.today (não Date.today) — não alterar essa linha. Inserir o bloco após o end do if/else e antes de visible = artes_do_dia.first(3). Snippet idêntico ao da view do cliente.

    PASSO 3 — GREEN:
    Executar bin/rails test test/controllers/client/home_controller_test.rb — verde.
    Executar bin/rails test test/controllers/admin/calendar_controller_test.rb — verde.
    Executar bin/rails test — suite completa verde.
  </action>
  <verify>
    <automated>bin/rails test</automated>
  </verify>
  <done>bin/rails test verde, 0 failures, 0 errors. Ambas as views contêm o bloco if (holiday = brazilian_holiday_for(date)) fora do if/else do número. Testes de integração de ambos os controllers passam incluindo assert_includes para "Tiradentes" com month=2026-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Span de feriado inserido nos dois calendários (cliente e admin). O módulo BrazilianHolidays e o helper estão ativos. Testes automatizados passam. Esta verificação confirma que o visual está correto no browser: texto vermelho abaixo do número do dia, sem quebra de layout.
  </what-built>
  <how-to-verify>
    1. Iniciar o servidor: bin/rails server (ou equivalente já em execução)
    2. Acessar o calendário do CLIENTE para abril de 2026:
       - Navegar para o calendário do cliente de qualquer cliente existente com ?month=2026-04 (ou usar os botões de navegação para chegar em abril de 2026)
       - Confirmar: célula do dia 5 exibe "Páscoa" em vermelho (text-red-400 / #F87171) abaixo do número
       - Confirmar: célula do dia 21 exibe "Tiradentes" em vermelho abaixo do número
       - Confirmar: outras células sem feriado não têm texto extra
       - Confirmar: chips de artes (se houver) aparecem abaixo do nome do feriado, não acima
    3. Acessar o calendário do ADMIN para abril de 2026:
       - Navegar para /admin/calendar?month=2026-04
       - Confirmar os mesmos pontos acima para Páscoa e Tiradentes
    4. Verificar um dia que coincida com hoje se for feriado (opcional — se hoje for 12/out, verificar que o círculo laranja e o nome "Ap. / Crianças" coexistem)
    5. Confirmar que o layout das células não foi quebrado (número do dia ainda aparece, chips ainda aparecem corretamente)
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o visual está correto, ou descreva o problema encontrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | grid_dates é um Array<Date> calculado internamente (parse_month_param + range); não é input direto do usuário |
| helper → ERB output | truncate(holiday, length: 15) sobre string hardcoded do module — sem XSS possível; Rails escapa automaticamente via <%= %> |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | XSS | span de feriado nas views ERB | accept | String vem de constante hardcoded no module (não de params); Rails escape automático via <%= %>; truncate é helper Rails seguro |
| T-16-04 | Information Disclosure | Nome do feriado visível no HTML | accept | Feriados nacionais são informação pública; nenhum dado sensível exposto |
| T-16-05 | Denial of Service | BrazilianHolidays.for chamado por cada date no grid (~35–42 dates) | accept | Operação O(1) — hash lookup em constante frozen; sem query de banco, sem I/O |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Fase não instala dependências; Package Legitimacy Audit confirma sem gems novas |

Avaliação ASVS L1: Views usam <%= %> (escape automático), helper não recebe input do usuário, module é imutável (.freeze). Nenhuma nova superfície de ataque introduzida.
</threat_model>

<verification>
Após execução de todos os tasks:

1. bin/rails test — suite completa verde, 0 failures, 0 errors
2. grep -n "brazilian_holiday_for" app/views/client/home/_month_calendar.html.erb — retorna resultado
3. grep -n "brazilian_holiday_for" app/views/admin/calendar/_calendar_grid.html.erb — retorna resultado
4. grep -n "Tiradentes" test/controllers/client/home_controller_test.rb — retorna resultado
5. grep -n "Tiradentes" test/controllers/admin/calendar_controller_test.rb — retorna resultado
6. Checkpoint visual aprovado pelo usuário
</verification>

<success_criteria>
- Ambas as views contêm o bloco de feriado fora do if/else do número do dia
- Span usa exatamente text-xs text-red-400 block truncate leading-snug mt-0.5
- Testes de integração de ambos os controllers incluem assert_includes "Tiradentes" com month=2026-04
- bin/rails test verde, 0 failures
- Checkpoint visual aprovado: texto vermelho visível abaixo do número, layout não quebrado
</success_criteria>

<output>
Criar .planning/phases/16-feriados-brasileiros/16-02-SUMMARY.md quando concluído
</output>
