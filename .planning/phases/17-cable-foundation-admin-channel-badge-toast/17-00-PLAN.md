---
phase: 17-cable-foundation-admin-channel-badge-toast
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - app/channels/application_cable/channel.rb
  - test/channels/application_cable/connection_test.rb
  - test/channels/admin_notifications_channel_test.rb
  - test/fixtures/clients.yml
  - test/fixtures/sessions.yml
autonomous: true
requirements:
  - CABLE-01
must_haves:
  truths:
    - "app/channels/application_cable/channel.rb existe e define ApplicationCable::Channel < ActionCable::Channel::Base"
    - "test/channels/application_cable/connection_test.rb existe com 4 casos de teste (stub RED)"
    - "test/channels/admin_notifications_channel_test.rb existe com 2 casos de teste (stub RED)"
    - "test/fixtures/clients.yml existe com fixtures one (active: true) e inactive (active: false)"
    - "test/fixtures/sessions.yml existe com fixture one referenciando users(:one)"
    - "bundle exec rails test test/channels/ executa sem LoadError ou NameError"
  artifacts:
    - path: "app/channels/application_cable/channel.rb"
      provides: "Base class ApplicationCable::Channel para todos os canais"
      contains: "class Channel < ActionCable::Channel::Base"
    - path: "test/channels/application_cable/connection_test.rb"
      provides: "Testes de autenticação da connection (RED state)"
      contains: "class ApplicationCable::ConnectionTest < ActionCable::Connection::TestCase"
    - path: "test/channels/admin_notifications_channel_test.rb"
      provides: "Testes de subscription do canal admin (RED state)"
      contains: "class AdminNotificationsChannelTest < ActionCable::Channel::TestCase"
    - path: "test/fixtures/clients.yml"
      provides: "Fixtures de Client para testes de canal"
      contains: "inactive:"
    - path: "test/fixtures/sessions.yml"
      provides: "Fixture mínima para tabela sessions"
      contains: "user: one"
  key_links:
    - from: "test/channels/application_cable/connection_test.rb"
      to: "test/fixtures/clients.yml"
      via: "clients(:one), clients(:inactive)"
      pattern: "clients\\(:"
    - from: "test/channels/admin_notifications_channel_test.rb"
      to: "app/channels/application_cable/channel.rb"
      via: "AdminNotificationsChannel < ApplicationCable::Channel"
      pattern: "ApplicationCable::Channel"
---

<objective>
Preparar a infraestrutura de testes de canal (Wave 0): criar os arquivos que as Plans 01 e 02 precisam que existam antes de qualquer implementação de canal.

Purpose: Três bloqueadores críticos impedem que os testes de canal compilem: (1) `ApplicationCable::Channel` base class ausente no projeto — qualquer canal levanta NameError sem ela; (2) fixtures `clients.yml` e `sessions.yml` ausentes — connection tests usam `clients(:one)` e `clients(:inactive)`; (3) os arquivos de teste em si precisam existir em estado RED para que os planos seguintes saibam exatamente o que implementar para fazê-los passar.

Output: 5 arquivos criados. `bundle exec rails test test/channels/` executa sem LoadError/NameError (falha nos asserts — RED state esperado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-CONTEXT.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-RESEARCH.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar ApplicationCable::Channel base class e fixtures de teste</name>
  <files>
    app/channels/application_cable/channel.rb,
    test/fixtures/clients.yml,
    test/fixtures/sessions.yml
  </files>
  <read_first>
    - app/channels/application_cable/connection.rb — ver estrutura do módulo ApplicationCable existente
    - test/fixtures/users.yml — padrão de fixture com ERB para BCrypt digest; replicar para clients.yml
    - .planning/phases/17-cable-foundation-admin-channel-badge-toast/17-PATTERNS.md (seções clients.yml e sessions.yml)
  </read_first>
  <behavior>
    - channel.rb: módulo ApplicationCable, classe Channel herda de ActionCable::Channel::Base, corpo vazio
    - clients.yml: fixture "one" com name "Client One", password_digest via BCrypt ERB, active: true; fixture "two" com active: true; fixture "inactive" com active: false — access_token não declarado (gerado pelo has_secure_token callback)
    - sessions.yml: fixture "one" com "user: one" — mínimo para satisfazer fixtures :all no test_helper sem falhar na tabela sessions
  </behavior>
  <action>
    Criar app/channels/application_cable/channel.rb com módulo ApplicationCable, classe Channel vazia herdando de ActionCable::Channel::Base.

    Criar test/fixtures/clients.yml com header ERB `password_digest = BCrypt::Password.create("password")`. Três fixtures: "one" (name: "Client One", active: true), "two" (name: "Client Two", active: true), "inactive" (name: "Inactive Client", active: false). Todos com password_digest ERB. Não declarar access_token — o has_secure_token callback preenche automaticamente.

    Criar test/fixtures/sessions.yml com fixture "one" contendo apenas "user: one" — referência de belongs_to ao users(:one). Arquivo mínimo; a session completa é criada inline nos testes com @user.sessions.create!.
  </action>
  <verify>
    <automated>bundle exec rails runner "puts ApplicationCable::Channel.ancestors.include?(ActionCable::Channel::Base)"</automated>
  </verify>
  <done>
    ApplicationCable::Channel está definida e herda de ActionCable::Channel::Base (runner imprime "true"). test/fixtures/clients.yml existe com fixtures one, two e inactive. test/fixtures/sessions.yml existe com fixture one.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar stubs de teste RED para connection e canal admin</name>
  <files>
    test/channels/application_cable/connection_test.rb,
    test/channels/admin_notifications_channel_test.rb
  </files>
  <read_first>
    - test/fixtures/users.yml — chaves de fixture disponíveis (users(:one), users(:two))
    - test/fixtures/clients.yml — recém criado na Task 1; chaves clients(:one), clients(:inactive)
    - .planning/phases/17-cable-foundation-admin-channel-badge-toast/17-PATTERNS.md (seções connection_test.rb e admin_notifications_channel_test.rb)
    - .planning/phases/17-cable-foundation-admin-channel-badge-toast/17-RESEARCH.md (seção Code Examples)
  </read_first>
  <behavior>
    - connection_test.rb: herda de ActionCable::Connection::TestCase; setup cria @session com @user.sessions.create!; 4 testes: (a) admin conecta via cookie retornando current_user igual a users(:one) e current_client nil, (b) cliente conecta via token params retornando current_client igual a clients(:one) e current_user nil, (c) rejeita sem credenciais, (d) rejeita cliente inactive via assert_reject_connection
    - admin_notifications_channel_test.rb: herda de ActionCable::Channel::TestCase; 2 testes: (a) stub_connection com current_user: users(:one), subscribe, assert subscription.confirmed?, assert_has_stream_for user; (b) stub_connection com current_user: nil e current_client: nil, subscribe, assert subscription.rejected?
    - Ambos os arquivos devem compilar sem erro mas falhar nos asserts (RED state) porque connection.rb ainda não tem current_client e AdminNotificationsChannel não existe ainda
  </behavior>
  <action>
    Criar test/channels/application_cable/connection_test.rb com require "test_helper", herda ActionCable::Connection::TestCase. Setup: @user = users(:one), @session = @user.sessions.create!. Os 4 casos de teste usam cookies.signed[:session_id] = @session.id para admin, connect params: { token: clients(:one).access_token } para cliente, connect sem args para sem credenciais, e connect params: { token: clients(:inactive).access_token } para cliente inativo.

    Criar test/channels/admin_notifications_channel_test.rb com require "test_helper", herda ActionCable::Channel::TestCase. Dois testes usando stub_connection(current_user: users(:one)) e stub_connection(current_user: nil, current_client: nil).

    Confirmar que os arquivos compilam executando `bundle exec rails test test/channels/` — espera-se erros de assert ou NameError em AdminNotificationsChannel (não existe ainda), NÃO LoadError ou SyntaxError.
  </action>
  <verify>
    <automated>bundle exec rails test test/channels/ 2>&1 | grep -E "(Finished|Error|error|NameError)" | head -20</automated>
  </verify>
  <done>
    bundle exec rails test test/channels/ executa sem LoadError ou SyntaxError. Falhas de assert ou NameError (AdminNotificationsChannel) são esperadas e aceitáveis — estado RED confirmado. Os dois arquivos de teste existem em test/channels/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → fixtures | Fixtures com BCrypt digest são geradas via ERB em contexto de teste — sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-00-01 | Information Disclosure | test/fixtures/clients.yml | accept | Fixtures usam senha "password" hardcoded — aceitável em ambiente de teste, nunca em produção; arquivo em .gitignore seria pior pois fixtures são necessárias para CI |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala pacotes externos — stack ActionCable, turbo-rails, stimulus-rails já instalados e verificados no Gemfile |
</threat_model>

<verification>
bundle exec rails test test/channels/ executa sem LoadError ou SyntaxError.
bundle exec rails runner "puts ApplicationCable::Channel.ancestors.include?(ActionCable::Channel::Base)" imprime "true".
ls test/fixtures/clients.yml test/fixtures/sessions.yml retorna ambos os arquivos.
</verification>

<success_criteria>
- app/channels/application_cable/channel.rb existe com ApplicationCable::Channel < ActionCable::Channel::Base
- test/fixtures/clients.yml existe com fixtures one, two, inactive (active: false)
- test/fixtures/sessions.yml existe com fixture one referenciando users(:one)
- test/channels/application_cable/connection_test.rb existe com 4 testes herdando ActionCable::Connection::TestCase
- test/channels/admin_notifications_channel_test.rb existe com 2 testes herdando ActionCable::Channel::TestCase
- bundle exec rails test test/channels/ não levanta LoadError ou SyntaxError
</success_criteria>

<output>
Criar `.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-00-SUMMARY.md` quando concluído
</output>
