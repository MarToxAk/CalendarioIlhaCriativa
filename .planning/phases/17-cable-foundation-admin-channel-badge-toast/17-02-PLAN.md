---
phase: 17-cable-foundation-admin-channel-badge-toast
plan: "02"
type: execute
wave: 1
depends_on:
  - "17-00"
files_modified:
  - app/channels/admin_notifications_channel.rb
autonomous: true
requirements:
  - CABLE-01
must_haves:
  truths:
    - "AdminNotificationsChannel existe e herda de ApplicationCable::Channel"
    - "subscribed chama reject se current_user for nil (per D-17)"
    - "subscribed chama stream_for current_user quando current_user presente (per D-15)"
    - "Canal NÃO envia badge count no subscribe (per D-16)"
    - "bundle exec rails test test/channels/admin_notifications_channel_test.rb passa ambos os 2 testes"
  artifacts:
    - path: "app/channels/admin_notifications_channel.rb"
      provides: "Canal per-user para broadcasts do admin"
      contains: "stream_for current_user"
  key_links:
    - from: "app/channels/admin_notifications_channel.rb"
      to: "app/channels/application_cable/channel.rb"
      via: "class AdminNotificationsChannel < ApplicationCable::Channel"
      pattern: "ApplicationCable::Channel"
    - from: "app/views/layouts/admin.html.erb"
      to: "app/channels/admin_notifications_channel.rb"
      via: "turbo_stream_from current_user, channel: AdminNotificationsChannel"
      pattern: "AdminNotificationsChannel"
---

<objective>
Criar AdminNotificationsChannel: canal per-user que autentica admin e cria o stream para receber broadcasts nas phases 18–20.

Purpose: Este canal é o receptor de todos os eventos de tempo real do admin (toast, badge increment, dashboard rows). O stream per-user via stream_for current_user garante que broadcasts sejam direcionados ao admin correto — seguro por GlobalID, sem nomes de stream adivináveis. A Phase 17 cria o canal; nenhum broadcast acontece ainda.

Output: app/channels/admin_notifications_channel.rb. 2 testes verdes em test/channels/admin_notifications_channel_test.rb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-CONTEXT.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-RESEARCH.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-PATTERNS.md
@.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-00-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar AdminNotificationsChannel com stream per-user e defesa de autenticação</name>
  <files>
    app/channels/admin_notifications_channel.rb
  </files>
  <read_first>
    - app/channels/application_cable/channel.rb — base class recém criada; confirmar que ApplicationCable::Channel existe
    - app/channels/application_cable/connection.rb — confirmar que current_user é um identifier disponível (identified_by :current_user, :current_client)
    - test/channels/admin_notifications_channel_test.rb — os 2 testes que devem passar após esta tarefa (RED → GREEN)
    - .planning/phases/17-cable-foundation-admin-channel-badge-toast/17-PATTERNS.md (seção admin_notifications_channel.rb)
    - .planning/phases/17-cable-foundation-admin-channel-badge-toast/17-RESEARCH.md (Pattern 2 — AdminNotificationsChannel com stream per-user)
  </read_first>
  <behavior>
    - Classe AdminNotificationsChannel herda de ApplicationCable::Channel (não de ApplicationCable::Connection)
    - Método subscribed: primeira linha é "reject unless current_user" — defesa em profundidade (D-17); admin sem current_user (ex: cliente conectado) não subscreve
    - Após o reject guard: stream_for current_user — usa GlobalID do User para gerar nome de stream opaco (D-15); Phase 18 usará AdminNotificationsChannel.broadcast_to(user, data) que resolve para o mesmo stream
    - Nenhuma outra lógica no subscribed — sem badge count, sem transmit de dados (D-16)
    - Nenhum método unsubscribed necessário nesta fase
    - Teste "subscribes and streams for current user": stub_connection(current_user: users(:one)), subscribe, assert subscription.confirmed?, assert_has_stream_for users(:one) — VERDE
    - Teste "rejects subscription without current_user": stub_connection(current_user: nil, current_client: nil), subscribe, assert subscription.rejected? — VERDE
  </behavior>
  <action>
    Criar app/channels/admin_notifications_channel.rb. Classe AdminNotificationsChannel herda de ApplicationCable::Channel. Único método público: subscribed. Primeira instrução de subscribed: "reject unless current_user" (per D-17). Segunda instrução: "stream_for current_user" (per D-15). Sem transmit, sem ActionCable.server.broadcast, sem referência a Arte ou badge count (per D-16).
  </action>
  <verify>
    <automated>bundle exec rails test test/channels/admin_notifications_channel_test.rb</automated>
  </verify>
  <done>
    bundle exec rails test test/channels/admin_notifications_channel_test.rb passa 2/2 testes. app/channels/admin_notifications_channel.rb contém "reject unless current_user" antes de "stream_for current_user". Canal não tem lógica de badge ou transmit no subscribed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket subscription → canal | current_user pode ser nil se um cliente (não admin) tentar assinar o canal admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Elevation of Privilege | AdminNotificationsChannel#subscribed | mitigate | "reject unless current_user" como primeira instrução — cliente com current_client definido mas current_user nil é rejeitado (D-17); defesa em profundidade além do connection.rb |
| T-17-02-02 | Information Disclosure | stream_for current_user | mitigate | stream_for usa GlobalID — nome de stream é opaco (base64 do GlobalID), não pode ser adivinhado por outros usuários; broadcasts só chegam ao stream do current_user correto (D-15) |
| T-17-02-03 | Tampering | AdminNotificationsChannel.broadcast_to (futuro Phase 18) | accept | Phase 17 não faz broadcasts; anotar para Phase 18: conteúdo de broadcast deve ser renderizado server-side via Turbo Stream template para evitar XSS |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala pacotes externos |
</threat_model>

<verification>
bundle exec rails test test/channels/admin_notifications_channel_test.rb retorna 2 runs, 0 failures, 0 errors.
grep "reject unless current_user" app/channels/admin_notifications_channel.rb retorna match.
grep "stream_for current_user" app/channels/admin_notifications_channel.rb retorna match.
grep -v "transmit\|broadcast\|badge\|Arte\|count" app/channels/admin_notifications_channel.rb — nenhuma lógica de dados no subscribed.
</verification>

<success_criteria>
- app/channels/admin_notifications_channel.rb existe e herda de ApplicationCable::Channel
- subscribed tem "reject unless current_user" antes de "stream_for current_user" (D-17, D-15)
- Nenhum transmit ou badge count no canal (D-16)
- 2 testes passam: subscription confirmada para admin, subscription rejeitada sem current_user
</success_criteria>

<output>
Criar `.planning/phases/17-cable-foundation-admin-channel-badge-toast/17-02-SUMMARY.md` quando concluído
</output>
