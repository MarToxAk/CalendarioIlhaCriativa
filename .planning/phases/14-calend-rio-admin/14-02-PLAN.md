---
phase: 14-calend-rio-admin
plan: "02"
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - app/controllers/admin/calendar_controller.rb
  - app/helpers/application_helper.rb
autonomous: true
requirements:
  - CADM-02
  - CADM-03
  - CADM-04

must_haves:
  truths:
    - "GET /admin/calendar retorna 200 (controller existe e herda Admin::BaseController)"
    - "Controller calcula @artes_by_date com artes de todos os clientes sem N+1"
    - "Helper client_color(client) retorna hash { bg:, text: } com classes Tailwind literais (per D-01)"
    - "parse_month_param faz rescue Date::Error — param inválido retorna Date.today.beginning_of_month (per CADM-04)"
  artifacts:
    - path: "app/controllers/admin/calendar_controller.rb"
      provides: "Admin::CalendarController com action index completa"
      contains: "class Admin::CalendarController < Admin::BaseController"
    - path: "app/helpers/application_helper.rb"
      provides: "Helper client_color(client) com paleta de 8 cores (per D-01)"
      contains: "def client_color"
  key_links:
    - from: "app/controllers/admin/calendar_controller.rb"
      to: "Arte.where(scheduled_on: ...).includes(:client).order(:id)"
      via: "ActiveRecord query"
      pattern: "includes\\(:client\\)"
    - from: "app/helpers/application_helper.rb"
      to: "paleta de 8 cores hex literais"
      via: "client.id % palette.size"
      pattern: "client\\.id % palette\\.size"
---

<objective>
Criar o `Admin::CalendarController` com a action `index` completa — replicando o padrão de `Client::HomeController` com as diferenças-chave do admin: query de artes de TODOS os clientes, `includes(:client)`, `order(:id)` (per CADM-02, CADM-04) — e adicionar o helper `client_color(client)` em `ApplicationHelper` com a paleta de 8 cores determinísticas baseada em `client.id % 8` (per D-01, CADM-03).

Purpose: Controller e helper são a camada de dados e transformação; as views do Plano 03 dependem de `@artes_by_date`, `@grid_dates`, `@prev_month`, `@next_month`, `@month_label` e `client_color`. Sem eles, o Plano 03 não pode ser executado.
Output: Controller funcional retornando 200, helper disponível em toda a app.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-calend-rio-admin/14-CONTEXT.md
@.planning/phases/14-calend-rio-admin/14-RESEARCH.md
@.planning/phases/14-calend-rio-admin/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar Admin::CalendarController com action index (per CADM-02, CADM-04)</name>
  <files>
    app/controllers/admin/calendar_controller.rb
  </files>
  <read_first>
    - app/controllers/client/home_controller.rb — código completo; é o analog exato; copiar verbatim parse_month_param, lógica de grid_start/grid_end, group_by; aplicar as diferenças-chave listadas abaixo
    - app/controllers/admin/base_controller.rb — herança e before_action já declarados aqui; Admin::CalendarController NÃO precisa redeclarar before_action ou layout
    - app/models/arte.rb — confirmar campo scheduled_on (date) e belongs_to :client
    - .planning/phases/14-calend-rio-admin/14-PATTERNS.md — Pattern Assignment para calendar_controller.rb: código completo com diferenças-chave anotadas
  </read_first>
  <behavior>
    - Test 1: GET /admin/calendar retorna 200 quando autenticado
    - Test 2: GET /admin/calendar sem autenticação redireciona (não retorna 200)
    - Test 3: GET /admin/calendar?month=2026-06 retorna 200 com mês correto em @current_month
    - Test 4: GET /admin/calendar?month=invalid retorna 200 (rescue Date::Error, não 500)
  </behavior>
  <action>
    Criar app/controllers/admin/calendar_controller.rb com classe `Admin::CalendarController < Admin::BaseController`. Não declarar `before_action` nem `layout` — herdados de Admin::BaseController.

    Action `index`: atribuir @current_month via parse_month_param; calcular @prev_month e @next_month com strftime("%Y-%m"); calcular @month_label com I18n.l(@current_month, format: "%B %Y") — se o locale pt-BR não estiver configurado, usar array de nomes de meses em português como fallback (A1 do RESEARCH.md); calcular grid_start como @current_month.beginning_of_week e grid_end como @current_month.end_of_month.end_of_week; query: Arte.where(scheduled_on: grid_start..grid_end).includes(:client).order(:id) — `includes(:client)` é obrigatório para evitar N+1 (Anti-Pattern 1 do RESEARCH.md); atribuir @artes_by_date via group_by(&:scheduled_on); atribuir @grid_dates como (grid_start..grid_end).to_a. Sem @summary — não necessário na view admin.

    Método privado `parse_month_param`: retornar Date.today.beginning_of_month se params[:month] não estiver presente; caso contrário Date.strptime(params[:month], "%Y-%m").beginning_of_month com rescue Date::Error retornando Date.today.beginning_of_month. Copiar verbatim de client/home_controller.rb.

    Verificar se I18n.l funciona: antes de usar I18n.l, checar se existe config/locales/pt-BR.yml ou se Gemfile tem gem "rails-i18n". Se não existir, implementar fallback com array: `MONTH_NAMES_PT = %w[janeiro fevereiro março abril maio junho julho agosto setembro outubro novembro dezembro]` e usar `"#{MONTH_NAMES_PT[@current_month.month - 1].capitalize} #{@current_month.year}"`.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/calendar_controller_test.rb 2>&1 | tail -10</automated>
  </verify>
  <done>
    - app/controllers/admin/calendar_controller.rb existe com `class Admin::CalendarController < Admin::BaseController`
    - Arquivo contém `includes(:client)` na query de artes
    - Arquivo contém `rescue Date::Error` em parse_month_param
    - bin/rails test test/controllers/admin/calendar_controller_test.rb roda sem erros (ainda 0 testes de conteúdo — OK)
    - bin/rails routes | grep calendar retorna rota válida
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar helper client_color(client) em ApplicationHelper (per D-01, CADM-03)</name>
  <files>
    app/helpers/application_helper.rb
  </files>
  <read_first>
    - app/helpers/application_helper.rb — conteúdo atual (module ApplicationHelper com include Pagy::Frontend); inserir client_color após o include
    - .planning/phases/14-calend-rio-admin/14-PATTERNS.md — seção "app/helpers/application_helper.rb": paleta de 8 cores com strings hex literais completas e lógica client.id % palette.size
    - .planning/phases/14-calend-rio-admin/14-CONTEXT.md — D-01: paleta exata de 8 pares bg/text; D-01 especifica que strings devem ser literais completas para Tailwind JIT detectar
  </read_first>
  <behavior>
    - Test 1: client_color(client_com_id_0) retorna { bg: "bg-[#F0FDF4]", text: "text-[#14A958]" } (verde — índice 0)
    - Test 2: client_color(client_com_id_8) retorna { bg: "bg-[#F0FDF4]", text: "text-[#14A958]" } (verde — 8 % 8 = 0, wraps around)
    - Test 3: client_color(client_com_id_1) retorna { bg: "bg-[#EFF6FF]", text: "text-[#2563EB]" } (azul — índice 1)
    - Test 4: Todas as 8 entradas da paleta têm keys :bg e :text com strings literais não concatenadas
  </behavior>
  <action>
    Em app/helpers/application_helper.rb: adicionar método `def client_color(client)` após o `include Pagy::Frontend`, antes do `end` do módulo.

    O método deve definir um array local `palette` com exatamente 8 hashes, cada um com chaves :bg e :text, contendo as strings literais completas das classes Tailwind (per D-01 do CONTEXT.md):
    índice 0: bg "bg-[#F0FDF4]" / text "text-[#14A958]" (verde),
    índice 1: bg "bg-[#EFF6FF]" / text "text-[#2563EB]" (azul),
    índice 2: bg "bg-[#FAF5FF]" / text "text-[#7C3AED]" (roxo),
    índice 3: bg "bg-[#FFF7ED]" / text "text-[#EA580C]" (laranja),
    índice 4: bg "bg-[#FFF0F3]" / text "text-[#E11D48]" (rosa),
    índice 5: bg "bg-[#F0FDFA]" / text "text-[#0D9488]" (teal),
    índice 6: bg "bg-[#FEFCE8]" / text "text-[#CA8A04]" (amarelo),
    índice 7: bg "bg-[#EEF2FF]" / text "text-[#4F46E5]" (índigo).

    Retornar `palette[client.id % palette.size]`. NUNCA usar interpolação de string para as classes hex (ex: `"bg-[#{hex}]"`) — Tailwind JIT não detecta classes geradas por interpolação em produção (Pitfall 3 do RESEARCH.md).
  </action>
  <verify>
    <automated>bin/rails runner "include ApplicationHelper; c = Client.new; c.id = 0; puts client_color(c).inspect" 2>&1 | grep "bg-\[#"</automated>
  </verify>
  <done>
    - app/helpers/application_helper.rb contém "def client_color"
    - Arquivo contém "palette[client.id % palette.size]"
    - Todas as 8 entradas usam strings literais completas (nenhuma interpolação)
    - bin/rails runner de verificação retorna hash com :bg e :text
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin::CalendarController → Arte (query) | Controller busca artes de TODOS os clientes — acesso restrito ao admin autenticado via Admin::BaseController |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Spoofing | Admin::CalendarController#index | mitigate | Herdado de Admin::BaseController: `before_action :require_authentication` bloqueia acesso não autenticado [VERIFIED: PATTERNS.md — Shared Patterns / Autenticação] |
| T-14-03 | Tampering | params[:month] em parse_month_param | mitigate | `rescue Date::Error` em parse_month_param retorna Date.today.beginning_of_month para qualquer input malicioso — previne 500 [VERIFIED: padrão copiado de client/home_controller.rb] |
| T-14-04 | Information Disclosure | Arte.where(...).includes(:client) expõe dados de todos os clientes | accept | Endpoint é admin-only; autenticação (T-14-02) garante que apenas o admin autenticado acessa; dados de clientes não são expostos publicamente |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote novo — sem risco de supply chain |
</threat_model>

<verification>
bin/rails routes | grep -v '^#' | grep calendar
# Deve mostrar: admin_calendar_index  GET  /admin/calendar(.:format)

grep "includes(:client)" app/controllers/admin/calendar_controller.rb
# Deve retornar linha com .includes(:client)

grep "rescue Date::Error" app/controllers/admin/calendar_controller.rb
# Deve retornar linha com rescue

grep "def client_color" app/helpers/application_helper.rb
# Deve retornar a definição do método

grep "palette\[client\.id % palette\.size\]" app/helpers/application_helper.rb
# Deve retornar a linha de retorno da paleta

bin/rails test test/controllers/admin/calendar_controller_test.rb
# Suite deve passar (ainda sem testes de conteúdo — OK neste plano)
</verification>

<success_criteria>
- Admin::CalendarController existe herdando Admin::BaseController (CADM-02 e CADM-04 implementados no controller)
- Query de artes inclui .includes(:client).order(:id) — sem N+1, ordenação determinística
- parse_month_param faz rescue Date::Error — parâmetro inválido não causa 500 (CADM-04)
- client_color(client) disponível em ApplicationHelper com paleta de 8 cores hex literais (D-01, CADM-03)
</success_criteria>

<output>
Criar `.planning/phases/14-calend-rio-admin/14-02-SUMMARY.md` ao concluir
</output>
