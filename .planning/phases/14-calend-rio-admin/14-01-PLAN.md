---
phase: 14-calend-rio-admin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/routes.rb
  - app/views/admin/shared/_sidebar.html.erb
  - test/controllers/admin/calendar_controller_test.rb
autonomous: true
requirements:
  - CADM-01

must_haves:
  truths:
    - "GET /admin/calendar retorna rota reconhecida (helper admin_calendar_index_path disponível)"
    - "Link 'Calendário' no sidebar aponta para admin_calendar_index_path, não mais '#'"
    - "Arquivo de testes existe com setup inline completo e suite roda sem erro"
  artifacts:
    - path: "config/routes.rb"
      provides: "Rota resources :calendar, only: [:index] no namespace admin"
      contains: "resources :calendar"
    - path: "app/views/admin/shared/_sidebar.html.erb"
      provides: "Link Calendário wired para admin_calendar_index_path"
      contains: "admin_calendar_index_path"
    - path: "test/controllers/admin/calendar_controller_test.rb"
      provides: "Scaffold de testes com setup inline pronto para Wave 3"
      contains: "Admin::CalendarControllerTest"
  key_links:
    - from: "app/views/admin/shared/_sidebar.html.erb"
      to: "admin_calendar_index_path"
      via: "nav_items hash"
      pattern: "admin_calendar_index_path"
    - from: "config/routes.rb"
      to: "admin/calendar#index"
      via: "resources :calendar namespace :admin"
      pattern: "resources :calendar"
---

<objective>
Criar a infraestrutura de entrada para o Calendário Admin: (1) registrar a rota `resources :calendar, only: [:index]` no namespace admin — gerando o helper `admin_calendar_index_path` (per CADM-01); (2) wirear o link "Calendário" no sidebar de `"#"` para `admin_calendar_index_path` (per CADM-01); (3) criar o arquivo de testes com setup inline pronto para os testes completos do Plano 03.

Purpose: Estes dois arquivos são pré-requisitos bloqueantes — sem a rota registrada, `admin_calendar_index_path` não existe e o sidebar lança NameError ao renderizar. Rota e sidebar DEVEM ser alterados na mesma wave (Pitfall 1 do RESEARCH.md).
Output: Rota registrada, sidebar wired, arquivo de testes scaffold.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-calend-rio-admin/14-CONTEXT.md
@.planning/phases/14-calend-rio-admin/14-RESEARCH.md
@.planning/phases/14-calend-rio-admin/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar rota calendar no namespace admin e wirear sidebar (per CADM-01)</name>
  <files>
    config/routes.rb
    app/views/admin/shared/_sidebar.html.erb
  </files>
  <read_first>
    - config/routes.rb — bloco `namespace :admin do` (linhas 7-20 aproximadamente); localizar linha `resources :approvals, only: [ :index ]` para inserir recursos de calendar imediatamente após ela, antes do `end` do namespace
    - app/views/admin/shared/_sidebar.html.erb — linha 16 aproximadamente; localizar `{ label: "Calendário", path: "#" }` e alterar apenas o valor de `path`
  </read_first>
  <action>
    Em config/routes.rb: no bloco `namespace :admin do`, após a linha `resources :approvals, only: [ :index ]`, inserir `resources :calendar,  only: [ :index ]`. Manter estilo de espaçamento existente (2 espaços, colchetes com espaços internos). Usar `resources` (plural) — NÃO `resource` singular (Pitfall: `resource` singular não gera helper `_index_path` e não define action `index` no padrão esperado, conforme Pattern 5 do RESEARCH.md).

    Em app/views/admin/shared/_sidebar.html.erb: alterar apenas a linha `{ label: "Calendário", path: "#" }` para `{ label: "Calendário", path: admin_calendar_index_path }`. Nenhuma outra linha deve ser alterada. Esta alteração DEVE ocorrer na mesma task que o registro da rota — se a rota não existir quando o sidebar for renderizado, Rails lança NameError.

    Verificar: `bin/rails routes | grep calendar` deve listar `admin_calendar_index GET /admin/calendar(.:format) admin/calendar#index`.
  </action>
  <verify>
    <automated>bin/rails routes | grep -v '^#' | grep calendar</automated>
  </verify>
  <done>
    - bin/rails routes | grep calendar mostra "admin_calendar_index  GET  /admin/calendar(.:format)  admin/calendar#index"
    - config/routes.rb contém "resources :calendar" dentro do namespace :admin
    - app/views/admin/shared/_sidebar.html.erb contém "admin_calendar_index_path" (não mais "#" para Calendário)
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar scaffold de testes Admin::CalendarControllerTest</name>
  <files>
    test/controllers/admin/calendar_controller_test.rb
  </files>
  <read_first>
    - test/controllers/admin/approvals_controller_test.rb — padrão de setup exato: User.create! com email_address/password/password_confirmation, sign_in_as(@user), Client.create! com name/password/password_confirmation, Arte.create! com todos os campos obrigatórios; replicar linha a linha
    - app/models/arte.rb — campos obrigatórios: client, scheduled_on (date), platform (enum), media_type (enum), status (enum), title, caption, approval_deadline, external_url
    - test/test_helper.rb — verificar se SessionTestHelper (sign_in_as) está definido aqui ou em support/
  </read_first>
  <action>
    Criar test/controllers/admin/calendar_controller_test.rb com classe `Admin::CalendarControllerTest < ActionDispatch::IntegrationTest`. O `setup` deve replicar o padrão exato do approvals_controller_test.rb: User.create! com email_address: "admin@example.com", password: "password", password_confirmation: "password"; sign_in_as(@user); Client.create! com name: "Test Client", password: "senha123", password_confirmation: "senha123"; Arte.create! com client: @client, scheduled_on: Date.current, platform: :instagram, media_type: :image, status: :pending, title: "Arte Teste", caption: "Legenda", approval_deadline: Date.current + 5, external_url: "https://drive.google.com/file/exemplo".

    O arquivo de testes deve incluir apenas o setup neste momento — os testes completos serão adicionados no Plano 03 (Wave 3). Incluir comentário `# Testes adicionados no plano 14-03` no corpo da classe para deixar claro o estado intencional.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/calendar_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <done>
    - test/controllers/admin/calendar_controller_test.rb existe
    - Suite roda sem erro (0 tests, 0 assertions é esperado neste plano)
    - Arquivo contém "Admin::CalendarControllerTest" e o bloco setup com User, Client e Arte
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/routes.rb → admin namespace | Qualquer rota adicionada ao namespace admin herda before_action :require_authentication do Admin::BaseController |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | GET /admin/calendar (nova rota) | mitigate | Herdado de Admin::BaseController: `before_action :require_authentication` — acesso sem sessão redireciona para /session/new [VERIFIED: base_controller.rb linhas 1-5 em PATTERNS.md] |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote novo; todo stack está em vendor/bundle — sem risco de supply chain neste plano |
</threat_model>

<verification>
bin/rails routes | grep -v '^#' | grep calendar
# Deve mostrar: admin_calendar_index  GET  /admin/calendar(.:format)  admin/calendar#index

grep -v '^#' app/views/admin/shared/_sidebar.html.erb | grep calendar
# Deve retornar a linha com admin_calendar_index_path

bin/rails test test/controllers/admin/calendar_controller_test.rb
# Deve rodar sem falhas (0 tests é OK neste plano)
</verification>

<success_criteria>
- Rota admin_calendar_index registrada e visível em `bin/rails routes`
- Link "Calendário" no sidebar aponta para admin_calendar_index_path (CADM-01 infraestrutura pronta)
- Arquivo de testes criado com setup inline correto e suite verde (0 tests, 0 assertions)
</success_criteria>

<output>
Criar `.planning/phases/14-calend-rio-admin/14-01-SUMMARY.md` ao concluir
</output>
