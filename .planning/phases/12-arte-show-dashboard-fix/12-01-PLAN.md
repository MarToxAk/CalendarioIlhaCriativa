---
phase: 12-arte-show-dashboard-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/admin/artes/show.html.erb
  - app/views/admin/dashboard/index.html.erb
autonomous: true
requirements:
  - SHOW-01
  - DASH-01
must_haves:
  truths:
    - "Na página show da arte, os botões Editar, Excluir e Marcar como Revisada aparecem ao lado do text link '← Artes' — sem nenhuma classe btn* residual"
    - "O botão Excluir é vermelho sólido (#EE3537), visualmente distinto dos demais"
    - "O botão Marcar como Revisada é verde sólido (#0F7949), mais largo (px-4) que os demais (px-3)"
    - "O link '← Artes' é texto simples, separado dos botões de ação, acima deles no mesmo flex container"
    - "No dashboard, o link 'Ver' em cada linha da tabela tem borda visível e deixou de ser texto puro"
  artifacts:
    - path: "app/views/admin/artes/show.html.erb"
      provides: "Barra de ações reestruturada com Tailwind puro"
      contains: "flex items-center gap-3 mb-6"
    - path: "app/views/admin/dashboard/index.html.erb"
      provides: "Link 'Ver' com estilo Tailwind"
      contains: "h-8 px-3 border border-gray-200 rounded-lg"
  key_links:
    - from: "app/views/admin/artes/show.html.erb"
      to: "admin_artes_path"
      via: "link_to back link"
      pattern: "admin_artes_path.*Voltar para lista de artes"
    - from: "app/views/admin/dashboard/index.html.erb"
      to: "admin_arte_path"
      via: "link_to Ver"
      pattern: "h-8 px-3 border border-gray-200"
---

<objective>
Substituir todas as classes placeholder sem CSS (btn, btn-secondary, btn-danger, btn-sm) por classes Tailwind
concretas em dois arquivos — show.html.erb de artes e dashboard/index.html.erb — sem alterar nenhum
comportamento, rota, método HTTP ou atributo data-* existente.

Purpose: Completar o polish visual do v1.3 eliminando os últimos elementos sem estilo do painel admin.
Output: show.html.erb com barra de ações reestruturada (D-01 a D-05) e dashboard/index.html.erb com link
"Ver" estilizado (D-06). Requisitos SHOW-01 e DASH-01 satisfeitos.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-arte-show-dashboard-fix/12-CONTEXT.md
@.planning/phases/12-arte-show-dashboard-fix/12-UI-SPEC.md
@.planning/phases/12-arte-show-dashboard-fix/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reestruturar barra de ações em artes/show.html.erb (D-01 a D-05)</name>
  <files>app/views/admin/artes/show.html.erb</files>

  <read_first>
    - app/views/admin/artes/show.html.erb — estado atual: linhas 16-27 contêm o div.mt-4.flex.gap-2 com os 4 elementos a substituir; linha 40 contém o submit "Salvar resposta" já estilizado com green (referência de tom para Marcar Revisada)
    - app/views/admin/clients/show.html.erb — template canônico: linhas 4-9 definem o flex items-center gap-3 mb-6 com back link em bloco + aria-label; linhas 11-12 definem o botão outline secondary; linhas 17-21 definem o botão destrutivo vermelho
    - .planning/phases/12-arte-show-dashboard-fix/12-PATTERNS.md — estrutura resultante completa da barra de ações (seção "Estrutura resultante da barra de ações")
    - .planning/phases/12-arte-show-dashboard-fix/12-UI-SPEC.md — contrato visual completo: seção "Componentes: Estado Atual → Estado Final", incluindo nota sobre form: { class: "inline" } no button_to Excluir
  </read_first>

  <action>
    Substituir o bloco `<div class="mt-4 flex gap-2">` (linhas 16-27 do arquivo atual) pela estrutura de barra de ações canônica da Fase 12.

    O novo wrapper é `<div class="flex items-center gap-3 mb-6">` (remove mt-4 e flex gap-2, aplica D-01).

    Dentro do wrapper, na ordem:

    1. Back link (D-02): `link_to` com bloco, destino `admin_artes_path`, aria `{ label: "Voltar para lista de artes" }`, classe `text-sm text-slate-600 hover:text-slate-900 transition-colors`, texto `&larr; Artes`. Sem condicional — sempre visível.

    2. Botão Editar (D-03): `link_to "Editar"`, destino `edit_admin_arte_path(@arte)`, classe `inline-flex items-center h-9 px-3 border border-gray-200 rounded-lg text-sm font-medium text-slate-700 bg-white hover:bg-gray-50 transition-colors`, condicional `if @arte.pending? || @arte.revised? || @arte.change_requested?` — preservar exatamente.

    3. Botão Excluir (D-04): `button_to "Excluir"`, destino `admin_arte_path(@arte)`, `method: :delete`, `data: { turbo_confirm: "Tem certeza?" }` (usar turbo_confirm, não confirm, para consistência com Turbo — verificar padrão existente no arquivo antes de alterar; se o arquivo usa `confirm:`, preservar como está), classe `inline-flex items-center h-9 px-3 bg-[#EE3537] hover:bg-red-700 text-white text-sm font-medium rounded-lg transition-colors`, adicionar `form: { class: "inline" }` para que o form wrapper não quebre o layout flex, condicional `if @arte.pending? && @arte.approval_responses.none?` — preservar exatamente.

    4. Botão Marcar como Revisada (D-05): dentro de `<% if @arte.change_requested? %>`, `button_to "Marcar como Revisada"`, destino `mark_revised_admin_arte_path(@arte)`, `method: :patch`, classe `inline-flex items-center h-9 px-4 bg-[#0F7949] hover:bg-[#0a5c37] text-white text-sm font-semibold rounded-lg transition-colors`, `form: { data: { turbo_confirm: "Confirmar: marcar esta arte como revisada?" } }` — preservar exatamente. Adicionar também `form: { class: "inline", data: { turbo_confirm: ... } }` para preservar layout flex.

    O `<div class="bg-white rounded-xl border border-gray-200 shadow-card p-6 max-w-2xl">` externo (linha 4) NÃO é alterado.

    O bloco condicional `<% if @arte.change_requested? || @arte.revised? %>` (linhas 30-44) NÃO é alterado.

    Após a edição, o arquivo não deve conter nenhuma das strings: `btn btn-secondary`, `btn btn-danger`, `class: "btn"`, `mt-4 flex gap-2`.
  </action>

  <verify>
    <automated>grep -n 'btn' /home/bot/calendario_livia/app/views/admin/artes/show.html.erb | grep -v '^#'</automated>
    <automated>grep -c 'flex items-center gap-3 mb-6' /home/bot/calendario_livia/app/views/admin/artes/show.html.erb</automated>
    <automated>grep -c 'EE3537' /home/bot/calendario_livia/app/views/admin/artes/show.html.erb</automated>
    <automated>grep -c '0F7949' /home/bot/calendario_livia/app/views/admin/artes/show.html.erb</automated>
    <automated>grep -c 'Voltar para lista de artes' /home/bot/calendario_livia/app/views/admin/artes/show.html.erb</automated>
    <automated>cd /home/bot/calendario_livia && bundle exec rails runner "puts 'show.html.erb parse ok'"</automated>
  </verify>

  <acceptance_criteria>
    - grep 'btn' show.html.erb retorna zero linhas (nenhuma classe btn* residual)
    - grep -c 'flex items-center gap-3 mb-6' retorna 1
    - grep -c 'EE3537' retorna 1 (botão Excluir vermelho)
    - grep -c '0F7949' retorna 2 (botão Marcar Revisada + submit Salvar resposta já existente)
    - grep -c 'Voltar para lista de artes' retorna 1 (aria-label do back link)
    - O arquivo contém a classe `h-9 px-3` para Editar e Excluir, e `h-9 px-4` para Marcar Revisada
    - O arquivo contém `form: { class: "inline"` em pelo menos um button_to para preservar layout flex
    - rails runner não lança SyntaxError (arquivo ERB parseável)
  </acceptance_criteria>

  <done>show.html.erb não contém nenhuma classe btn*, exibe back link "← Artes" como text link com aria-label, botão Excluir com #EE3537, botão Marcar Revisada com #0F7949 e px-4, todos dentro de flex items-center gap-3 mb-6. Comportamentos e rotas preservados.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Estilizar link "Ver" em dashboard/index.html.erb (D-06)</name>
  <files>app/views/admin/dashboard/index.html.erb</files>

  <read_first>
    - app/views/admin/dashboard/index.html.erb — linha 57: `link_to "Ver", admin_arte_path(arte), class: "btn btn-sm"` — substituição cirúrgica de apenas o atributo class
    - .planning/phases/12-arte-show-dashboard-fix/12-CONTEXT.md — D-06: classe exata do link "Ver" no dashboard
    - .planning/phases/11-arte-index-polish/11-CONTEXT.md — D-02: decisão original do link "Ver" (carry-forward)
  </read_first>

  <action>
    Na linha 57 do arquivo, substituir apenas o valor do atributo `class:` do `link_to "Ver"`.

    Estado atual: `class: "btn btn-sm"`

    Estado final (D-06, carry-forward de D-02 da Fase 11): `class: "h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600 hover:bg-gray-50 transition-colors"`

    Nenhuma outra linha do arquivo é alterada. O path `admin_arte_path(arte)`, o texto "Ver", a posição dentro do `<td>` e todo o restante da estrutura da tabela permanecem intactos.
  </action>

  <verify>
    <automated>grep -n 'btn' /home/bot/calendario_livia/app/views/admin/dashboard/index.html.erb | grep -v '^#'</automated>
    <automated>grep -c 'h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600' /home/bot/calendario_livia/app/views/admin/dashboard/index.html.erb</automated>
    <automated>cd /home/bot/calendario_livia && bundle exec rails runner "puts 'dashboard/index.html.erb parse ok'"</automated>
  </verify>

  <acceptance_criteria>
    - grep 'btn' dashboard/index.html.erb retorna zero linhas (nenhuma classe btn* residual)
    - grep -c 'h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600' retorna 1
    - O arquivo ainda contém `link_to "Ver", admin_arte_path(arte)` com o mesmo path inalterado
    - rails runner não lança SyntaxError
  </acceptance_criteria>

  <done>dashboard/index.html.erb não contém nenhuma classe btn*. O link "Ver" na coluna de ações da tabela exibe borda visível (border-gray-200), fundo hover (hover:bg-gray-50) e texto compacto (text-xs text-slate-600), consistente com o link "Ver" do index de artes implementado na Fase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Rails view | Todas as entradas de usuário chegam como params de request HTTP — nenhum input novo nesta fase |
| admin session → dashboard | Acesso ao dashboard requer sessão admin autenticada (implementada na Fase 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | button_to Excluir (method: :delete) | accept | CSRF token automático do Rails form_authenticity_token; method :delete não é acessível via GET; sem mudança de comportamento nesta fase |
| T-12-02 | Tampering | button_to Marcar Revisada (method: :patch) | accept | Mesmo mecanismo CSRF; condicional if @arte.change_requested? preservada; sem nova superfície de ataque |
| T-12-03 | Elevation of Privilege | link_to Ver no dashboard | accept | Rota admin_arte_path já protegida por before_action :require_admin na Fase 2; nenhuma rota nova criada |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência de pacote instalada nesta fase — apenas edição de templates ERB com classes Tailwind já em uso |

Nenhuma ameaça de severidade alta identificada. Esta fase é styling puro em templates existentes com zero novas rotas, zero novos controllers e zero novos parâmetros de entrada.
</threat_model>

<verification>
Após execução dos dois tasks:

1. `grep -rn 'btn' app/views/admin/artes/show.html.erb app/views/admin/dashboard/index.html.erb` → zero resultados (nenhuma classe placeholder residual em nenhum dos dois arquivos)
2. `grep -c 'flex items-center gap-3 mb-6' app/views/admin/artes/show.html.erb` → 1
3. `grep -c 'EE3537' app/views/admin/artes/show.html.erb` → 1 (Excluir)
4. `grep -c 'h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600' app/views/admin/dashboard/index.html.erb` → 1
5. `bundle exec rails runner "Arte; Client; puts 'models ok'"` → sem erro
6. Visualmente (checkpoint opcional): acessar `/admin/artes/:id` e `/admin` para confirmar que os botões têm cor e borda visíveis
</verification>

<success_criteria>
- SHOW-01 satisfeito: botões Editar (outline neutro), Excluir (vermelho #EE3537), Marcar Revisada (verde #0F7949) e back link "← Artes" (text link) visíveis e semânticos em show de artes
- DASH-01 satisfeito: link "Ver" no dashboard tem border-gray-200, hover:bg-gray-50, text-xs text-slate-600 — consistente com o "Ver" do index de artes (Fase 11)
- Zero classes btn*, btn-secondary, btn-danger, btn-sm remanescentes nos dois arquivos
- Milestone v1.3 Arte UI Polish completo (Fases 10, 11, 12 concluídas)
</success_criteria>

<output>
Criar `.planning/phases/12-arte-show-dashboard-fix/12-01-SUMMARY.md` ao concluir.
</output>
