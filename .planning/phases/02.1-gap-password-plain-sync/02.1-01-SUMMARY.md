---
phase: 02.1-gap-password-plain-sync
plan: 01
subsystem: auth
tags: [rails, password, client-management]

# Dependency graph
requires:
  - phase: 02-admin-auth-client-management
    provides: ClientsController com create/update actions e modelo Client com password_plain
provides:
  - update action sincroniza password_plain ao alterar senha do cliente (CLIE-04)
affects: [client-management, admin-show-client]

# Tech tracking
tech-stack:
  added: []
  patterns: [controller update action espelha lógica do create para sincronizar password_plain via merge]

key-files:
  created: []
  modified:
    - app/controllers/admin/clients_controller.rb
    - test/controllers/admin/clients_controller_test.rb

key-decisions:
  - "update action usa mesmo padrão do create: merge(password_plain: filtered[:password]) quando senha presente"

patterns-established:
  - "Sincronização password_plain: toda action que persiste senha deve usar merge(password_plain: password) antes do save/update"

requirements-completed: [CLIE-04]

# Metrics
duration: 8min
completed: 2026-05-27
---

# Phase 02.1 Plan 01: Gap Password Plain Sync Summary

**ClientsController#update corrigido para sincronizar password_plain ao alterar senha, fechando o gap CLIE-04 onde a tela de detalhes exibia senha desatualizada após edição**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-27T13:20:00Z
- **Completed:** 2026-05-27T13:28:19Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Fix do gap crítico: `update` agora sincroniza `password_plain` com `merge(password_plain: filtered[:password])` quando senha está presente no params
- Comportamento preservado: edição com senha em branco mantém a senha anterior (regressão não introduzida)
- Teste de regressão adicionado cobrindo o fluxo PATCH com nova senha verificando `password_plain` após reload

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: Sincronizar password_plain no ClientsController#update** - `929ef2e` (fix)
2. **Task 2: Teste — update com nova senha sincroniza password_plain** - `1111541` (test)

## Files Created/Modified
- `app/controllers/admin/clients_controller.rb` - Adicionado bloco `if filtered[:password].present?` com merge no método `update`
- `test/controllers/admin/clients_controller_test.rb` - Adicionado teste "update com nova senha sincroniza password_plain (CLIE-04)"

## Decisions Made
Seguiu o plano exatamente como especificado — espelhar a lógica já existente no `create` action.

## Deviations from Plan

None - plano executado exatamente como escrito.

## Issues Encountered

Ao executar os testes a partir do diretório do worktree, o Rails não conseguia conectar ao banco por falta do `.env`. Solução: copiar `.env` do repositório principal para o worktree antes de rodar os testes. Não é uma modificação de código — apenas setup de execução do worktree isolado.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Gap CLIE-04 fechado: password_plain agora sempre reflete a senha atual após edição pelo admin
- Pronto para continuar com as demais fases do roadmap

---
*Phase: 02.1-gap-password-plain-sync*
*Completed: 2026-05-27*
