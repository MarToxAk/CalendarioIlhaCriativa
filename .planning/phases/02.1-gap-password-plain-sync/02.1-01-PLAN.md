---
phase: 02.1-gap-password-plain-sync
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/controllers/admin/clients_controller.rb
  - test/controllers/admin/clients_controller_test.rb
autonomous: true
requirements:
  - CLIE-04
user_setup: []

must_haves:
  truths:
    - "Admin edita a senha de um cliente com um novo valor e a tela de detalhes exibe a nova senha corretamente"
    - "Edição com campo senha em branco continua mantendo a senha anterior (comportamento existente preservado)"
  artifacts:
    - path: "app/controllers/admin/clients_controller.rb"
      provides: "update action sincroniza password_plain quando password presente"
      contains: "password_plain: filtered[:password]"
    - path: "test/controllers/admin/clients_controller_test.rb"
      provides: "Teste cobrindo atualização de senha com password_plain sincronizado"
      contains: "password_plain"
  key_links:
    - from: "app/controllers/admin/clients_controller.rb"
      to: "app/models/client.rb"
      via: "@client.update(filtered) com password_plain sincronizado"
      pattern: "password_plain"
---

<objective>
Corrigir o gap onde o ClientsController#update não sincroniza password_plain ao alterar a senha do cliente. O create já faz isso corretamente (linhas 23-24 do controller), mas o update não. Resultado: após editar a senha, a tela de detalhes exibe a senha antiga.

Purpose: CLIE-04 exige que password_plain mostre a senha atual do cliente na tela de detalhes do admin. A edição quebra esse requisito silenciosamente.
Output: update action corrigida + teste cobrindo o caso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sincronizar password_plain no ClientsController#update</name>
  <files>app/controllers/admin/clients_controller.rb</files>
  <read_first>
    app/controllers/admin/clients_controller.rb
  </read_first>
  <action>
    No método `update` em `app/controllers/admin/clients_controller.rb`, após a linha:
    `filtered = client_params.reject { |k, v| [ "password", "password_plain" ].include?(k) && v.blank? }`

    Adicionar a sincronização de password_plain, igual ao que o create já faz:
    ```ruby
    if filtered[:password].present?
      filtered = filtered.merge(password_plain: filtered[:password])
    end
    ```

    Inserir essas 3 linhas imediatamente após a linha `filtered = ...` e antes do `if @client.update(filtered)`.

    Não alterar nenhum outro trecho do arquivo.
  </action>
  <verify>
    <automated>grep -n "password_plain: filtered\[:password\]" app/controllers/admin/clients_controller.rb</automated>
    <automated>bin/rails test test/controllers/admin/clients_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "password_plain: filtered\[:password\]" app/controllers/admin/clients_controller.rb` retorna exatamente 1 linha dentro do método `update`
    - Os testes existentes continuam passando (sem regressões)
  </acceptance_criteria>
  <done>update action sincroniza password_plain quando password está presente no params</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste — update com nova senha sincroniza password_plain</name>
  <files>test/controllers/admin/clients_controller_test.rb</files>
  <read_first>
    test/controllers/admin/clients_controller_test.rb
    app/controllers/admin/clients_controller.rb
  </read_first>
  <behavior>
    - Teste: PATCH admin_client_url com password: "novasenha99" → @client.password_plain deve ser "novasenha99" após reload
  </behavior>
  <action>
    Adicionar um novo teste em `Admin::ClientsControllerTest`, na seção `# ── update ──`, após o teste existente "update com senha em branco mantém senha original".

    Teste — "update com nova senha sincroniza password_plain (CLIE-04)":
    Faz PATCH em `admin_client_url(@client)` com `client: { name: @client.name, password: "novasenha99" }`.
    Asserta `assert_response :redirect`.
    Faz `@client.reload`.
    Asserta `assert_equal "novasenha99", @client.password_plain`.

    Não remover nem modificar os testes existentes.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/clients_controller_test.rb 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sincroniza password_plain" test/controllers/admin/clients_controller_test.rb` retorna exatamente 1 linha
    - `bin/rails test test/controllers/admin/clients_controller_test.rb` termina com `0 failures, 0 errors`
    - A contagem total de testes aumenta em 1
  </acceptance_criteria>
  <done>Teste cobre o fluxo de update com nova senha e verifica password_plain sincronizado</done>
</task>

</tasks>

<verification>
1. `bin/rails test test/controllers/admin/clients_controller_test.rb` — todos os testes passam
2. `grep -n "password_plain: filtered\[:password\]" app/controllers/admin/clients_controller.rb` — retorna 1 linha
3. Comportamento: editar senha de um cliente → show exibe nova senha
</verification>

<success_criteria>
1. Admin edita a senha de um cliente e a tela de detalhes exibe a senha correta após salvar
2. Edição com campo senha em branco mantém a senha anterior (regressão não introduzida)
3. Teste cobre o caso de atualização com nova senha
</success_criteria>

<output>
Criar `.planning/phases/02.1-gap-password-plain-sync/02.1-01-SUMMARY.md` quando concluído
</output>
