---
phase: 01-data-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Gemfile
  - config/database.yml
  - config/application.rb
  - config/tailwind.config.js
  - app/assets/stylesheets/application.css
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "O comando `rails new` cria o projeto com PostgreSQL, Tailwind CSS e importmap"
    - "`bin/rails db:create` conecta ao banco sem erro"
    - "O servidor sobe com `bin/rails server` sem erros de boot"
    - "config.time_zone retorna 'Brasilia' em `rails runner 'puts Time.zone.name'`"
    - "Todos os gems do Gemfile (rack-attack, good_job, simple_calendar, pagy, etc.) instalam sem conflito"
  artifacts:
    - path: "Gemfile"
      provides: "Dependências do projeto incluindo rack-attack ~> 6.8, good_job ~> 4.0, simple_calendar ~> 3.1, pagy ~> 9.3, active_storage_validations, bcrypt ~> 3.1, image_processing ~> 1.2"
      contains: "rack-attack"
    - path: "config/application.rb"
      provides: "Timezone Brasilia configurado"
      contains: "config.time_zone = 'Brasilia'"
    - path: "config/database.yml"
      provides: "Conexão PostgreSQL configurada"
  key_links:
    - from: "Gemfile"
      to: "bundle install"
      via: "bundler"
      pattern: "gem .rack-attack"
---

<objective>
Criar o projeto Rails 8.1.3 com a configuração correta de banco, timezone e gems. Este plano executa o `rails new`, ajusta o Gemfile com todos os gems necessários para as 6 fases do projeto, configura timezone Brasília e cria o banco de dados.

Purpose: Estabelecer a fundação do projeto antes de qualquer gerador ou migração. Todos os planos subsequentes dependem deste.

Output: Aplicação Rails funcionando em dev (`bin/rails server` sobe sem erro), banco criado, gems instalados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation-security/01-RESEARCH.md
@.planning/phases/01-data-foundation-security/01-UI-SPEC.md
@.planning/phases/01-data-foundation-security/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar projeto Rails 8.1.3 e configurar Gemfile completo</name>
  <files>
    Gemfile
    config/database.yml
    config/application.rb
  </files>
  <read_first>
    Leia .planning/phases/01-data-foundation-security/01-RESEARCH.md seção "1. Setup do Projeto Rails 8" (comando exato) e seção "8. Gems a Adicionar" (Gemfile completo) antes de qualquer ação.
  </read_first>
  <action>
    Passo 1 — Criar o projeto. No diretório /home/bot/calendario_livia, executar:

    rails new . --database=postgresql --css=tailwind --javascript=importmap --skip-thruster

    (O diretório já existe — usar `.` como destino. Se o rails new perguntar sobre sobrescrever arquivos existentes, aceitar com `y`.)

    Passo 2 — Substituir o conteúdo do Gemfile pelo Gemfile da seção "8. Gems a Adicionar" do RESEARCH.md. O Gemfile DEVE conter exatamente estas gems com estas versões:
    - ruby "3.3.3"
    - gem "rails", "~> 8.1.3"
    - gem "pg", "~> 1.5"
    - gem "puma", ">= 5.0"
    - gem "turbo-rails"
    - gem "stimulus-rails"
    - gem "tailwindcss-rails"
    - gem "bcrypt", "~> 3.1"
    - gem "rack-attack", "~> 6.8"
    - gem "image_processing", "~> 1.2"
    - gem "simple_calendar", "~> 3.1"
    - gem "pagy", "~> 9.3"
    - gem "good_job", "~> 4.0"
    - gem "active_storage_validations"
    - gem "dotenv-rails", groups: [:development, :test]
    - group :development, :test: debug, brakeman, rubocop-rails-omakase
    - group :development: web-console

    Passo 3 — Rodar `bundle install` para instalar todos os gems.

    Passo 4 — Editar config/database.yml. O bloco `default` deve ter:
    - adapter: postgresql
    - encoding: unicode
    - pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    - username: <%= ENV.fetch("POSTGRES_USER", ENV["USER"]) %>
    - password: <%= ENV.fetch("POSTGRES_PASSWORD", "") %>
    - host: <%= ENV.fetch("POSTGRES_HOST", "localhost") %>

    O bloco `development` deve ter `database: calendario_livia_development`. O bloco `test` deve ter `database: calendario_livia_test`.

    Passo 5 — Criar o banco: `bin/rails db:create`
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && grep -c "rack-attack" Gemfile && grep -c "good_job" Gemfile && grep -c "simple_calendar" Gemfile && bin/rails db:version 2>&1 | grep -E "Current version|No migration"</automated>
  </verify>
  <acceptance_criteria>
    - Gemfile contém `gem "rack-attack", "~> 6.8"`
    - Gemfile contém `gem "good_job", "~> 4.0"`
    - Gemfile contém `gem "simple_calendar", "~> 3.1"`
    - Gemfile contém `gem "pagy", "~> 9.3"`
    - Gemfile contém `gem "active_storage_validations"`
    - `bin/rails db:create` exit 0 ou banco já existe (mensagem "already exists")
    - `bin/rails db:version` exit 0 e retorna "Current version: 0" ou "No migration"
  </acceptance_criteria>
  <done>Projeto Rails criado, Gemfile com todos os gems instalados, banco PostgreSQL criado e conectado.</done>
</task>

<task type="auto">
  <name>Task 2: Configurar timezone Brasilia e Tailwind com design tokens</name>
  <files>
    config/application.rb
    config/tailwind.config.js
    app/assets/stylesheets/application.css
  </files>
  <read_first>
    Leia config/application.rb (estado atual após rails new) e .planning/phases/01-data-foundation-security/01-UI-SPEC.md seções "1.1 Paleta de Cores", "1.2 Tipografia", "1.3 Espaçamento" antes de editar.
  </read_first>
  <action>
    Passo 1 — Em config/application.rb, dentro do bloco `class Application < Rails::Application`, adicionar as seguintes linhas após `config.load_defaults`:

    config.time_zone = 'Brasilia'
    config.active_record.default_timezone = :local

    Passo 2 — Em config/tailwind.config.js, dentro de `theme.extend`, adicionar as cores, fontes e espaçamentos do design system da UI-SPEC. A configuração deve estender o tema com:

    colors: {
      brand: { dark: '#0F7949', mid: '#14A958', yellow: '#FAED23', coral: '#EE3537' },
      accent: { DEFAULT: '#EA580C', hover: '#C2410C', light: '#FFF7ED' },
      sidebar: { bg: '#0F7949', 'item-active': '#14A958' }
    }

    fontFamily: {
      sans: ['Inter', 'Helvetica Neue', 'Arial', 'sans-serif']
    }

    Passo 3 — Em app/assets/stylesheets/application.css, após as diretivas @tailwind, adicionar as CSS custom properties da UI-SPEC (--color-brand-dark, --color-accent, --color-bg, --color-surface, --color-border, etc.) e os keyframes fadeInUp, shake, spin dentro de uma regra @layer base ou como CSS global. Incluir a regra @media (prefers-reduced-motion: reduce).

    Passo 4 — Verificar timezone: `bin/rails runner "puts Time.zone.name"`
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && grep "config.time_zone" config/application.rb && bin/rails runner "puts Time.zone.name" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - config/application.rb contém `config.time_zone = 'Brasilia'`
    - config/application.rb contém `config.active_record.default_timezone = :local`
    - `bin/rails runner "puts Time.zone.name"` imprime `Brasilia`
    - config/tailwind.config.js contém a cor `#0F7949` na seção brand.dark
    - app/assets/stylesheets/application.css contém `--color-brand-dark: #0F7949`
    - app/assets/stylesheets/application.css contém `prefers-reduced-motion`
  </acceptance_criteria>
  <done>Timezone Brasilia ativo e design tokens do UI-SPEC disponíveis no Tailwind e como CSS custom properties.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ambiente local → Gemfile | Gems instalados via RubyGems.org — supply chain |
| database.yml → PostgreSQL | Credenciais de banco em texto; em dev sem senha |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Gemfile / bundle install | mitigate | Auditoria de legitimidade de gems concluída no RESEARCH.md — todos os gems aprovados (rack-attack 140M downloads, good_job 7.4M, etc.) |
| T-01-02 | Information Disclosure | config/database.yml | accept | Credenciais de dev sem senha; NUNCA commitar database.yml com senha de produção; usar ENV vars conforme configurado |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Não há installs npm/pip/cargo neste plano; tailwindcss-rails usa binary standalone |

</threat_model>

<verification>
`bin/rails server` inicia sem erros de boot.
`bin/rails runner "puts Time.zone.name"` retorna `Brasilia`.
`bin/rails db:version` retorna sem erro.
`grep -c "rack-attack" Gemfile` retorna 1.
</verification>

<success_criteria>
Projeto Rails 8.1.3 criado com PostgreSQL, Tailwind e importmap. Todos os gems instalados. Banco criado. Timezone Brasilia configurado. Design tokens do UI-SPEC disponíveis. `bin/rails server` sobe sem erros.
</success_criteria>

<output>
Criar `.planning/phases/01-data-foundation-security/01-01-SUMMARY.md` quando concluído.
</output>
