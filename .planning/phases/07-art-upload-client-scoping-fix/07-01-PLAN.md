---
phase: 07-art-upload-client-scoping-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/admin/artes_controller.rb
autonomous: true
requirements:
  - ARTE-10

must_haves:
  truths:
    - "Após set_arte, @client está disponível em show, edit, update, destroy e mark_revised sem que a view precise fazer @arte.client"
    - "Acessar /admin/artes/:id de qualquer arte não gera erro NoMethodError por @client nil em view ou layout"
  artifacts:
    - path: "app/controllers/admin/artes_controller.rb"
      provides: "set_arte com @client derivado da arte"
      contains: "@client = @arte.client"
  key_links:
    - from: "app/controllers/admin/artes_controller.rb"
      to: "app/models/arte.rb"
      via: "@arte.client (belongs_to :client)"
      pattern: "@client = @arte\\.client"
---

<objective>
Adicionar `@client = @arte.client` ao final do método privado `set_arte` em `Admin::ArtesController`.

Purpose: ARTE-10 exige que set_arte exponha `@client` para todas as actions que o usam (show, edit, update, destroy, mark_revised), eliminando o risco de @client nil nas views e garantindo que o contexto de cliente esteja disponível sem depender de params[:client_id].

Output: `app/controllers/admin/artes_controller.rb` com `set_arte` atualizado — uma linha adicionada, nenhuma outra mudança.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-art-upload-client-scoping-fix/07-CONTEXT.md
@.planning/phases/07-art-upload-client-scoping-fix/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar @client = @arte.client em set_arte</name>
  <files>app/controllers/admin/artes_controller.rb</files>
  <read_first>
    - app/controllers/admin/artes_controller.rb — ler o método set_arte atual (linhas 58-60) e verificar todos os before_actions antes de modificar
    - app/controllers/admin/base_controller.rb — confirmar herança e métodos disponíveis (require_authentication)
  </read_first>
  <action>
    No arquivo `app/controllers/admin/artes_controller.rb`, localizar o método privado `set_arte` (atualmente linhas 58-60):

    ```
    def set_arte
      @arte = Arte.includes(:approval_responses).find(params[:id])
    end
    ```

    Adicionar a linha `@client = @arte.client` imediatamente após a linha que atribui `@arte`, resultando em:

    ```
    def set_arte
      @arte = Arte.includes(:approval_responses).find(params[:id])
      @client = @arte.client
    end
    ```

    Nenhuma outra linha do controller é alterada. `set_client` (linhas 62-64) permanece intacto — ele é before_action apenas de :new e :create, e continuar retornando nil quando não há params[:client_id] é comportamento válido (D-05, per D-06).
  </action>
  <verify>
    <automated>grep -n "@client = @arte.client" app/controllers/admin/artes_controller.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@client = @arte.client" app/controllers/admin/artes_controller.rb` retorna exatamente uma linha dentro do método `set_arte`
    - O método `set_arte` tem exatamente 2 linhas de corpo: a que atribui `@arte` e a que atribui `@client`
    - `set_client` permanece inalterado: `@client = Client.find_by(id: params[:client_id])`
    - `bin/rails runner "puts Admin::ArtesController.instance_method(:set_arte).source_location"` não levanta exceção
    - Não há sintaxe inválida: `bundle exec ruby -c app/controllers/admin/artes_controller.rb` retorna "Syntax OK"
  </acceptance_criteria>
  <done>set_arte atribui @arte e @client; todas as actions que usam set_arte (show, edit, update, destroy, mark_revised) têm @client disponível sem mudança adicional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| URL → controller | params[:id] vem do cliente HTTP — qualquer inteiro pode ser enviado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | set_arte / params[:id] | accept | Sistema single-admin autenticado via require_authentication herdado de BaseController. Não há múltiplos admins — exposição cross-user não se aplica. ActiveRecord lança RecordNotFound (404) para IDs inexistentes. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova instalada neste plano. |
</threat_model>

<verification>
- bundle exec ruby -c app/controllers/admin/artes_controller.rb retorna "Syntax OK"
- grep -c "@client = @arte.client" app/controllers/admin/artes_controller.rb retorna 1
- grep "set_client" app/controllers/admin/artes_controller.rb mostra a linha original intacta (Client.find_by)
</verification>

<success_criteria>
set_arte expõe @client = @arte.client em uma única linha adicional. O controller passa na verificação de sintaxe Ruby. Nenhum outro método foi alterado.
</success_criteria>

<output>
Criar `.planning/phases/07-art-upload-client-scoping-fix/07-01-SUMMARY.md` ao concluir.
</output>
