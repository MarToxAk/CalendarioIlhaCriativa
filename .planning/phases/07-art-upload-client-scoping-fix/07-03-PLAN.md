---
phase: 07-art-upload-client-scoping-fix
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - app/controllers/admin/artes_controller.rb
autonomous: true
requirements:
  - ARTE-10
gap_closure: true

must_haves:
  truths:
    - "set_arte usa @client.artes.find(params[:id]) quando @client está presente, levantando RecordNotFound para IDs de outras contas"
    - "set_arte cai no find irrestrito apenas quando @client é nil, e nesse caso atribui @client = @arte.client"
    - "index action filtra artes por client_id quando params[:client_id] está presente"
    - "O comentário '# Filtering logic can be added here' não existe mais no arquivo"
    - "A declaração before_action :set_client inclui os actions show, edit, update, destroy, mark_revised além de new e create"
  artifacts:
    - path: "app/controllers/admin/artes_controller.rb"
      provides: "Proteção cross-client em set_arte e filtragem no index"
      contains: "@client.artes.includes(:approval_responses).find(params[:id])"
  key_links:
    - from: "artes_controller.rb set_arte"
      to: "Client#artes association"
      via: "@client.artes.includes(:approval_responses).find(params[:id])"
      pattern: "@client\\.artes\\.includes"
    - from: "artes_controller.rb index"
      to: "Arte model"
      via: "Arte.where(client_id: params[:client_id]) quando params[:client_id].present?"
      pattern: "params\\[:client_id\\]\\.present\\?"
---

<objective>
Fecha os blockers SC3 e SC4 da VERIFICATION.md da fase 07: adiciona proteção cross-client em `set_arte` e filtragem por cliente no `index`, removendo o comentário de dívida não rastreável.

Purpose: REQUIREMENTS.md define ARTE-10 como "evitando acesso cross-client" e o ROADMAP SC3 exige que admin não consiga acessar arte de outro cliente mesmo manipulando a URL. SC4 exige que o índice liste apenas artes do cliente selecionado. Nenhum dos dois estava implementado após os planos 07-01 e 07-02.

Output: `app/controllers/admin/artes_controller.rb` com set_arte escopado por @client quando disponível, index com filtragem condicional por client_id, e zero comentários de dívida sem referência rastreável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-art-upload-client-scoping-fix/07-CONTEXT.md
@.planning/phases/07-art-upload-client-scoping-fix/07-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar proteção cross-client em set_arte e ampliar set_client (SC3 — ARTE-10)</name>
  <files>app/controllers/admin/artes_controller.rb</files>

  <read_first>
    - app/controllers/admin/artes_controller.rb — ler o arquivo completo antes de qualquer edição; verificar linhas 1-5 (before_actions), 58-65 (set_arte e set_client atuais)
    - .planning/phases/07-art-upload-client-scoping-fix/07-CONTEXT.md — confirmar D-07 (single-admin) e entender por que o find irrestrito é o fallback correto quando @client é nil
  </read_first>

  <behavior>
    - Quando params[:client_id] está presente na URL de show/edit/update/destroy/mark_revised: set_client é executado primeiro (before_action declarado antes de set_arte), seta @client; set_arte usa @client.artes.includes(:approval_responses).find(params[:id]) — levanta ActiveRecord::RecordNotFound se o ID não pertence ao cliente
    - Quando params[:client_id] está ausente (acesso direto por /admin/artes/:id sem client_id): @client é nil após set_client; set_arte usa Arte.includes(:approval_responses).find(params[:id]) e atribui @client = @arte.client
    - Actions new e create continuam recebendo set_client (já declarado); set_arte não roda para eles (sem alteração)
  </behavior>

  <action>
  Faça duas alterações cirúrgicas no arquivo:

  **Alteração 1 — before_action :set_client (linha 3):**
  Altere a lista `only:` de `%i[new create]` para `%i[new create show edit update destroy mark_revised]`.
  A declaração de `before_action :set_client` DEVE aparecer ANTES de `before_action :set_arte` no arquivo — isso já é o caso nas linhas 2-3, e não deve mudar. A ordem garante que @client esteja disponível quando set_arte for executado.

  **Alteração 2 — método privado set_arte (linhas 58-61):**
  Substitua o corpo atual:
    `@arte = Arte.includes(:approval_responses).find(params[:id])`
    `@client = @arte.client`

  Pelo corpo condicional (per ARTE-10 e D-07 do CONTEXT.md):
    `if @client`
    `  @arte = @client.artes.includes(:approval_responses).find(params[:id])`
    `else`
    `  @arte = Arte.includes(:approval_responses).find(params[:id])`
    `  @client = @arte.client`
    `end`

  Nenhuma outra linha do arquivo deve ser alterada nesta task.
  </action>

  <verify>
    <automated>bundle exec ruby -c app/controllers/admin/artes_controller.rb</automated>
    <automated>grep -n "set_client" app/controllers/admin/artes_controller.rb | head -5</automated>
    <automated>grep -n "@client\.artes\.includes" app/controllers/admin/artes_controller.rb</automated>
    <automated>grep -n "@client = @arte\.client" app/controllers/admin/artes_controller.rb</automated>
  </verify>

  <acceptance_criteria>
    - `bundle exec ruby -c app/controllers/admin/artes_controller.rb` retorna "Syntax OK"
    - `grep -n "set_client" app/controllers/admin/artes_controller.rb` mostra a declaração `before_action :set_client` com `only:` contendo `show`, `edit`, `update`, `destroy`, `mark_revised` (além de `new` e `create`) — todos na mesma linha ou bloco
    - `grep -n "@client\.artes\.includes" app/controllers/admin/artes_controller.rb` retorna exatamente 1 ocorrência dentro de `set_arte`
    - `grep -n "@client = @arte\.client" app/controllers/admin/artes_controller.rb` retorna exatamente 1 ocorrência (dentro do ramo `else` de `set_arte`)
    - `grep -n "if @client" app/controllers/admin/artes_controller.rb` retorna pelo menos 1 ocorrência em `set_arte`
    - A declaração `before_action :set_client` aparece em linha numericamente menor que `before_action :set_arte` (verificar com `grep -n "before_action :" app/controllers/admin/artes_controller.rb`)
  </acceptance_criteria>

  <done>set_arte usa @client.artes.find quando @client está presente; fallback irrestrito quando nil; before_action :set_client declarado antes de :set_arte e expandido para incluir show/edit/update/destroy/mark_revised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar filtragem por cliente no index e remover comentário de dívida (SC4)</name>
  <files>app/controllers/admin/artes_controller.rb</files>

  <read_first>
    - app/controllers/admin/artes_controller.rb — ler novamente após Task 1 para ver o estado atual do método index (linhas 7-13 no estado pré-Task2); confirmar que o comentário `# Filtering logic can be added here` ainda existe na linha 12
    - .planning/phases/07-art-upload-client-scoping-fix/07-VERIFICATION.md — seção Gaps Summary / BLOCKER 2 para confirmar o que deve ser implementado e o que remove o bloqueio
  </read_first>

  <behavior>
    - Quando params[:client_id] está presente: index carrega apenas artes daquele cliente usando `Arte.where(client_id: params[:client_id]).includes(:client).order(scheduled_on: :desc)`
    - Quando params[:client_id] está ausente: index carrega todas as artes (comportamento atual, sem regressão)
    - O comentário `# Filtering logic can be added here` não existe mais no arquivo após esta task
  </behavior>

  <action>
  Substitua o método `index` inteiro. O método atual (após Task 1) é:

    def index
      @artes = Arte.includes(:client).order(scheduled_on: :desc)
      @clients = Client.all
      @status_options = Arte.statuses.keys
      @platform_options = Arte.platforms.keys
      # Filtering logic can be added here
    end

  O método novo deve ser:

    def index
      @artes = if params[:client_id].present?
        Arte.where(client_id: params[:client_id]).includes(:client).order(scheduled_on: :desc)
      else
        Arte.includes(:client).order(scheduled_on: :desc)
      end
      @clients = Client.all
      @status_options = Arte.statuses.keys
      @platform_options = Arte.platforms.keys
    end

  O comentário `# Filtering logic can be added here` é removido ao ser substituído pela implementação real. Não adicionar nenhum comentário novo.
  </action>

  <verify>
    <automated>bundle exec ruby -c app/controllers/admin/artes_controller.rb</automated>
    <automated>grep -c "Filtering logic can be added here" app/controllers/admin/artes_controller.rb</automated>
    <automated>grep -n "params\[:client_id\]\.present?" app/controllers/admin/artes_controller.rb</automated>
    <automated>grep -n "Arte\.where(client_id:" app/controllers/admin/artes_controller.rb</automated>
  </verify>

  <acceptance_criteria>
    - `bundle exec ruby -c app/controllers/admin/artes_controller.rb` retorna "Syntax OK"
    - `grep -c "Filtering logic can be added here" app/controllers/admin/artes_controller.rb` retorna `0` (comentário removido)
    - `grep -n "params\[:client_id\]\.present?" app/controllers/admin/artes_controller.rb` retorna exatamente 1 ocorrência dentro do método `index`
    - `grep -n "Arte\.where(client_id:" app/controllers/admin/artes_controller.rb` retorna exatamente 1 ocorrência dentro do método `index`
    - `grep -n "Arte\.includes(:client)\.order" app/controllers/admin/artes_controller.rb` retorna exatamente 1 ocorrência (no ramo `else` do index)
    - Nenhum outro comentário do tipo `# TODO`, `# FIXME`, `# can be added`, `# will be` presente em `artes_controller.rb` (verificar com `grep -iE "TODO|FIXME|can be added|will be" app/controllers/admin/artes_controller.rb`)
  </acceptance_criteria>

  <done>index action filtra por params[:client_id] quando presente; carrega todas as artes quando ausente; comentário de dívida removido; sintaxe Ruby válida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| URL params → set_arte | params[:id] é controlado pelo usuário; sem escopamento, permite acesso a qualquer Arte por ID numérico |
| URL params → index | params[:client_id] é controlado pelo usuário; sem validação, pode ser usado para enumerar artes de clientes arbitrários |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | set_arte / Arte.find irrestrito | mitigate | Task 1: when @client present, use @client.artes.find — RecordNotFound levantado automaticamente para IDs fora do escopo do cliente |
| T-07-03-02 | Information Disclosure | index / Arte.all irrestrito | mitigate | Task 2: when params[:client_id].present?, scope to Arte.where(client_id: params[:client_id]) |
| T-07-03-03 | Tampering | params[:client_id] não validado em index | accept | Sistema single-admin (D-07 CONTEXT.md): admin tem acesso legítimo a todos os clientes; client_id é apenas filtro de conveniência, não boundary de segurança |
| T-07-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta task |
</threat_model>

<verification>
Após execução de ambas as tasks, executar na ordem:

```bash
# Sintaxe Ruby
bundle exec ruby -c app/controllers/admin/artes_controller.rb

# SC3: before_action :set_client ampliado
grep -n "before_action :set_client" app/controllers/admin/artes_controller.rb

# SC3: before_action :set_client antes de :set_arte
grep -n "before_action :" app/controllers/admin/artes_controller.rb | head -5

# SC3: proteção cross-client em set_arte
grep -n "@client\.artes\.includes" app/controllers/admin/artes_controller.rb

# SC4: filtragem no index
grep -n "params\[:client_id\]\.present?" app/controllers/admin/artes_controller.rb

# SC4: comentário de dívida removido
grep -c "Filtering logic can be added here" app/controllers/admin/artes_controller.rb
# Expected: 0

# Ausência de outros marcadores de dívida
grep -iE "TODO|FIXME|can be added|will be" app/controllers/admin/artes_controller.rb
# Expected: nenhuma saída
```

Todos os checks devem passar antes de marcar o plano como completo.
</verification>

<success_criteria>
- SC3 (ARTE-10): `set_arte` usa `@client.artes.includes(:approval_responses).find(params[:id])` quando `@client` está presente; levanta `ActiveRecord::RecordNotFound` para IDs de outras contas automaticamente via Active Record
- SC4: `index` action filtra por `params[:client_id]` quando presente; sem parâmetro carrega todas as artes (comportamento existente preservado)
- Zero comentários de dívida não rastreáveis em `artes_controller.rb`
- Sintaxe Ruby válida (`bundle exec ruby -c` retorna "Syntax OK")
- `before_action :set_client` declarado em linha numericamente anterior a `before_action :set_arte` no arquivo
</success_criteria>

<output>
Criar `.planning/phases/07-art-upload-client-scoping-fix/07-03-SUMMARY.md` ao concluir, seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
```
