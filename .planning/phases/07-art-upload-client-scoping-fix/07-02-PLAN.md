---
phase: 07-art-upload-client-scoping-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/admin/artes/_form.html.erb
autonomous: true
requirements:
  - ARTE-08
  - ARTE-09

must_haves:
  truths:
    - "Admin que navega direto para /admin/artes/new (sem ?client_id=X) vê um <select> de clientes e consegue criar a arte"
    - "Admin que navega via página do cliente (client_id presente) vê hidden_field com client_id pré-preenchido — comportamento inalterado"
    - "Quando o form é re-exibido após falha de validação, os erros de :base (media_source_present, only_one_media_source) são visíveis na tela"
    - "Upload de arquivo e link externo funcionam após o selector de cliente estar correto"
  artifacts:
    - path: "app/views/admin/artes/_form.html.erb"
      provides: "selector condicional de cliente + bloco de erros de base"
      contains: "arte.client_id.present?"
    - path: "app/views/admin/artes/_form.html.erb"
      provides: "bloco de erros :base visível"
      contains: "arte.errors[:base]"
  key_links:
    - from: "app/views/admin/artes/_form.html.erb"
      to: "app/models/arte.rb"
      via: "arte.client_id.present? e arte.errors[:base]"
      pattern: "arte\\.client_id\\.present\\?"
    - from: "app/views/admin/artes/_form.html.erb"
      to: "app/controllers/admin/artes_controller.rb"
      via: "Arte.new(client: @client) — quando @client nil, arte.client_id nil → selector exibido"
      pattern: "f\\.select :client_id"
---

<objective>
Corrigir `app/views/admin/artes/_form.html.erb` com duas mudanças:

1. Substituir o `f.hidden_field :client_id` incondicional por lógica condicional: se `arte.client_id.present?`, mantém o hidden_field; caso contrário, exibe `f.select :client_id` populado com `Client.order(:name)`.

2. Adicionar bloco de exibição de `arte.errors[:base]` no topo do form, para que erros de validação customizados (`media_source_present`, `only_one_media_source`) sejam visíveis ao re-renderizar o form após falha.

Purpose: D-01: o bug raiz é que @client nil faz `Arte.new(client: nil)` → validação falha antes de qualquer upload. D-03: quando @client é nil (admin navegou direto para /admin/artes/new), exibir f.select :client_id populado com Client.order(:name). D-04: quando @client está presente (admin chegou via página do cliente), manter hidden_field :client_id com valor pré-preenchido — fluxo normal intacto. ARTE-09 — arte nunca é criada sem client_id válido; quando o admin chega direto em /admin/artes/new, o selector garante que ele escolha um cliente antes de submeter. D-02: ARTE-08 — o erro de "Precisa de arquivo ou link externo" agora é exibido, tornando o upload depurável e funcional.

Output: `_form.html.erb` com selector condicional e bloco de erros. Nenhum outro arquivo é alterado neste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-art-upload-client-scoping-fix/07-CONTEXT.md
@.planning/phases/07-art-upload-client-scoping-fix/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selector condicional de cliente no _form.html.erb</name>
  <files>app/views/admin/artes/_form.html.erb</files>
  <read_first>
    - app/views/admin/artes/_form.html.erb — ler o arquivo completo antes de qualquer edição; focar linha 43 (hidden_field :client_id) e linhas 42-46 (div flex gap-2 com botões)
    - app/views/admin/artes/_form.html.erb linhas 20-26 — padrão de f.select existente (:platform, :media_type) para replicar estrutura de div e classes
    - .planning/phases/07-art-upload-client-scoping-fix/07-PATTERNS.md — seção "selector condicional" para o padrão exato a implementar
  </read_first>
  <action>
    No arquivo `app/views/admin/artes/_form.html.erb`:

    PASSO 1 — Localizar a `<div class="flex gap-2">` (atualmente linha 42) que contém `f.hidden_field :client_id`, o botão submit e o link Cancelar.

    PASSO 2 — Remover a linha `<%= f.hidden_field :client_id %>` de dentro do `<div class="flex gap-2">`.

    PASSO 3 — Inserir o bloco condicional de client_id IMEDIATAMENTE ANTES da `<div class="flex gap-2">` (os botões permanecem onde estão):

    O bloco condicional deve ser:
    - Se `arte.client_id.present?`: renderizar `f.hidden_field :client_id` (sem wrapper div — é campo oculto)
    - Se não: renderizar um `<div class="mb-4">` com `f.label :client_id` com texto "Cliente" e classe "block text-sm font-medium mb-1", seguido de `f.select :client_id` com collection `Client.order(:name).map { |c| [c.name, c.id] }`, options hash `{ prompt: "Selecione o cliente" }`, e html_options `class: "form-input w-full"`

    A estrutura exata do bloco else (copiada do 07-PATTERNS.md):
    - wrapper: `<div class="mb-4">`
    - label: `<%= f.label :client_id, "Cliente", class: "block text-sm font-medium mb-1" %>`
    - select: `<%= f.select :client_id, Client.order(:name).map { |c| [c.name, c.id] }, { prompt: "Selecione o cliente" }, class: "form-input w-full" %>`
    - fechar `</div>`

    PASSO 4 — Verificar que o `<div class="flex gap-2">` ficou sem o hidden_field, contendo apenas o botão submit e o link Cancelar — exatamente como estava antes exceto pela remoção do hidden_field.

    Não alterar nenhuma outra linha do form. O Stimulus `media-type-toggle` (data-controller, targets, actions) permanece intacto.
  </action>
  <verify>
    <automated>grep -n "arte.client_id.present?" app/views/admin/artes/_form.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - `grep "arte.client_id.present?" app/views/admin/artes/_form.html.erb` retorna uma linha com o condicional ERB
    - `grep "f.select :client_id" app/views/admin/artes/_form.html.erb` retorna uma linha dentro do bloco else
    - `grep "Client.order(:name)" app/views/admin/artes/_form.html.erb` retorna uma linha (collection do select)
    - `grep 'prompt: "Selecione o cliente"' app/views/admin/artes/_form.html.erb` retorna uma linha
    - `grep "flex gap-2" app/views/admin/artes/_form.html.erb` retorna uma linha — a div dos botões
    - O hidden_field :client_id NÃO aparece dentro da div "flex gap-2": `grep -A5 'flex gap-2' app/views/admin/artes/_form.html.erb` não contém "hidden_field"
    - `bundle exec erb -T - app/views/admin/artes/_form.html.erb` não levanta erro de sintaxe ERB (se erb CLI disponível; caso contrário, `bin/rails runner "ActionView::Template::Handlers::ERB.erb_implementation.new(File.read('app/views/admin/artes/_form.html.erb'), trim_mode: '-').result"` não levanta exceção de sintaxe)
  </acceptance_criteria>
  <done>_form.html.erb exibe selector de clientes quando arte.client_id é nil, e hidden_field quando client_id já está presente. Admin pode criar arte navegando direto para /admin/artes/new.</done>
</task>

<task type="auto">
  <name>Task 2: Exibir erros de :base no form de arte</name>
  <files>app/views/admin/artes/_form.html.erb</files>
  <read_first>
    - app/views/admin/artes/_form.html.erb — ler o estado atual (após Task 1) para localizar onde inserir o bloco de erros; o bloco deve vir logo após a abertura do `form_with` (primeira linha do bloco ERB, antes do primeiro `<div class="mb-4">`)
    - app/views/admin/clients/_form.html.erb — padrão de exibição de erros por campo (linhas 15-18) como referência de classe CSS; adaptar para erros de :base
    - app/models/arte.rb — confirmar que erros customizados usam `errors.add(:base, ...)` (linhas 21-23 e 25-27)
    - .planning/phases/07-art-upload-client-scoping-fix/07-PATTERNS.md — seção "exibição de erros de :base" para o padrão de bloco
  </read_first>
  <action>
    No arquivo `app/views/admin/artes/_form.html.erb`, inserir bloco de erros de `:base` logo após a linha de abertura do `form_with` (linha 2, que começa com `<%= form_with model: arte`), antes do primeiro `<div class="mb-4">` (campo :title).

    O bloco a inserir:
    - Condicional: `if arte.errors[:base].any?`
    - Wrapper: `<div class="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-sm text-red-700">`
    - Iteração: `arte.errors[:base].each do |msg|` → `<p><%= msg %></p>`
    - Fechar divs e end

    Este bloco cobre os erros de validação customizados do model Arte:
    - "Precisa de arquivo ou link externo" (media_source_present, errors.add(:base))
    - "Use arquivo OU link externo, não ambos" (only_one_media_source, errors.add(:base))

    Também cobre erros de :client quando arte.errors[:client].any? é relevante — mas dado que o selector resolve o problema de client_id, erros de :base são a prioridade. Não é necessário adicionar bloco separado para :client neste momento.

    Não mover nem alterar nenhuma outra linha do form além da inserção do bloco.
  </action>
  <verify>
    <automated>grep -n "arte.errors\[:base\]" app/views/admin/artes/_form.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - `grep "arte.errors\[:base\]" app/views/admin/artes/_form.html.erb` retorna pelo menos 1 linha (o condicional)
    - O bloco aparece ANTES do primeiro campo :title: o número de linha retornado pelo grep acima é menor que a linha com "f.label :title"
    - `grep "bg-red-50" app/views/admin/artes/_form.html.erb` retorna uma linha (o wrapper do bloco de erros)
    - O arquivo completo ainda tem o form_with, todos os campos originais, o selector condicional de cliente (Task 1), e os botões — nenhum campo foi removido
    - `grep -c "mb-4" app/views/admin/artes/_form.html.erb` é maior ou igual ao número de divs mb-4 que existiam antes (campos não foram removidos)
  </acceptance_criteria>
  <done>Erros de :base são exibidos no topo do form quando o form é re-renderizado após falha de validação. "Precisa de arquivo ou link externo" e "Use arquivo OU link externo, não ambos" ficam visíveis para o admin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| form submit → controller | params[:arte][:client_id] vem do browser — pode ser manipulado |
| form submit → controller | params[:arte][:media_file] é upload de arquivo do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | params[:arte][:client_id] via select | accept | Sistema single-admin. O admin é o único usuário autenticado. Não há isolamento entre clientes necessário do ponto de vista de segurança neste sistema. arte_params já permite :client_id e o model valida presença. |
| T-07-02-02 | Tampering | params[:arte][:media_file] upload | accept | ActiveStorage valida o arquivo no backend. Armazenamento local (config/storage.yml usa :local). ASVS L1 não exige validação de content-type no lado servidor para sistema single-admin interno. |
| T-07-02-03 | Information Disclosure | arte.errors exibidos na view | accept | Erros são gerados pelo model da própria aplicação (não refletem input do usuário cru). Não há PII exposto nos erros de :base definidos em arte.rb. |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma dependência nova instalada neste plano. Alterações são apenas em ERB/view. |
</threat_model>

<verification>
- grep -n "arte.client_id.present?" app/views/admin/artes/_form.html.erb — retorna 1 resultado
- grep -n "f.select :client_id" app/views/admin/artes/_form.html.erb — retorna 1 resultado
- grep -n "arte.errors\[:base\]" app/views/admin/artes/_form.html.erb — retorna pelo menos 1 resultado
- grep "hidden_field :client_id" app/views/admin/artes/_form.html.erb — retorna 1 resultado (dentro do bloco if, não na div flex gap-2)
- Navegação manual: GET /admin/artes/new sem client_id → form exibe select de clientes
- Navegação manual: GET /admin/artes/new?client_id=1 → form exibe hidden_field (comportamento inalterado)
</verification>

<success_criteria>
1. Admin navega para /admin/artes/new sem client_id: vê selector de clientes, seleciona um, preenche os campos, submete — arte é criada com sucesso (ARTE-09 satisfeito).
2. Admin navega via página do cliente (/admin/clients/:id → "Nova Arte"): comportamento anterior mantido — hidden_field com client_id pré-preenchido (D-04 honrado).
3. Admin submete form sem selecionar mídia: re-exibição mostra "Precisa de arquivo ou link externo" na tela (ARTE-08 depurável, D-02 satisfeito).
</success_criteria>

<output>
Criar `.planning/phases/07-art-upload-client-scoping-fix/07-02-SUMMARY.md` ao concluir.
</output>
