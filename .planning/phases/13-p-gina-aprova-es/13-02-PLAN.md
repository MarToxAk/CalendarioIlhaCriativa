---
phase: 13-p-gina-aprova-es
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - app/controllers/admin/approvals_controller.rb
  - test/controllers/admin/approvals_controller_test.rb
autonomous: true
requirements:
  - APRO-04
  - APRO-06
  - APRO-07

must_haves:
  truths:
    - "GET /admin/approvals retorna 200 com admin autenticado"
    - "Lista de respostas está ordenada por responded_at DESC (mais recente primeiro)"
    - "Paginação com pagy retorna no máximo 25 itens por página"
    - "Filtro por client_id retorna apenas respostas daquele cliente"
    - "Filtro por decision retorna apenas respostas com aquela decisão (per D-01)"
    - "Filtro com decision inválida é ignorado sem lançar erro"
    - "Cada resposta tem link para admin_arte_path(arte) disponível via @approval_responses"
    - "Query usa joins(arte: :client) + includes(arte: :client) — sem N+1"
  artifacts:
    - path: "app/controllers/admin/approvals_controller.rb"
      provides: "Admin::ApprovalsController com action index paginada e filtrável"
      contains: "class Admin::ApprovalsController"
      exports: ["index"]
    - path: "test/controllers/admin/approvals_controller_test.rb"
      provides: "Testes de integração cobrindo APRO-03..07"
      contains: "test_should_get_index"
  key_links:
    - from: "Admin::ApprovalsController#index"
      to: "ApprovalResponse.joins(arte: :client).includes(arte: :client)"
      via: "ActiveRecord query"
      pattern: "joins.*arte.*client"
    - from: "Admin::ApprovalsController#index"
      to: "pagy(scope, limit: 25)"
      via: "Pagy::Backend (herdado do Admin::BaseController)"
      pattern: "pagy\\(scope"
---

<objective>
Criar Admin::ApprovalsController com action index que implementa: query sem N+1 com joins+includes, ordenação por responded_at DESC (per D-02), paginação pagy com 25 itens por página (per D-03), filtro por client_id e filtro por decision com validação de enum (per D-01). Também completar o arquivo de testes com todos os cenários de integração cobrindo APRO-03 a APRO-07.

Purpose: O controller é o coração da Página Aprovações — sem ele a rota criada no Wave 1 retorna RoutingError. Os testes garantem que a query e os filtros se comportam corretamente antes de qualquer view ser criada.
Output: Admin::ApprovalsController funcional com testes verdes cobrindo todos os requisitos de comportamento.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-p-gina-aprova-es/13-CONTEXT.md
@.planning/phases/13-p-gina-aprova-es/13-RESEARCH.md
@.planning/phases/13-p-gina-aprova-es/13-PATTERNS.md
@.planning/phases/13-p-gina-aprova-es/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar Admin::ApprovalsController com index paginado e filtrável</name>
  <files>
    app/controllers/admin/approvals_controller.rb
  </files>
  <read_first>
    - app/controllers/admin/dashboard_controller.rb — padrão de scope com filtros; o ApprovalsController replica essa estrutura com ApprovalResponse em vez de Arte
    - app/controllers/admin/base_controller.rb — confirmar que include Pagy::Backend está presente (adicionado no Wave 1)
    - app/models/approval_response.rb — enum decision: { approved: 0, change_requested: 1 }; belongs_to :arte; responded_at via before_create
    - app/models/arte.rb — belongs_to :client; has_many :approval_responses
    - .planning/phases/13-p-gina-aprova-es/13-RESEARCH.md — Pattern 2 (Pagy no controller), Pattern 3 (query sem N+1), Pattern 4 (validação de enum)
  </read_first>
  <behavior>
    - Test 1: GET /admin/approvals retorna 200 com admin autenticado
    - Test 2: GET /admin/approvals sem autenticação redireciona para /session/new
    - Test 3: @approval_responses está ordenado por responded_at DESC
    - Test 4: @pagy.limit == 25 (paginação de 25 itens per D-03)
    - Test 5: GET /admin/approvals?client_id=X retorna apenas respostas de artes do cliente X
    - Test 6: GET /admin/approvals?decision=approved retorna apenas respostas com decision approved (per D-01)
    - Test 7: GET /admin/approvals?decision=nonexistent retorna 200 sem lançar erro, todos os registros
    - Test 8: @clients contém todos os clientes ordenados por name
  </behavior>
  <action>
    Criar app/controllers/admin/approvals_controller.rb com classe `Admin::ApprovalsController < Admin::BaseController`.

    Na action `index`, construir o scope em etapas:
    1. Scope base: `ApprovalResponse.joins(arte: :client).includes(arte: :client).order(responded_at: :desc)`
    2. Filtro cliente: se `params[:client_id].present?`, adicionar `.where(artes: { client_id: params[:client_id] })` — usar `artes:` para qualificar a tabela e evitar ambiguidade de coluna (Pitfall 2 do RESEARCH.md)
    3. Filtro decision: se `params[:decision].present?` E `ApprovalResponse.decisions.key?(params[:decision])`, adicionar `.where(decision: params[:decision])` — a validação com `.key?` previne ArgumentError para valores inválidos de enum (Pitfall 3 e Pattern 4 do RESEARCH.md)
    4. Paginar: `@pagy, @approval_responses = pagy(scope, limit: 25)`
    5. Carregar clientes para dropdown: `@clients = Client.order(:name)`

    Nenhum outro método ou before_action adicional — autenticação é herdada de Admin::BaseController.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/approvals_controller_test.rb</automated>
  </verify>
  <done>
    - app/controllers/admin/approvals_controller.rb existe e herda de Admin::BaseController
    - Todos os testes do arquivo passam sem erros
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Completar testes de integração do ApprovalsController</name>
  <files>
    test/controllers/admin/approvals_controller_test.rb
  </files>
  <read_first>
    - test/controllers/admin/approvals_controller_test.rb — setup existente do Wave 1; adicionar testes abaixo do comentário placeholder
    - test/controllers/admin/dashboard_controller_test.rb — padrão exato de assertions: assert_response :success, assert_includes response.body, get admin_X_url, params: {}
    - app/controllers/admin/approvals_controller.rb — comportamento implementado na Task 1 para criar asserções precisas
  </read_first>
  <behavior>
    - Test 1: test_should_get_index — GET admin_approvals_url retorna :success
    - Test 2: test_redirects_when_unauthenticated — GET admin_approvals_url sem sign_in retorna redirect (3xx)
    - Test 3: test_displays_approval_data — response.body contém o nome do cliente e o title da arte
    - Test 4: test_filter_by_client_id — GET com client_id=@client.id retorna :success e contém o nome do cliente
    - Test 5: test_filter_by_decision_approved — GET com decision=approved retorna :success e contém o título da arte
    - Test 6: test_filter_by_decision_change_requested — GET com decision=change_requested retorna :success (lista vazia)
    - Test 7: test_filter_by_invalid_decision — GET com decision=nonexistent retorna :success e contém o título da arte (filtro ignorado)
    - Test 8: test_link_to_arte_present — response.body contém o path admin_arte_path(@arte) (cobre APRO-07)
  </behavior>
  <action>
    Adicionar os 8 testes ao arquivo test/controllers/admin/approvals_controller_test.rb, substituindo o comentário placeholder. Seguir o padrão do dashboard_controller_test.rb:
    - Usar `get admin_approvals_url` para todos os testes
    - Para test_redirects_when_unauthenticated: usar uma subclasse ou teardown — OU simplesmente criar novo User.create! em um test separado sem sign_in e chamar get; verificar `assert_redirected_to new_session_url` (ou `assert_response :redirect`)
    - Para test_displays_approval_data: `assert_includes response.body, @client.name` e `assert_includes response.body, @arte.title`
    - Para test_link_to_arte_present: `assert_includes response.body, admin_arte_path(@arte)`
    - Para test_filter_by_decision_change_requested: espera :success com lista vazia (o setup cria apenas um registro :approved)

    Remover o comentário placeholder `# Testes adicionados no plano 13-02` após inserir os testes.

    Nota: test_redirects_when_unauthenticated precisa evitar o sign_in do setup. Usar um bloco separado sem herdar o setup, ou chamar `delete session_path` antes do get para desautenticar, ou criar um test inline que usa uma nova instância limpa de User sem session. A abordagem mais simples que segue o padrão do projeto: no test, chamar `delete session_path` para deslogar, então GET, então assert_response :redirect.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/approvals_controller_test.rb</automated>
  </verify>
  <done>
    - Todos os 8 testes passam (8 tests, N assertions, 0 failures, 0 errors)
    - bin/rails test (suite completa) passa sem regressões
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET params → ActiveRecord WHERE | params[:client_id] e params[:decision] chegam como strings de query string não confiáveis |
| Browser → Admin::ApprovalsController | Request HTTP sem sessão válida deve ser rejeitado antes de qualquer lógica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Spoofing | Admin::ApprovalsController#index | mitigate | before_action :require_authentication herdado de Admin::BaseController — sessão inválida redireciona para /session/new [VERIFIED: base_controller.rb] |
| T-13-03 | Tampering | params[:decision] enum injection | mitigate | Validar com `ApprovalResponse.decisions.key?(params[:decision])` antes de passar ao WHERE — previne ArgumentError do enum Rails [Pattern 4 do RESEARCH.md] |
| T-13-04 | Tampering | params[:client_id] SQL injection | accept | ActiveRecord parametriza automaticamente valores em `.where(artes: { client_id: ... })` — sem risco de injeção SQL |
| T-13-05 | Information Disclosure | Acesso a respostas de outros clientes via client_id manipulado | accept | Admin tem acesso a todos os clientes por design — sem multi-tenancy admin no sistema |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
bin/rails test test/controllers/admin/approvals_controller_test.rb
# Deve mostrar: 8 tests, N assertions, 0 failures, 0 errors

bin/rails test
# Suite completa deve passar sem regressões

bin/rails routes | grep approvals
# admin_approvals  GET  /admin/approvals(.:format)  admin/approvals#index

# Verificar query anti-N+1:
# grep "includes" app/controllers/admin/approvals_controller.rb
# deve mostrar: includes(arte: :client)

# Verificar filtro enum seguro:
# grep "decisions.key?" app/controllers/admin/approvals_controller.rb
# deve mostrar a validação antes do where(decision:)
</verification>

<success_criteria>
- Admin::ApprovalsController existe com action index implementando scope com joins+includes+order+filtros+pagy
- Filtro client_id usa where(artes: { client_id: ... }) — tabela qualificada para evitar ambiguidade
- Filtro decision valida com ApprovalResponse.decisions.key?() antes de aplicar (per D-01)
- pagy(scope, limit: 25) pagina a lista (per D-03)
- Ordenação responded_at: :desc (per D-02)
- 8 testes passam cobrindo APRO-03, APRO-04, APRO-06, APRO-07
- Suite completa bin/rails test sem regressões
</success_criteria>

<output>
Criar `.planning/phases/13-p-gina-aprova-es/13-02-SUMMARY.md` ao concluir
</output>
