---
phase: 13-p-gina-aprova-es
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - app/views/admin/approvals/index.html.erb
  - app/views/admin/approvals/_approval_row.html.erb
  - app/views/admin/approvals/_decision_badge.html.erb
autonomous: true
requirements:
  - APRO-05
  - APRO-06
  - APRO-07

must_haves:
  truths:
    - "GET /admin/approvals renderiza tabela com colunas CLIENTE, ARTE, DECISÃO, DATA DA RESPOSTA, COMENTÁRIO, AÇÕES"
    - "Cada linha exibe o badge correto para decision approved (verde) e change_requested (vermelho)"
    - "Coluna DATA DA RESPOSTA usa formato dd/mm/YYYY com safe navigation para responded_at nil"
    - "Coluna COMENTÁRIO exibe texto truncado em 80 chars ou '—' se nil"
    - "Coluna AÇÕES exibe link 'Ver arte' apontando para admin_arte_path(arte)"
    - "Filtros de cliente e decisão ficam FORA do turbo-frame com data-turbo-frame='approvals-content'"
    - "Conteúdo da tabela fica DENTRO do turbo-frame id='approvals-content'"
    - "pagy_nav(@pagy) está dentro do turbo-frame"
    - "Estado vazio exibe heading 'Nenhuma aprovação encontrada'"
    - "Cards mobile exibem cliente, badge de decisão, título da arte e link para admin_arte_path"
  artifacts:
    - path: "app/views/admin/approvals/index.html.erb"
      provides: "View index com filtros fora do frame e tabela dentro do turbo-frame"
      contains: "turbo-frame id=\"approvals-content\""
    - path: "app/views/admin/approvals/_approval_row.html.erb"
      provides: "Partial de linha de tabela desktop com 6 colunas"
      contains: "decision_badge"
    - path: "app/views/admin/approvals/_decision_badge.html.erb"
      provides: "Badge de decisão (approved=verde, change_requested=vermelho)"
      contains: "Aprovado"
  key_links:
    - from: "app/views/admin/approvals/index.html.erb"
      to: "turbo-frame id='approvals-content'"
      via: "form data: { turbo_frame: 'approvals-content' }"
      pattern: "approvals-content"
    - from: "app/views/admin/approvals/_approval_row.html.erb"
      to: "admin_arte_path(approval_response.arte)"
      via: "link_to Ver arte"
      pattern: "admin_arte_path"
    - from: "app/views/admin/approvals/_approval_row.html.erb"
      to: "app/views/admin/approvals/_decision_badge.html.erb"
      via: "render decision_badge"
      pattern: "render.*decision_badge"
---

<objective>
Criar as views da Página Aprovações: (1) index.html.erb com barra de filtros FORA do turbo-frame e tabela/paginação DENTRO do frame (padrão Phase 6); (2) partial _approval_row.html.erb com as 6 colunas definidas no UI-SPEC; (3) partial _decision_badge.html.erb com as cores do design system. A view deve ser acessível (aria-label na tabela, sr-only na coluna ações, caption sr-only) e responsiva (tabela desktop hidden sm:block, cards mobile block sm:hidden).

Purpose: Com o controller e rota prontos (Wave 1+2), esta wave entrega a interface completa ao usuário — implementando APRO-05 (colunas visíveis), APRO-06 (filtros funcionais via Turbo Frame) e APRO-07 (link Ver arte).
Output: Três arquivos de view que compõem a Página Aprovações completa e visualmente consistente com o design system do projeto.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-p-gina-aprova-es/13-CONTEXT.md
@.planning/phases/13-p-gina-aprova-es/13-RESEARCH.md
@.planning/phases/13-p-gina-aprova-es/13-PATTERNS.md
@.planning/phases/13-p-gina-aprova-es/13-UI-SPEC.md
@.planning/phases/13-p-gina-aprova-es/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar _decision_badge e _approval_row (partials de linha)</name>
  <files>
    app/views/admin/approvals/_decision_badge.html.erb
    app/views/admin/approvals/_approval_row.html.erb
  </files>
  <read_first>
    - app/views/admin/artes/_status_badge.html.erb — padrão exato de badge com case/when; adaptar para decision (apenas 2 casos: approved e change_requested)
    - app/views/admin/artes/_arte_row.html.erb — padrão de `tr` com hover, td com classes text-sm, link "Ver" outline; adaptar para 6 colunas de aprovação
    - .planning/phases/13-p-gina-aprova-es/13-UI-SPEC.md — seção "Badge de decisão" e seção "Tabela desktop" para valores exatos de classes e colunas
    - .planning/phases/13-p-gina-aprova-es/13-PATTERNS.md — seção "_decision_badge" e "_approval_row" para código de referência
  </read_first>
  <action>
    Criar app/views/admin/approvals/_decision_badge.html.erb: usar hash config (não case/when) com 2 entradas:
    - chave "approved": label "Aprovado", classes "bg-[#F0FDF4] text-[#14A958] border-[#14A958]/20"
    - chave "change_requested": label "Pediu Alteração", classes "bg-[#FEF2F2] text-[#EE3537] border-[#EE3537]/20"
    - fallback (else via hash default): classes "bg-gray-50 text-slate-600 border-gray-200", label approval_response.decision.to_s
    Estrutura do span: `inline-flex items-center gap-1 px-2 py-1 rounded-full text-xs font-semibold border` com as classes do config. Incluir `<span aria-hidden="true">●</span>` antes do label. A variável local recebida é `approval_response`.

    Criar app/views/admin/approvals/_approval_row.html.erb com `<tr class="hover:bg-gray-50 transition-colors border-b border-gray-100 last:border-0">` e 6 `<td>`:
    1. Cliente: `approval_response.arte.client.name`, classes `py-3 px-4 text-sm text-slate-900`
    2. Arte: `approval_response.arte.title.presence || "Sem título"`, classes `py-3 px-4 text-sm text-slate-900`
    3. Decisão: `render "decision_badge", approval_response: approval_response`, classes `py-3 px-4`
    4. Data: `approval_response.responded_at&.strftime("%d/%m/%Y") || "—"`, classes `py-3 px-4 text-sm text-slate-600`
    5. Comentário: `truncate(approval_response.comment.to_s, length: 80).presence || "—"`, classes `py-3 px-4 text-sm text-slate-500` — quando nil/vazio, exibir "—" em italic: `class="py-3 px-4 text-sm text-slate-400 italic"`; usar condicional ERB para alternar a classe
    6. Ações: `link_to "Ver arte", admin_arte_path(approval_response.arte), class: "h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600 hover:bg-gray-50 transition-colors"`, td com classes `py-3 px-4`

    A variável local recebida pelo partial é `approval_response`.
  </action>
  <verify>
    <automated>bin/rails runner "ar = ApprovalResponse.joins(arte: :client).includes(arte: :client).first; puts ar.arte.client.name if ar"</automated>
  </verify>
  <done>
    - app/views/admin/approvals/_decision_badge.html.erb existe com hash config de 2 entradas (approved, change_requested) e fallback
    - app/views/admin/approvals/_approval_row.html.erb existe com 6 colunas exatas do UI-SPEC
    - Coluna DATA usa safe navigation `responded_at&.strftime`
    - Coluna COMENTÁRIO usa `truncate(..., length: 80)` com fallback "—"
    - Coluna AÇÕES usa `link_to "Ver arte", admin_arte_path(approval_response.arte)`
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar view index.html.erb (filtros + turbo-frame + tabela + mobile + paginação)</name>
  <files>
    app/views/admin/approvals/index.html.erb
  </files>
  <read_first>
    - app/views/admin/dashboard/index.html.erb — padrão exato de form fora do turbo-frame + `data: { turbo_frame: ... }` + turbo-frame envolvendo tabela; replicar estrutura
    - app/views/admin/artes/index.html.erb — padrão de tabela desktop (hidden sm:block), cards mobile (block sm:hidden), estado vazio com SVG e heading
    - .planning/phases/13-p-gina-aprova-es/13-UI-SPEC.md — seção "Layout e Estrutura da Página", "Barra de filtros", "Tabela desktop", "Lista de cards mobile", "Copywriting Contract"
    - .planning/phases/13-p-gina-aprova-es/13-PATTERNS.md — seção "index.html.erb" com código de referência para cada bloco da view
    - .planning/phases/13-p-gina-aprova-es/13-RESEARCH.md — Pitfall 3 (ids de turbo-frame devem ser idênticos) e Open Question (pagy_nav precisa de wrapper div)
  </read_first>
  <action>
    Criar app/views/admin/approvals/index.html.erb com a seguinte estrutura:

    1. `<% content_for(:page_title) { "Aprovações" } %>`
    2. `<h1 class="text-2xl font-semibold text-slate-900 mb-6">Aprovações</h1>`
    3. Barra de filtros FORA do turbo-frame: `form_with url: admin_approvals_path, method: :get, data: { turbo_frame: "approvals-content" }, class: "flex items-center gap-3 mb-6"` com:
       - select :client_id com opções `[["Todos os clientes", ""]] + @clients.map { |c| [c.name, c.id] }`, `selected: params[:client_id]`, classes `h-9 px-3 border border-gray-200 rounded-lg text-sm text-slate-700 bg-white`
       - select :decision com opções `[["Todas as decisões", ""], ["Aprovado", "approved"], ["Pediu Alteração", "change_requested"]]`, `selected: params[:decision]`, mesmas classes
       - submit "Filtrar" com classes `h-9 px-4 bg-[#0F7949] hover:bg-[#0a5c37] text-white text-sm font-semibold rounded-lg transition-colors cursor-pointer`
    4. `<turbo-frame id="approvals-content">` — CRÍTICO: id deve ser exatamente "approvals-content" para corresponder ao data-turbo-frame do form (Pitfall 3 do RESEARCH.md)
    5. Dentro do turbo-frame, condicional `@approval_responses.empty?`:
       - Estado vazio: div `py-16 text-center` com SVG heroicon inbox (outline, width="48" height="48", class="mx-auto text-slate-300 mb-4"), heading `text-lg font-semibold text-slate-900 mb-1` com copy "Nenhuma aprovação encontrada", parágrafo `text-sm text-slate-500` com copy condicional: se params[:client_id].present? || params[:decision].present? usar "Nenhuma aprovação corresponde aos filtros selecionados." senão usar "Ainda não há respostas de aprovação registradas."
       - Estado com dados: tabela desktop + cards mobile + pagy_nav
    6. Tabela desktop: `div class="bg-white rounded-xl border border-gray-200 shadow-card overflow-hidden hidden sm:block"`, `table aria-label="Lista de aprovações" class="w-full"`, `caption class="sr-only"` com copy "Respostas de aprovação — clique em Ver arte para ver os detalhes", thead com `bg-gray-50 border-b border-gray-200`, 6 th com classes `py-3 px-4 text-xs font-semibold text-slate-500 uppercase tracking-wide text-left`: CLIENTE, ARTE, DECISÃO, DATA DA RESPOSTA, COMENTÁRIO, e `<th scope="col" class="py-3 px-4 w-[48px]"><span class="sr-only">Ações</span></th>`. tbody com `@approval_responses.each { |ar| render "approval_row", approval_response: ar }`.
    7. Cards mobile: `div class="block sm:hidden space-y-3 mt-4"` com `@approval_responses.each` — cada card é `link_to admin_arte_path(ar.arte), class: "block bg-white rounded-xl border border-gray-200 shadow-card px-4 py-3"` contendo: linha 1 com nome cliente (semibold) e badge (`render "decision_badge", approval_response: ar`), linha 2 com título da arte e data em `text-xs text-slate-500`, linha 3 condicional com comentário truncado em 60 chars se presente (`text-xs text-slate-400 italic`), chevron SVG direito.
    8. Paginação dentro do turbo-frame (após a tabela e os cards): `<div class="flex justify-center mt-6"><%== pagy_nav(@pagy) %></div>` — usar `<%==` (raw output) pois pagy_nav retorna HTML montado.
    9. Fechar `</turbo-frame>`

    AVISO do RESEARCH.md Open Question: pagy_nav gera `<nav class="pagy nav">` sem estilo Tailwind. O wrapper `div class="flex justify-center mt-6"` já posiciona o nav. Adicionar no wrapper: `style="gap: 0.25rem;"` ou garantir que os links internos herdem a cor padrão de link. Se os links aparecerem sem estilo aceitável, adicionar na view um bloco `<style>` inline com `.pagy.nav a { @apply px-2 py-1 text-sm rounded border border-gray-200 text-slate-600 hover:bg-gray-50; } .pagy.nav a[aria-current] { @apply bg-[#0F7949] text-white border-[#0F7949]; }` — ou verificar se a paginação é visível e funcional primeiro antes de estilizar.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/approvals_controller_test.rb</automated>
  </verify>
  <done>
    - GET /admin/approvals retorna 200 e renderiza sem erros
    - View contém turbo-frame com id="approvals-content"
    - Form de filtro tem data-turbo-frame="approvals-content"
    - Tabela tem aria-label="Lista de aprovações" e caption sr-only
    - Coluna AÇÕES tem span sr-only "Ações"
    - pagy_nav está dentro do turbo-frame
    - Estado vazio exibe "Nenhuma aprovação encontrada"
    - bin/rails test (suite completa) passa sem regressões
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view ERB → HTML output | Dados de approval_response.arte.client.name passam pelo ERB antes de ser enviados ao browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Information Disclosure | ERB output de dados de clientes | accept | Admin autenticado tem acesso a todos os dados por design; dados são somente-leitura nesta view |
| T-13-07 | Tampering | XSS via approval_response.comment | mitigate | Rails ERB usa `<%= %>` (html_escape por padrão) — campos de texto são automaticamente escapados. `truncate()` também escapa HTML. Não usar `<%==` em campos de dado do usuário |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
bin/rails test
# Suite completa deve passar sem regressões

# Verificação smoke manual (após rails server):
# 1. Acessar http://localhost:3000/admin/approvals — deve renderizar a página com filtros e tabela
# 2. Selecionar um cliente no dropdown e clicar Filtrar — turbo-frame deve atualizar sem recarregar sidebar
# 3. Selecionar "Aprovado" no dropdown de decisão — lista deve filtrar corretamente
# 4. Clicar "Ver arte" em qualquer linha — deve navegar para admin_arte_path da arte correspondente
# 5. Link "Aprovações" no sidebar deve estar destacado (current_page? retorna true)

# Verificações de arquivo:
grep -c 'turbo-frame id="approvals-content"' app/views/admin/approvals/index.html.erb
# deve retornar 1

grep 'admin_arte_path' app/views/admin/approvals/_approval_row.html.erb
# deve mostrar o link "Ver arte"

grep 'Aprovado' app/views/admin/approvals/_decision_badge.html.erb
# deve mostrar a entrada do hash config para approved
</verification>

<success_criteria>
- GET /admin/approvals renderiza com status 200 sem erros
- Tabela exibe as 6 colunas: CLIENTE, ARTE, DECISÃO, DATA DA RESPOSTA, COMENTÁRIO, AÇÕES (APRO-05)
- Badge _decision_badge exibe "Aprovado" em verde e "Pediu Alteração" em vermelho
- Filtros via turbo-frame atualizam apenas o conteúdo da tabela sem recarregar a página (APRO-06)
- Link "Ver arte" em cada linha aponta para admin_arte_path(arte) (APRO-07)
- Estado vazio exibe mensagem "Nenhuma aprovação encontrada"
- Cards mobile presentes para viewport pequeno
- pagy_nav renderizado dentro do turbo-frame
- Suite completa bin/rails test sem regressões
- Todos os 5 requisitos APRO-03..07 cumpridos ao final desta wave
</success_criteria>

<output>
Criar `.planning/phases/13-p-gina-aprova-es/13-03-SUMMARY.md` ao concluir
</output>
