---
phase: 13-p-gina-aprova-es
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/admin/base_controller.rb
  - app/helpers/application_helper.rb
  - config/routes.rb
  - app/views/admin/shared/_sidebar.html.erb
  - test/controllers/admin/approvals_controller_test.rb
autonomous: true
requirements:
  - APRO-03

must_haves:
  truths:
    - "GET /admin/approvals retorna 200 (rota existe)"
    - "Link 'Aprovações' no sidebar aponta para admin_approvals_path, não mais '#'"
    - "Arquivo de testes do ApprovalsController existe e tem setup com dados reais"
  artifacts:
    - path: "config/routes.rb"
      provides: "Rota resources :approvals, only: [:index] no namespace admin"
      contains: "resources :approvals"
    - path: "app/controllers/admin/base_controller.rb"
      provides: "include Pagy::Backend disponível para todos os controllers admin"
      contains: "include Pagy::Backend"
    - path: "app/helpers/application_helper.rb"
      provides: "include Pagy::Frontend disponível em todas as views"
      contains: "include Pagy::Frontend"
    - path: "app/views/admin/shared/_sidebar.html.erb"
      provides: "Link Aprovações wired para admin_approvals_path"
      contains: "admin_approvals_path"
    - path: "test/controllers/admin/approvals_controller_test.rb"
      provides: "Arquivo de testes com setup inline para ApprovalResponse"
      contains: "Admin::ApprovalsControllerTest"
  key_links:
    - from: "app/views/admin/shared/_sidebar.html.erb"
      to: "admin_approvals_path"
      via: "nav_items hash"
      pattern: "admin_approvals_path"
    - from: "app/controllers/admin/base_controller.rb"
      to: "Pagy::Backend"
      via: "include"
      pattern: "include Pagy::Backend"
---

<objective>
Instalar a infraestrutura necessária para a Página Aprovações funcionar: (1) habilitar Pagy no projeto incluindo Pagy::Backend no Admin::BaseController e Pagy::Frontend no ApplicationHelper — ambos ausentes hoje; (2) registrar a rota resources :approvals no namespace admin; (3) wire o link "Aprovações" no sidebar de "#" para admin_approvals_path (per APRO-03); (4) criar o arquivo de testes com setup inline pronto para os testes do Wave 2.

Purpose: Estas três modificações são pré-requisitos bloqueantes — sem elas o controller lança NoMethodError para `pagy` e a view para `pagy_nav`, e o link do sidebar não navega.
Output: Base controller com Pagy::Backend, ApplicationHelper com Pagy::Frontend, rota registrada, sidebar wired, arquivo de testes pronto.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-p-gina-aprova-es/13-CONTEXT.md
@.planning/phases/13-p-gina-aprova-es/13-RESEARCH.md
@.planning/phases/13-p-gina-aprova-es/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Habilitar Pagy no projeto (Backend + Frontend) e registrar rota</name>
  <files>
    app/controllers/admin/base_controller.rb
    app/helpers/application_helper.rb
    config/routes.rb
  </files>
  <read_first>
    - app/controllers/admin/base_controller.rb — conteúdo atual (4 linhas); inserir include Pagy::Backend antes do end
    - app/helpers/application_helper.rb — conteúdo atual (2 linhas); inserir include Pagy::Frontend dentro do module
    - config/routes.rb — namespace admin (linhas 7-19); inserir resources :approvals, only: [:index] após resources :artes block, antes do end do namespace
  </read_first>
  <behavior>
    - Test 1: Admin::ApprovalsController instanciado responde ao método pagy (não lança NoMethodError)
    - Test 2: pagy_nav(@pagy) está disponível em views admin sem chamar include explicitamente na view
    - Test 3: GET /admin/approvals sem controller criado retorna 404 (rota existe, controller ainda não)
  </behavior>
  <action>
    Em app/controllers/admin/base_controller.rb: adicionar `include Pagy::Backend` como última linha antes do `end` final da classe. O include deve ficar após `before_action :require_authentication`.

    Em app/helpers/application_helper.rb: adicionar `include Pagy::Frontend` como primeira linha dentro do `module ApplicationHelper`, antes do `end`.

    Em config/routes.rb: no bloco `namespace :admin do`, após o bloco `resources :artes do ... end`, inserir `resources :approvals, only: [ :index ]` antes do `end` do namespace. Manter o estilo de espaçamento existente (2 espaços, colchetes com espaços).

    Verificar após edição: `bin/rails routes | grep approvals` deve listar `admin_approvals GET /admin/approvals(.:format)`.
  </action>
  <verify>
    <automated>bin/rails routes | grep approvals</automated>
  </verify>
  <done>
    - bin/rails routes | grep approvals mostra "admin_approvals GET /admin/approvals(.:format) admin/approvals#index"
    - app/controllers/admin/base_controller.rb contém "include Pagy::Backend"
    - app/helpers/application_helper.rb contém "include Pagy::Frontend"
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire link Aprovações no sidebar e criar arquivo de testes</name>
  <files>
    app/views/admin/shared/_sidebar.html.erb
    test/controllers/admin/approvals_controller_test.rb
  </files>
  <read_first>
    - app/views/admin/shared/_sidebar.html.erb — linha 13 contém `{ label: "Aprovações", path: "#" }`; alterar apenas o valor de path
    - test/controllers/admin/dashboard_controller_test.rb — padrão de setup inline com User.create!, Client.create!, Arte.create! e sign_in_as; replicar exatamente para ApprovalResponse
    - app/models/approval_response.rb — campos obrigatórios: arte (belongs_to), decision (enum: approved/change_requested); responded_at preenchido por before_create
  </read_first>
  <action>
    Em app/views/admin/shared/_sidebar.html.erb: alterar apenas a linha `{ label: "Aprovações", path: "#" }` para `{ label: "Aprovações", path: admin_approvals_path }`. Nenhuma outra linha deve ser alterada no arquivo.

    Criar test/controllers/admin/approvals_controller_test.rb com classe `Admin::ApprovalsControllerTest < ActionDispatch::IntegrationTest`. O `setup` deve replicar o padrão do dashboard_controller_test.rb: User.create! com email_address e password, sign_in_as(@user), Client.create! com name/password/password_confirmation, Arte.create! com os campos obrigatórios (client, scheduled_on: Date.current, platform: :instagram, media_type: :image, status: :pending, title: "Arte Teste", caption: "Legenda", approval_deadline: Date.current + 5, external_url: "https://drive.google.com/file/exemplo"), e também `@approval_response = ApprovalResponse.create!(arte: @arte, decision: :approved)`. Não passar responded_at — é preenchido pelo before_create.

    O arquivo de testes deve incluir apenas o setup neste momento — os testes em si serão adicionados no Plano 02 (Wave 2). Incluir um comentário `# Testes adicionados no plano 13-02` no corpo da classe para deixar claro o estado intencional.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/approvals_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <done>
    - app/views/admin/shared/_sidebar.html.erb contém "admin_approvals_path" (não mais "#" para Aprovações)
    - test/controllers/admin/approvals_controller_test.rb existe e o suite roda sem erro (0 tests, 0 assertions por enquanto)
    - bin/rails test roda sem falhas
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/routes.rb → admin namespace | Qualquer rota adicionada ao namespace admin herda o before_action :require_authentication do Admin::BaseController |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | GET /admin/approvals (nova rota) | mitigate | Herdado de Admin::BaseController: `before_action :require_authentication` — acesso sem sessão redireciona para /session/new [VERIFIED: base_controller.rb] |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote novo; Pagy já está em vendor/bundle — sem risco de supply chain neste plano |
</threat_model>

<verification>
bin/rails routes | grep approvals
# Deve mostrar: admin_approvals  GET  /admin/approvals(.:format)  admin/approvals#index

bin/rails test test/controllers/admin/approvals_controller_test.rb
# Deve rodar sem falhas (0 tests é OK neste plano)

grep "admin_approvals_path" app/views/admin/shared/_sidebar.html.erb
# Deve retornar a linha com o link wired

grep "Pagy::Backend" app/controllers/admin/base_controller.rb
grep "Pagy::Frontend" app/helpers/application_helper.rb
# Ambos devem retornar a linha com o include
</verification>

<success_criteria>
- Rota admin_approvals registrada e visível em `bin/rails routes`
- Pagy::Backend incluído em Admin::BaseController (desbloqueando método `pagy` para todos os controllers admin)
- Pagy::Frontend incluído em ApplicationHelper (desbloqueando `pagy_nav` em todas as views)
- Link "Aprovações" no sidebar aponta para admin_approvals_path (APRO-03 cumprido)
- Arquivo de testes criado com setup inline correto e suite verde
</success_criteria>

<output>
Criar `.planning/phases/13-p-gina-aprova-es/13-01-SUMMARY.md` ao concluir
</output>
