---
phase: 09-calendar-summary-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/client/home_controller.rb
  - app/views/client/home/index.html.erb
autonomous: true
requirements:
  - CAL2-01

must_haves:
  truths:
    - "Cliente vê faixa de resumo acima da grade do calendário quando há artes no mês"
    - "Faixa exibe 4 chips na ordem: total, aprovadas, pendentes, pediu alteração"
    - "Contagens refletem apenas artes com scheduled_on dentro do mês corrente (não dias de overflow do grid)"
    - "Status revised é contado junto com pending no chip pendentes (D-04)"
    - "Faixa não é renderizada quando não há artes no mês corrente (D-06)"
    - "Após cliente aprovar arte, reload da página mostra contagens atualizadas automaticamente (SSR)"
  artifacts:
    - path: "app/controllers/client/home_controller.rb"
      provides: "Hash @summary com chaves :total, :approved, :pending, :change_requested"
      contains: "@summary ="
    - path: "app/views/client/home/index.html.erb"
      provides: "Summary strip inline com 4 chips coloridos e guard de visibilidade"
      contains: "role=\"status\""
  key_links:
    - from: "app/controllers/client/home_controller.rb"
      to: "app/views/client/home/index.html.erb"
      via: "@summary instance variable"
      pattern: "@summary\\[:total\\]"
    - from: "app/views/client/home/index.html.erb"
      to: "Tailwind color classes"
      via: "chip spans bg-green-100, bg-amber-100, bg-red-100, bg-slate-100"
      pattern: "bg-green-100|bg-amber-100|bg-red-100|bg-slate-100"
---

<objective>
Adicionar a faixa de resumo (summary strip) de status de artes acima do calendário no portal do cliente, implementando CAL2-01.

Purpose: O cliente precisa visualizar rapidamente o total de artes do mês e a distribuição por status (aprovadas, pendentes, pediu alteração) sem precisar vasculhar o calendário.

Output: Controller calcula @summary em memória a partir de @artes já carregado; view renderiza 4 chips coloridos inline entre o header de navegação de mês e a grade do calendário, visíveis apenas quando há artes no mês corrente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-calendar-summary-strip/09-CONTEXT.md
@.planning/phases/09-calendar-summary-strip/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar @summary ao Client::HomeController#index (D-01, D-02, D-03, D-04)</name>
  <files>app/controllers/client/home_controller.rb</files>

  <read_first>
    - app/controllers/client/home_controller.rb — ler inteiro; ponto de inserção é após a linha `@artes_by_date = @artes.group_by(&:scheduled_on)` (linha 17 atual). Não alterar nada acima ou abaixo.
    - app/models/arte.rb — confirmar que `status` é string ou enum e que os valores `pending`, `revised`, `approved`, `change_requested` existem.
  </read_first>

  <behavior>
    - @summary[:total] retorna apenas artes cujo scheduled_on está dentro do mês corrente (não inclui dias de overflow do grid da semana anterior/posterior)
    - @summary[:approved] retorna contagem de artes com status == 'approved' dentro do mês
    - @summary[:pending] retorna contagem de artes com status 'pending' OU 'revised' dentro do mês (D-04)
    - @summary[:change_requested] retorna contagem de artes com status == 'change_requested' dentro do mês
    - Se não houver artes no mês, @summary[:total] == 0 e todos os outros campos == 0
    - Não executa query adicional ao banco; usa .select em memória sobre @artes já carregado
  </behavior>

  <action>
    Inserir após a linha `@artes_by_date = @artes.group_by(&:scheduled_on)` e antes de `@grid_dates = (grid_start..grid_end).to_a` o seguinte bloco de código (per D-01, D-02, D-03, D-04):

    Definir variável local `artes_do_mes` filtrando @artes com .select comparando `scheduled_on.month == @current_month.month && scheduled_on.year == @current_month.year`.

    Definir `@summary` como hash com as chaves:
    - :total — artes_do_mes.count
    - :approved — artes_do_mes.count filtrando status.to_s == 'approved'
    - :pending — artes_do_mes.count filtrando %w[pending revised].include?(status.to_s) (per D-04)
    - :change_requested — artes_do_mes.count filtrando status.to_s == 'change_requested'

    Nomes dos identificadores Ruby: variável local `artes_do_mes`, instância `@summary`. Bloco com colchetes (hash literal), não com Hash.new. Não usar `.to_sym` ou `.freeze` extras.
  </action>

  <verify>
    <automated>bundle exec rails test test/controllers/client/home_controller_test.rb</automated>
  </verify>

  <acceptance_criteria>
    - app/controllers/client/home_controller.rb contém a string `artes_do_mes =`
    - app/controllers/client/home_controller.rb contém a string `@summary =`
    - app/controllers/client/home_controller.rb contém a string `%w[pending revised].include?`
    - app/controllers/client/home_controller.rb contém a string `:change_requested`
    - A linha com `@summary =` aparece após a linha com `@artes_by_date` e antes da linha com `@grid_dates`
    - Nenhuma query ActiveRecord adicional foi adicionada (sem `.where`, `.find`, `.joins` novos no método index além dos já existentes)
    - `rails test test/controllers/client/home_controller_test.rb` passa sem erros
  </acceptance_criteria>

  <done>Controller calcula @summary com as 4 chaves corretas filtrando apenas artes do mês corrente em memória, sem query adicional ao banco.</done>
</task>

<task type="auto">
  <name>Task 2: Inserir summary strip inline em index.html.erb (D-05, D-06, D-07, D-08, D-09, D-10)</name>
  <files>app/views/client/home/index.html.erb</files>

  <read_first>
    - app/views/client/home/index.html.erb — ler inteiro (32 linhas); ponto de inserção é após a linha 23 (`</div>` que fecha o header de navegação de mês) e antes da linha 25 (`&lt;%= render partial: "client/home/month_calendar" %&gt;`). A linha 24 é uma linha em branco; inserir o bloco entre as linhas 23 e 25.
    - app/views/client/shared/_arte_status_badge.html.erb — confirmar classes Tailwind por status: approved=bg-green-100/text-green-800, pending=bg-amber-100/text-amber-800, change_requested=bg-red-100/text-red-800, total=bg-slate-100/text-slate-700.
    - .planning/phases/09-calendar-summary-strip/09-UI-SPEC.md — confirmar estrutura do chip e atributos ARIA obrigatórios.
  </read_first>

  <action>
    Inserir bloco inline após a linha 23 (`</div>`) e antes da linha 25 (`&lt;%= render partial: "client/home/month_calendar" %&gt;`) em app/views/client/home/index.html.erb (per D-05, D-06, D-07, D-08, D-09, D-10).

    Estrutura do bloco (usar ERB, sem partial separado — per D-10):

    Abrir guard ERB: `<% if @summary[:total] > 0 %>` (per D-06).

    Container div com atributos: role="status", aria-label="Resumo do mês", class="flex flex-wrap gap-2 mb-4 justify-center" (per UI-SPEC, D-08).

    Dentro do container, 4 chips span na ordem total → aprovadas → pendentes → pediu alteração (per D-03):

    Chip total: classe base "px-3 py-1 rounded-full text-xs font-medium bg-slate-100 text-slate-700". Conteúdo: `<span class="font-semibold"><%= @summary[:total] %></span> total`

    Chip aprovadas: classes "px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800". Conteúdo: `<span class="font-semibold"><%= @summary[:approved] %></span> aprovadas`

    Chip pendentes: classes "px-3 py-1 rounded-full text-xs font-medium bg-amber-100 text-amber-800". Conteúdo: `<span class="font-semibold"><%= @summary[:pending] %></span> pendentes`

    Chip pediu alteração: classes "px-3 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800". Conteúdo: `<span class="font-semibold"><%= @summary[:change_requested] %></span> pediu alteração`

    Fechar container div. Fechar guard ERB: `<% end %>`.

    Sem Stimulus data-controller, sem Turbo Stream, sem JavaScript inline (per D-09). Sem `<%= render partial: ... %>` para a faixa (per D-10).
  </action>

  <verify>
    <automated>bundle exec rails test test/controllers/client/home_controller_test.rb &amp;&amp; bundle exec rails test test/integration/ 2>/dev/null || true</automated>
    <human-check>Abrir o portal do cliente em /client?token=TOKEN&amp;month=YYYY-MM com artes cadastradas no mês e confirmar: (1) faixa aparece acima do calendário com 4 chips coloridos; (2) contagens batem com o número de artes por status; (3) navegar para um mês sem artes e confirmar que a faixa não aparece.</human-check>
  </verify>

  <acceptance_criteria>
    - app/views/client/home/index.html.erb contém a string `role="status"`
    - app/views/client/home/index.html.erb contém a string `aria-label="Resumo do mês"`
    - app/views/client/home/index.html.erb contém a string `@summary[:total] > 0`
    - app/views/client/home/index.html.erb contém a string `flex flex-wrap gap-2`
    - app/views/client/home/index.html.erb contém a string `bg-green-100 text-green-800`
    - app/views/client/home/index.html.erb contém a string `bg-amber-100 text-amber-800`
    - app/views/client/home/index.html.erb contém a string `bg-red-100 text-red-800`
    - app/views/client/home/index.html.erb contém a string `bg-slate-100 text-slate-700`
    - app/views/client/home/index.html.erb contém a string `pediu alteração` (sem abreviação — per D-08)
    - app/views/client/home/index.html.erb NÃO contém `render partial:` para a faixa (a faixa é inline — per D-10)
    - app/views/client/home/index.html.erb NÃO contém `data-controller` na faixa (sem Stimulus — per D-09)
    - O bloco da faixa aparece após a linha com `</div>` do header de navegação e antes da linha com `render partial: "client/home/month_calendar"`
    - `bundle exec rails test` passa sem erros de sintaxe ERB
  </acceptance_criteria>

  <done>Summary strip renderiza inline em index.html.erb com 4 chips na ordem e cores corretas, guardado por @summary[:total] > 0, sem partial nem JS adicionais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→controller | Token de acesso no parâmetro `token` — valido pelo ClientController antes do index; @summary não expõe dados de outros clientes |
| controller→view | @summary é hash calculado a partir de @artes já escopado por @client; nenhum dado não-autorizado pode vazar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | @summary no controller | accept | @artes já escopado por `@client.artes` no ClientController; @summary deriva apenas desse escopo — sem risco de cross-client leak |
| T-09-02 | Tampering | params[:month] no parse_month_param | accept | parse_month_param já existente usa Date.strptime com rescue Date::Error; nenhuma mudança nesta fase |
| T-09-03 | Spoofing | access_token na URL do cliente | accept | Mecanismo existente no ClientController; fora do escopo desta fase |
</threat_model>

<verification>
Executar após ambas as tasks concluídas:

1. `bundle exec rails test` — suite completa passa sem erros
2. Grep de sanidade no controller: `grep -n "artes_do_mes\|@summary" app/controllers/client/home_controller.rb` — deve retornar 5+ linhas com as atribuições corretas
3. Grep de sanidade na view: `grep -n "role=\"status\"\|@summary\|pediu alteração" app/views/client/home/index.html.erb` — deve retornar o bloco completo da faixa
4. Verificação visual: acessar portal do cliente com mês com artes — confirmar faixa visível com contagens; acessar mês sem artes — confirmar faixa ausente
</verification>

<success_criteria>
- Controller calcula @summary com 4 chaves (:total, :approved, :pending, :change_requested) usando select em memória sobre @artes, filtrando por mês e ano correntes
- revised contado em :pending (D-04)
- View renderiza 4 chips coloridos inline entre o header de mês e a grade do calendário
- Faixa oculta quando @summary[:total] == 0 (D-06)
- Chips com classes Tailwind idênticas ao _arte_status_badge.html.erb (D-07)
- Labels não abreviados: "pediu alteração" completo em mobile (D-08)
- Nenhum partial, Turbo Stream ou Stimulus adicionado (D-09, D-10)
- bundle exec rails test passa sem falhas
</success_criteria>

<output>
Criar `.planning/phases/09-calendar-summary-strip/09-01-SUMMARY.md` ao concluir.
</output>
