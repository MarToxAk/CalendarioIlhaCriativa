---
phase: 02-admin-auth-client-management
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/views/admin/clients/new.html.erb
  - app/views/admin/clients/edit.html.erb
  - app/views/admin/clients/_form.html.erb
  - test/controllers/admin/clients_controller_test.rb
autonomous: true
requirements:
  - CLIE-01
  - CLIE-02

must_haves:
  truths:
    - "Admin acessa /admin/clients/new, preenche nome e senha, clica 'Cadastrar cliente' e é redirecionado para /admin/clients/:id com flash de sucesso"
    - "Admin acessa /admin/clients/:id/edit, edita nome com senha em branco, salva, e a senha anterior é mantida"
    - "Erros de validação (nome vazio, senha curta) são exibidos inline abaixo do campo com mensagens PT-BR"
    - "Campo senha usa password_toggle_controller existente para mostrar/ocultar"
    - "password_plain é salvo no create com o mesmo valor que password"
  artifacts:
    - path: "app/views/admin/clients/new.html.erb"
      provides: "Formulário de criação de cliente com card max-w-lg"
    - path: "app/views/admin/clients/edit.html.erb"
      provides: "Formulário de edição com campos pré-preenchidos e hint de senha vazia"
    - path: "app/views/admin/clients/_form.html.erb"
      provides: "Partial compartilhada do formulário (nome + senha + botões)"
    - path: "test/controllers/admin/clients_controller_test.rb"
      provides: "Testes de create e update cobrindo happy path e validações"
  key_links:
    - from: "app/views/admin/clients/new.html.erb"
      to: "app/views/admin/clients/_form.html.erb"
      via: "render partial"
      pattern: "render.*form"
    - from: "app/controllers/admin/clients_controller.rb"
      to: "app/models/client.rb"
      via: "Client.new + client.save"
      pattern: "Client\\.new|@client\\.save"
---

<objective>
Entrega o slice de criação e edição de clientes: formulários new e edit com design UI-SPEC, validação inline, toggle de senha reutilizando o password_toggle_controller existente da Phase 1. Inclui testes de controller cobrindo os fluxos de create e update.

Após este plano, o admin consegue criar um novo cliente (CLIE-01) e editar seus dados (CLIE-02) com a experiência visual completa especificada.

Purpose: Capacidade de criar e editar clientes — a primeira ação de valor do sistema.
Output: new.html.erb, edit.html.erb, _form.html.erb partial, testes de controller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-admin-auth-client-management/02-CONTEXT.md
@.planning/phases/02-admin-auth-client-management/02-PATTERNS.md
@.planning/phases/02-admin-auth-client-management/02-UI-SPEC.md
@.planning/phases/02-admin-auth-client-management/02-01-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Testes de controller para create e update de clientes</name>
  <files>
    test/controllers/admin/clients_controller_test.rb
  </files>
  <read_first>
    - test/controllers/sessions_controller_test.rb (padrão de setup com find_or_create_by!, login helper e assertions)
    - app/controllers/admin/clients_controller.rb (ações create e update implementadas em 02-01)
    - app/models/client.rb (validações presentes)
    - .planning/phases/02-admin-auth-client-management/02-CONTEXT.md (D-10: blank password guard no update)
  </read_first>
  <behavior>
    Testes RED antes de qualquer ajuste:
    - test "create com dados válidos redireciona para show com notice": POST admin_clients_path com name: "Loja da Maria", password: "abcd1234", password_plain: "abcd1234" → assert_redirected_to admin_client_path(Client.last); assert_equal "Cliente cadastrado com sucesso.", flash[:notice]; assert Client.last.password_plain == "abcd1234"
    - test "create com nome vazio renderiza new com status 422": POST com name: "", password: "abcd1234" → assert_response :unprocessable_entity; assert_select "span[role='alert']"
    - test "update com senha em branco mantém senha original (D-10)": PATCH com name: "Nome Novo", password: "", password_plain: "" → assert_equal senha_original, @client.reload.password_plain; assert_redirected_to admin_client_path(@client)
    - test "update com dados inválidos renderiza edit com status 422": PATCH com name: "" → assert_response :unprocessable_entity
    - Setup: criar user admin com find_or_create_by! e fazer login via POST session_path (mesmo padrão do sessions_controller_test.rb). Criar @client de test com Client.create!.
  </behavior>
  <action>
    Criar test/controllers/admin/clients_controller_test.rb com classe AdminClientsControllerTest &lt; ActionDispatch::IntegrationTest. Setup: User.find_or_create_by!(email_address: "admin@ilhacriativa.com.br") { |u| u.password = ENV.fetch("ADMIN_PASSWORD", "SenhaSegura123!") }; depois POST session_path com credenciais para autenticar. Criar @client = Client.create!(name: "Test Client", password: "senha1234", password_plain: "senha1234") no setup.

    Implementar os 4 testes descritos acima usando assert_redirected_to, assert_response, assert_equal e assert_select conforme o padrão Rails minitest.

    Rodar `bin/rails test test/controllers/admin/clients_controller_test.rb` — deve falhar (RED) se os testes cobrem comportamentos ainda não testados, ou passar se o controller já está correto de 02-01.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/clients_controller_test.rb 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bin/rails test test/controllers/admin/clients_controller_test.rb` termina com "0 failures, 0 errors"
    - Arquivo contém pelo menos 4 test methods (grep -c "def test_" retorna >= 4)
    - Contém assert para flash[:notice] "Cliente cadastrado com sucesso."
    - Contém assert para D-10 (blank password kept): grep "password_plain" no arquivo retorna match com assert
  </acceptance_criteria>
  <done>4+ testes de controller passando cobrindo create valid, create invalid, update blank password, update invalid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Views new, edit e _form partial com design UI-SPEC</name>
  <files>
    app/views/admin/clients/new.html.erb
    app/views/admin/clients/edit.html.erb
    app/views/admin/clients/_form.html.erb
  </files>
  <read_first>
    - app/views/sessions/new.html.erb (form_with pattern, input CSS classes, password toggle data-* attributes, flash/error markup)
    - app/javascript/controllers/password_toggle_controller.js (data-controller, targets, toggle method — confirmar nomes exatos)
    - .planning/phases/02-admin-auth-client-management/02-UI-SPEC.md (Screen 2: card max-w-lg p-8, Screen 3: campos pré-preenchidos, copywriting completo de labels/placeholders/hints/botões/mensagens de validação)
    - .planning/phases/02-admin-auth-client-management/02-PATTERNS.md (form_with pattern para [:admin, @client], input CSS verbatim, submit button pattern)
    - app/controllers/admin/clients_controller.rb (confirmar @client disponível em new/edit)
  </read_first>
  <behavior>
    - new.html.erb: content_for(:page_title) { "Novo cliente" }. Link "← Voltar para Clientes" (link_to admin_clients_path, Ghost: text-sm text-slate-600 hover:text-slate-900, aria-label="Voltar para lista de clientes"). Card `bg-white rounded-xl border border-gray-200 shadow-card p-8 max-w-lg`. Renderiza partial _form com locals: button_label: "Cadastrar cliente", cancel_path: admin_clients_path.
    - edit.html.erb: content_for(:page_title) { "Editar cliente" }. Link "← Voltar para #{@client.name}" (admin_client_path(@client)). Mesmo card. Renderiza _form com locals: button_label: "Salvar alterações", cancel_path: admin_client_path(@client).
    - _form.html.erb: form_with model: [:admin, client], class: "space-y-5", aria: { label: "Formulário de cliente" }. Campo nome: label "Nome do cliente" + text_field com placeholder "Ex: Loja da Maria", required: true, autofocus: true, CSS class verbatim de sessions/new.html.erb linha 41. Erros inline: `if client.errors[:name].any?` → `span[role="alert"] class="text-xs text-red-600 mt-1 block"`. Campo senha: label "Senha do portal" + div data-controller="password-toggle" + relative wrapper + password_field com data-password-toggle-target="field" e pr-11 extra, placeholder dinâmico ("Mínimo 4 caracteres" em new, "••••••••" em edit) + button toggle com aria-label="Mostrar senha" aria-pressed="false" data-action="click->password-toggle#toggle" data-password-toggle-target="toggle". Hint abaixo: "Escolha uma senha fácil de lembrar para o cliente." (new) / "Deixe em branco para manter a senha atual." (edit) — text-xs text-slate-500. Erros inline de senha. Rodapé: flex justify-end gap-3 com link_to cancel_label "Voltar sem salvar" (Secondary: h-10 px-4 border border-gray-200 rounded-lg text-sm) e submit button_label (Primary Admin: h-10 px-4 bg-[#0F7949] hover:bg-[#0a5c37] active:scale-[0.99] text-white text-sm font-semibold rounded-lg).
    - O partial _form recebe locals: `client` (o objeto @client), `button_label`, `cancel_path`. Verificar se `edit?` é necessário para alternar placeholder de senha — usar `client.persisted?` para distinguir new vs edit.
  </behavior>
  <action>
    Criar app/views/admin/clients/_form.html.erb. Usar `local_assigns` para receber button_label, cancel_path. Detectar estado edit via `client.persisted?` para alternar placeholder de senha e hint. O form_with usa `model: [:admin, client]` — Rails infere a URL correta para new (POST /admin/clients) e edit (PATCH /admin/clients/:id). CSS class do input: copiar verbatim de sessions/new.html.erb linha 41 (`block w-full h-11 px-3 border border-gray-200 rounded-lg text-sm text-slate-900 bg-white focus:outline-none focus:border-[#0F7949] focus:ring-2 focus:ring-[#0F7949]/10 transition-colors placeholder-slate-400`). Campo senha adiciona `pr-11`. O button toggle de senha: reusar data-controller="password-toggle" e targets exatos do password_toggle_controller.js.

    Criar app/views/admin/clients/new.html.erb com content_for e render "form", client: @client, button_label: "Cadastrar cliente", cancel_path: admin_clients_path.

    Criar app/views/admin/clients/edit.html.erb com content_for e render "form", client: @client, button_label: "Salvar alterações", cancel_path: admin_client_path(@client).

    Não criar campos hidden para password_plain neste formulário — o controller já recebe password_plain de client_params; o valor será o mesmo que password no create (responsabilidade do controller: `@client = Client.new(client_params)` onde client_params inclui password_plain). Se o admin criar um cliente, ele digita a senha uma vez e o controller salva tanto `password` quanto `password_plain` com o mesmo valor. O form inclui um campo password_field apenas — não há campo separado para password_plain no formulário de criação/edição. O controller ao criar deve fazer: `params_with_plain = client_params.merge(password_plain: client_params[:password]) if client_params[:password].present?` — verificar se isso já está no controller de 02-01 e ajustar se necessário.
  </action>
  <verify>
    <automated>
      grep "data-controller=\"password-toggle\"" app/views/admin/clients/_form.html.erb &amp;&amp;
      grep "client.persisted?" app/views/admin/clients/_form.html.erb &amp;&amp;
      grep "Mínimo 4 caracteres" app/views/admin/clients/_form.html.erb &amp;&amp;
      grep "Deixe em branco" app/views/admin/clients/_form.html.erb &amp;&amp;
      bin/rails test test/controllers/admin/clients_controller_test.rb 2>&amp;1 | grep "0 failures"
    </automated>
    <human-check>Acesse /admin/clients/new: (1) card max-w-lg visível; (2) campo senha com toggle olho; (3) hint "Escolha uma senha fácil de lembrar"; (4) botões "Voltar sem salvar" + "Cadastrar cliente" à direita. Submeter com campos vazios: erros inline aparecem. Submeter válido: redireciona para show.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep "data-controller=\"password-toggle\"" app/views/admin/clients/_form.html.erb` retorna match
    - `grep "client.persisted?" app/views/admin/clients/_form.html.erb` retorna match (diferencia new vs edit)
    - `grep "shadow-card" app/views/admin/clients/new.html.erb` retorna match (card com sombra)
    - `grep "Voltar sem salvar" app/views/admin/clients/_form.html.erb` retorna match
    - `grep "Deixe em branco para manter" app/views/admin/clients/_form.html.erb` retorna match
    - `grep 'role="alert"' app/views/admin/clients/_form.html.erb` retorna match (erros inline)
    - `bin/rails test test/controllers/admin/clients_controller_test.rb` → 0 failures, 0 errors
  </acceptance_criteria>
  <done>Views new e edit criadas com partial _form compartilhada, toggle de senha, validação inline PT-BR, e testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin form → ClientsController | Dados de criação/edição de cliente passam por strong params; senha em branco na edição não sobrescreve hash existente |
| password_plain escrita | Coluna escrita apenas no create (valor = password) e no update quando senha não está em branco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | create action — password_plain sync | mitigate | Controller sincroniza password_plain com o valor de password no create; sem campos extras no form que permitam discrepância |
| T-02-07 | Tampering | update — blank password bypass | mitigate | D-10: blank password guard no update rejeita sobreescrita com string vazia; implementado como `reject { k.in?([...]) && v.blank? }` |
| T-02-08 | Information Disclosure | Erros de validação no form | accept | Erros exibem mensagens genéricas de campo (não expõem detalhes internos); padrão Rails padrão |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Sem novos pacotes — apenas views ERB e testes minitest |
</threat_model>

<verification>
Após execução de todos os tasks deste plano:

```bash
# Testes passando
bin/rails test test/controllers/admin/clients_controller_test.rb
# → 4 runs, 0 failures, 0 errors

# Form partial existe com password toggle
grep "data-controller=\"password-toggle\"" app/views/admin/clients/_form.html.erb
# → match

# Edit hint presente
grep "Deixe em branco" app/views/admin/clients/_form.html.erb
# → match

# Create end-to-end manual
# Acesse /admin/clients/new, crie "Loja da Maria" senha "ilhacriativa", confirme redirect para /admin/clients/:id
```
</verification>

<success_criteria>
1. Admin cria um cliente em /admin/clients/new e é redirecionado para /admin/clients/:id com flash "Cliente cadastrado com sucesso."
2. Admin edita um cliente com senha em branco e a senha anterior é preservada (password_plain inalterado)
3. Erros de validação aparecem inline abaixo dos campos em PT-BR
4. `bin/rails test test/controllers/admin/clients_controller_test.rb` → 0 failures, 0 errors
5. Campo senha tem toggle mostrar/ocultar funcional via password_toggle_controller existente
</success_criteria>

<output>
Create `.planning/phases/02-admin-auth-client-management/02-02-SUMMARY.md` when done
</output>
