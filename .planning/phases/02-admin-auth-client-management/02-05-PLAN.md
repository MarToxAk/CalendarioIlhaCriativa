---
phase: 02-admin-auth-client-management
plan: "05"
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - app/views/admin/clients/_confirm_modal.html.erb
  - app/views/admin/clients/show.html.erb
  - app/controllers/client_controller.rb
  - app/controllers/client/sessions_controller.rb
  - test/controllers/admin/clients_controller_test.rb
  - test/controllers/client/sessions_controller_test.rb
autonomous: true
gap_closure: true
requirements:
  - CLIE-03

must_haves:
  truths:
    - "Admin clica 'Desativar cliente', confirma no modal e o cliente fica inativo — badge muda para 'Inativo'"
    - "Cliente inativo recebe 403 ao tentar acessar o portal via token"
    - "Cliente inativo não consegue autenticar: /c/:token/session/new retorna 422 com mensagem de erro mesmo com senha correta"
  artifacts:
    - path: "app/views/admin/clients/_confirm_modal.html.erb"
      provides: "Suporte a hidden_fields: local para payload de form"
      contains: "hidden_fields"
    - path: "app/views/admin/clients/show.html.erb"
      provides: "Render de confirm_modal de desativação passando hidden_fields com client[active]=false"
      contains: "active: false"
    - path: "app/controllers/client_controller.rb"
      provides: "Verificação active? após load_client_from_token — retorna 403 para clientes inativos"
      contains: "active?"
    - path: "app/controllers/client/sessions_controller.rb"
      provides: "Verificação active? no create antes de autenticar — retorna 422 para clientes inativos"
      contains: "active?"
    - path: "test/controllers/admin/clients_controller_test.rb"
      provides: "Teste de que update com client[active]=false desativa o cliente"
      contains: "active.*false"
    - path: "test/controllers/client/sessions_controller_test.rb"
      provides: "Teste de que cliente inativo não consegue autenticar"
      contains: "inativo"
  key_links:
    - from: "app/views/admin/clients/show.html.erb"
      to: "app/controllers/admin/clients_controller.rb#update"
      via: "form PATCH com hidden field client[active]=false"
      pattern: "active.*false"
    - from: "app/controllers/client_controller.rb"
      to: "Client#active?"
      via: "guard após find_by!(access_token:)"
      pattern: "active\\?"
    - from: "app/controllers/client/sessions_controller.rb"
      to: "Client#active?"
      via: "guard antes de authenticate no create"
      pattern: "active\\?"
---

<objective>
Fechar os dois blockers do CLIE-03 identificados no VERIFICATION.md da fase 02:

**Blocker 1 — Form de desativação sem payload:** O `_confirm_modal` partial submete PATCH sem `client[active]=false`, causando `ActionController::ParameterMissing` no update action. O partial não aceita `hidden_fields:` local — sem esse suporte, o form não consegue enviar o payload necessário.

**Blocker 2 — Portal não bloqueia clientes inativos:** `client_controller.rb` carrega o cliente por `access_token` sem verificar `active?`. `client/sessions_controller.rb` autentica via senha sem verificar `active?`. Cliente desativado continua acessando o portal.

Purpose: CLIE-03 ("Admin pode desativar um cliente (bloqueia acesso ao portal)") está completamente bloqueado. Este plano entrega as correções mínimas e os testes de regressão que provam o comportamento end-to-end.
Output: `_confirm_modal` com suporte a `hidden_fields:`, render de desativação corrigido em `show.html.erb`, guards de `active?` nos dois controllers do portal, e testes de integração cobrindo o fluxo completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-auth-client-management/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir _confirm_modal (hidden_fields support) + show.html.erb (passar active:false)</name>
  <files>
    app/views/admin/clients/_confirm_modal.html.erb
    app/views/admin/clients/show.html.erb
    test/controllers/admin/clients_controller_test.rb
  </files>
  <read_first>
    - app/views/admin/clients/_confirm_modal.html.erb — ver form_with atual (linha 44): `form_with url: form_action, method: method` sem hidden fields — esse é o stub a corrigir
    - app/views/admin/clients/show.html.erb — linhas 14-38: ver render do confirm_modal de desativação (sem hidden_fields: atualmente) e o button_to de reativação com params: { client: { active: true } } (referência de como passar params)
    - app/views/admin/clients/show.html.erb — linhas 100-108: ver render do modal de rotação (NÃO alterar — usa POST sem payload e continua funcionando)
    - test/controllers/admin/clients_controller_test.rb — ver testes existentes para entender o padrão: setup com @client, sign_in_as(@admin), assert_redirected_to, assert_equal
    - app/controllers/admin/clients_controller.rb — verificar que strong parameters já incluem :active (params.require(:client).permit(..., :active))
  </read_first>
  <behavior>
    - _confirm_modal com hidden_fields omitido (nil): form renderizado sem hidden_field_tags — comportamento atual preservado (modal de rotação de token continua funcionando)
    - _confirm_modal com hidden_fields: { client: { active: false } }: form inclui `hidden_field_tag "client[active]", false` dentro do form_with
    - _confirm_modal com hidden_fields: { client: { active: true } } (regressão hipotética): form inclui `hidden_field_tag "client[active]", true`
    - show.html.erb render de desativação: passa hidden_fields: { client: { active: false } }
    - show.html.erb render de rotação: NÃO alterado — sem hidden_fields
    - Teste: PATCH /admin/clients/:id com params { client: { active: false } } retorna redirect e o client.reload.active? == false
    - Teste de regressão: testes existentes (4 runs) continuam passando
  </behavior>
  <action>
    Em `app/views/admin/clients/_confirm_modal.html.erb`, adicionar no início do partial (após os comentários iniciais, antes do div overlay) a extração do local:

    `<% hidden_fields = local_assigns.fetch(:hidden_fields, nil) %>`

    Dentro do bloco `form_with url: form_action, method: method do |f|`, ANTES do `f.submit`, adicionar o loop de hidden fields. O hash hidden_fields tem dois níveis de profundidade (ex: { client: { active: false } }), então iterar com flattening:

    ```
    <% if hidden_fields %>
      <% hidden_fields.each do |outer_key, outer_value| %>
        <% if outer_value.is_a?(Hash) %>
          <% outer_value.each do |inner_key, inner_value| %>
            <%= hidden_field_tag "#{outer_key}[#{inner_key}]", inner_value %>
          <% end %>
        <% else %>
          <%= hidden_field_tag outer_key, outer_value %>
        <% end %>
      <% end %>
    <% end %>
    ```

    Não usar fenced code blocks na implementação — implementar o erb diretamente nos arquivos.

    Em `app/views/admin/clients/show.html.erb`, localizar o render do confirm_modal de desativação (render "confirm_modal" com form_action: admin_client_path(@client), method: :patch) e adicionar o local `hidden_fields: { client: { active: false } }` ao render call. O render do modal de rotação (rotate_token_admin_client_path) NÃO deve ser alterado.

    Em `test/controllers/admin/clients_controller_test.rb`, adicionar dois novos testes após os existentes:

    Teste 1 — "update com active: false desativa o cliente (CLIE-03)":
    - `patch admin_client_path(@client), params: { client: { active: false } }`
    - `assert_redirected_to admin_client_path(@client)`
    - `@client.reload`
    - `assert_equal false, @client.active?`

    Teste 2 — "update com active: true reativa o cliente (CLIE-03)":
    - `@client.update!(active: false)` para começar inativo
    - `patch admin_client_path(@client), params: { client: { active: true } }`
    - `assert_redirected_to admin_client_path(@client)`
    - `@client.reload`
    - `assert_equal true, @client.active?`

    Os novos testes usam o mesmo setup já definido (@admin autenticado via sign_in_as, @client criado com Client.create!). O modelo Client tem `active` boolean com default: true na tabela clients (confirmado no schema.rb linha 47).
  </action>
  <verify>
    <automated>grep -c "hidden_fields" app/views/admin/clients/_confirm_modal.html.erb &amp;&amp; grep "active: false" app/views/admin/clients/show.html.erb &amp;&amp; grep "rotate_token_admin_client_path" app/views/admin/clients/show.html.erb &amp;&amp; bin/rails test test/controllers/admin/clients_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hidden_fields" app/views/admin/clients/_confirm_modal.html.erb` retorna valor >= 2 (declaração fetch + uso no loop)
    - `grep "active: false" app/views/admin/clients/show.html.erb` retorna match na linha do render confirm_modal de desativação
    - `grep "rotate_token_admin_client_path" app/views/admin/clients/show.html.erb` retorna match (modal de rotação intacto)
    - `grep "hidden_fields" app/views/admin/clients/show.html.erb` NÃO retorna match no render do modal de rotação (rotate_token não usa hidden_fields)
    - `bin/rails test test/controllers/admin/clients_controller_test.rb` retorna "6 runs, 0 failures, 0 errors" (4 existentes + 2 novos)
    - `grep "active.*false\|CLIE-03" test/controllers/admin/clients_controller_test.rb` retorna match nos novos testes
  </acceptance_criteria>
  <done>
    O `_confirm_modal` suporta `hidden_fields:` local. O render de desativação em `show.html.erb` passa `hidden_fields: { client: { active: false } }`, gerando `hidden_field_tag "client[active]", false` dentro do form PATCH. Os dois novos testes provam que PATCH com active:false desativa e PATCH com active:true reativa. Modal de rotação continua funcionando sem alterações.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bloquear clientes inativos no portal + testes de regressão</name>
  <files>
    app/controllers/client_controller.rb
    app/controllers/client/sessions_controller.rb
    test/controllers/client/sessions_controller_test.rb
  </files>
  <read_first>
    - app/controllers/client_controller.rb — inteiro (24 linhas): load_client_from_token na linha 9 faz find_by!(access_token:) sem verificar active?; require_client_auth nas linhas 15-22 verifica sessão mas também não verifica active?
    - app/controllers/client/sessions_controller.rb — inteiro (23 linhas): create action na linha 8 chama @client.authenticate sem guard de active?; skip_before_action :require_client_auth, only: [:new, :create] (linha 2) — significa que load_client_from_token roda mas require_client_auth não roda no create
    - test/controllers/client/sessions_controller_test.rb — ver 5 testes existentes: setup cria @client com active: true implícito (default), todos os testes devem continuar passando
    - app/models/client.rb — active? vem do atributo booleano padrão do ActiveRecord (t.boolean "active", default: true, null: false no schema) — não precisa de método custom
  </read_first>
  <behavior>
    - Cliente ativo + token válido: comportamento atual preservado — load_client_from_token carrega @client, prossegue normalmente
    - Cliente inativo + token válido: load_client_from_token carrega @client, guard `unless @client.active?` renderiza 403 com plain text "Acesso bloqueado" e retorna — nenhuma action subsequente é executada
    - Cliente inativo + senha correta no sessions#create: guard `unless @client.active?` dispara ANTES do authenticate, flash.now[:alert] = "Acesso bloqueado. Entre em contato com o administrador.", render :new com status 422, return
    - Token inválido: rescue ActiveRecord::RecordNotFound preservado — render plain: "Link inválido", status: :not_found
    - Sessão de cliente inativo via require_client_auth: o guard em load_client_from_token roda primeiro (before_action order), então cliente inativo nunca chega ao require_client_auth
    - Testes novos: cliente inativo não autentica (422), cliente inativo recebe 403 no portal
    - Testes existentes: 5 runs existentes continuam passando (todos criam @client sem active: false, portanto active = true por default)
  </behavior>
  <action>
    Em `app/controllers/client_controller.rb`, no método `load_client_from_token`, inserir guard de active APÓS a linha `@client = Client.find_by!(access_token: params[:token])` e DENTRO do bloco begin (antes do rescue). A estrutura correta após a edição:

    ```ruby
    def load_client_from_token
      @client = Client.find_by!(access_token: params[:token])
      unless @client.active?
        render plain: "Acesso bloqueado", status: :forbidden
      end
    rescue ActiveRecord::RecordNotFound
      render plain: "Link inválido", status: :not_found
    end
    ```

    O `unless @client.active?` não precisa de `return` explícito porque `render` em Rails sinaliza a resposta. O rescue cobre apenas o find_by! — o guard de active fica dentro do begin implícito do método mas antes do rescue.

    Em `app/controllers/client/sessions_controller.rb`, no método `create`, inserir guard de active ANTES da linha `if @client.authenticate(params[:password])`. Incluir `return` explícito para evitar double render:

    ```ruby
    def create
      unless @client.active?
        flash.now[:alert] = "Acesso bloqueado. Entre em contato com o administrador."
        render :new, status: :unprocessable_entity
        return
      end
      if @client.authenticate(params[:password])
        session[:client_id]            = @client.id
        session[:client_token_version] = @client.token_version
        redirect_to client_root_path(token: @client.access_token)
      else
        flash.now[:alert] = "Senha incorreta. Tente novamente."
        render :new, status: :unprocessable_entity
      end
    end
    ```

    Em `test/controllers/client/sessions_controller_test.rb`, adicionar dois testes após os existentes:

    Teste 1 — "POST com senha correta mas cliente inativo retorna 422 com mensagem de bloqueio":
    - `@client.update!(active: false)`
    - `post client_session_path(token: @client.access_token), params: { password: "senha123" }`
    - `assert_response :unprocessable_entity`
    - `assert_includes response.body, "Acesso bloqueado"`

    Teste 2 — "GET portal de cliente inativo retorna 403 (load_client_from_token guard)":
    - Criar @inactive_client com active: false via `Client.create!(name: "Inativo", password: "x", active: false)`
    - `get new_client_session_path(token: @inactive_client.access_token)`
    - `assert_response :forbidden`
    - `assert_includes response.body, "Acesso bloqueado"`

    O segundo teste verifica o guard em load_client_from_token que bloqueia QUALQUER rota do portal para clientes inativos — incluindo a tela de login new, que passa por load_client_from_token (a rota client/sessions#new inclui :token no path e antes_action load_client_from_token roda em ClientController, parent de Client::SessionsController, mesmo para :new e :create que têm skip_before_action :require_client_auth mas NÃO skip de load_client_from_token).
  </action>
  <verify>
    <automated>grep -c "active?" app/controllers/client_controller.rb &amp;&amp; grep -c "active?" app/controllers/client/sessions_controller.rb &amp;&amp; grep "forbidden" app/controllers/client_controller.rb &amp;&amp; bin/rails test test/controllers/client/sessions_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "active?" app/controllers/client_controller.rb` retorna >= 1
    - `grep -c "active?" app/controllers/client/sessions_controller.rb` retorna >= 1
    - `grep "forbidden" app/controllers/client_controller.rb` retorna match (status :forbidden no guard de inactive)
    - `grep "unprocessable_entity" app/controllers/client/sessions_controller.rb` retorna match na guard de inactive (além do match já existente no bloco de senha errada)
    - `grep "return" app/controllers/client/sessions_controller.rb` retorna match (return explícito após render no guard de inactive)
    - `bin/rails test test/controllers/client/sessions_controller_test.rb` retorna "7 runs, 0 failures, 0 errors" (5 existentes + 2 novos)
    - `bin/rails test test/controllers/` retorna 0 failures, 0 errors (todos os controllers combinados)
  </acceptance_criteria>
  <done>
    Clientes inativos recebem 403 ao acessar qualquer rota do portal via token (guard em load_client_from_token), e 422 com mensagem "Acesso bloqueado" ao tentar autenticar em sessions#create. Os 5 testes existentes continuam passando. O fluxo CLIE-03 está completo end-to-end: admin desativa via modal (Task 1) → controller salva active=false → portal rejeita cliente inativo (Task 2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| form modal → admin controller | PATCH com hidden field client[active]=false vem de admin autenticado via before_action authenticate |
| token URL → portal controller | Token vem de URL pública; cliente pode estar inativo — guard adicionado neste plano |
| portal login → sessions controller | POST com senha vem de URL pública; cliente inativo não deve obter sessão — guard adicionado neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | `_confirm_modal` hidden field active | mitigate | CSRF token automático via form_with Rails; admin autenticado via before_action em Admin::BaseController |
| T-02-15 | Elevation of Privilege | `client_controller.rb` load_client_from_token | mitigate | Guard `unless @client.active?` renderiza 403 antes de qualquer action do portal — adicionado neste plano |
| T-02-16 | Elevation of Privilege | `client/sessions_controller.rb` create | mitigate | Verificação `@client.active?` antes de `authenticate` impede obtenção de sessão por cliente inativo; `return` explícito evita double render |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum novo pacote instalado neste plano |
</threat_model>

<verification>
Após execução das duas tasks, validar:

```bash
# Hidden fields no partial
grep -c "hidden_fields" app/views/admin/clients/_confirm_modal.html.erb
# → retorna >= 2

# active:false no render de desativação
grep "active: false" app/views/admin/clients/show.html.erb
# → retorna match

# Guard no portal controller
grep "active?" app/controllers/client_controller.rb
# → retorna match

# Guard no sessions controller
grep "active?" app/controllers/client/sessions_controller.rb
# → retorna match

# Status 403 no guard de inactive
grep "forbidden" app/controllers/client_controller.rb
# → retorna match

# Todos os testes passam
bin/rails test test/controllers/
# → 0 failures, 0 errors
```
</verification>

<success_criteria>
1. CLIE-03 satisfeito: admin desativa cliente via modal (form envia client[active]=false) e o portal bloqueia cliente inativo com 403
2. `bin/rails test test/controllers/` — 0 failures, 0 errors (todos os controllers)
3. Modal de rotação de token continua funcionando (não usa hidden_fields — não alterado)
4. Reativação via button_to com params: { client: { active: true } } continua funcionando (não alterada)
5. Testes novos em admin/clients_controller_test.rb: 6 runs totais (4 existentes + 2 de active:false/true)
6. Testes novos em client/sessions_controller_test.rb: 7 runs totais (5 existentes + 2 de cliente inativo)
</success_criteria>

<output>
Criar `.planning/phases/02-admin-auth-client-management/02-05-SUMMARY.md` ao concluir
</output>
