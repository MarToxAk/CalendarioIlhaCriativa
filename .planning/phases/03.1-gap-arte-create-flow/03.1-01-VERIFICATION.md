---
phase: 03.1-gap-arte-create-flow
verified: 2026-05-27T00:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Acessar o show de um cliente no painel admin, clicar em 'Nova Arte', preencher o formulário, submeter e confirmar que a arte criada fica associada ao cliente correto (client_id não nulo)"
    expected: "Arte criada com client_id igual ao cliente de origem; redirecionamento para admin_arte_path da arte criada"
    why_human: "O hidden_field e o arte_params estão corretamente conectados, mas a validação end-to-end de que o client_id preenchido pelo Arte.new(client: @client) chega ao banco requer navegação real ou teste de integração completo com ActiveRecord; os testes automatizados existentes cobrem o happy path, mas a confirmação visual fecha o ciclo de produção"
  - test: "No formulário de criação de arte, selecionar 'Upload de arquivo' e confirmar que apenas o campo de arquivo fica visível; selecionar 'Link externo' e confirmar que apenas o campo de URL fica visível"
    expected: "Nunca dois campos de mídia exibidos simultaneamente; toggle funciona sem página recarregar (Stimulus)"
    why_human: "A classe CSS ternária está correta no HTML renderizado, mas o comportamento do Stimulus media-type-toggle depende de JavaScript no browser; não é verificável por grep ou curl"
  - test: "Clicar no link 'Artes' na sidebar e confirmar que navega para /admin/artes com a lista de artes"
    expected: "Página /admin/artes carrega com status 200 e exibe o index de artes"
    why_human: "Navegação real na UI para confirmar que o link active-state funciona corretamente e que a rota está acessível"
---

# Phase 03.1 Plan 01: Verificacao do Gap Arte Create Flow

**Phase Goal:** Fechar os gaps que impedem o admin de criar artes pelo painel (client_id nil, media toggle inoperante, sidebar sem link para Artes, tela de cliente sem secao de artes)
**Verified:** 2026-05-27T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Admin cria uma arte a partir da tela do cliente e a arte fica associada ao cliente correto (client_id nao nulo) | VERIFIED | `f.hidden_field :client_id` na linha 43 de `_form.html.erb`; `:client_id` em `arte_params` (linha 67 do controller); `Arte.new(client: @client)` no `new` action; `set_client before_action` carrega `@client` via `params[:client_id]` |
| 2 | O formulario exibe apenas um dos campos de midia por vez — upload ou link externo — nunca os dois simultaneamente | VERIFIED | Linha 34: `class="<%= arte.media_file.attached? ? 'mb-4' : 'mb-4 hidden' %>"` — ternario unico sem duplicate class attribute; linha 38: idem para `external_url.present?`; `grep -c "class='hidden'" _form.html.erb` retorna `0` |
| 3 | A sidebar exibe o link 'Artes' que leva para admin_artes_path | VERIFIED | `{ label: "Artes", path: admin_artes_path }` na linha 15 de `_sidebar.html.erb`, posicionado apos "Clientes" (linha 14) e antes de "Calendario" (linha 16) |
| 4 | A tela show do cliente exibe a secao 'Artes' com lista de artes do cliente e botao 'Nova Arte' pre-preenchido | VERIFIED | Card "Artes do Cliente" presente nas linhas 134-163 de `clients/show.html.erb`; itera `@artes`; exibe mensagem de lista vazia; botao "Nova Arte" linka para `new_admin_arte_path(client_id: @client.id)` na linha 138 |
| 5 | O fluxo GET new?client_id=X pre-preenche o cliente no formulario | VERIFIED | `set_client before_action` faz `Client.find_by(id: params[:client_id])`; `Arte.new(client: @client)` define o atributo; testes "should get new with client_id pre-filled" e "should get new without client_id" passam (13 runs, 0 failures) |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/views/admin/artes/_form.html.erb` | Formulario com hidden_field :client_id e classes CSS sem duplicata | VERIFIED | `hidden_field :client_id` na linha 43; classe ternaria nas linhas 34 e 38; zero ocorrencias de `class='hidden'` |
| `app/views/admin/shared/_sidebar.html.erb` | Nav com link Artes | VERIFIED | `{ label: "Artes", path: admin_artes_path }` na linha 15, entre Clientes e Calendario |
| `app/views/admin/clients/show.html.erb` | Card de artes do cliente com botao Nova Arte | VERIFIED | Card completo nas linhas 134-163 com botao `new_admin_arte_path(client_id: @client.id)`, loop `@artes.each`, mensagem de lista vazia, campos `arte.title`, `arte.scheduled_on.strftime`, `arte.platform`, `arte.status` |
| `app/controllers/admin/clients_controller.rb` | show action carregando @artes | VERIFIED | Linha 9: `@artes = @client.artes.order(scheduled_on: :desc)` dentro do `show`; `@artes_with_responses` preservado (linhas 10-14) |
| `test/controllers/admin/artes_controller_test.rb` | Teste do fluxo GET new com client_id | VERIFIED | Linhas 97-106: dois novos testes adicionados; suite completa executa 13 runs, 33 assertions, 0 failures, 0 errors, 0 skips |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/views/admin/artes/_form.html.erb` | `app/controllers/admin/artes_controller.rb` | `hidden_field :client_id` propagado no POST | WIRED | `f.hidden_field :client_id` (form linha 43); `:client_id` em `arte_params` (controller linha 67); `Arte.new(arte_params)` no create action |
| `app/views/admin/clients/show.html.erb` | `app/views/admin/artes/new.html.erb` | `new_admin_arte_path(client_id: @client.id)` | WIRED | Linha 138 contem `new_admin_arte_path(client_id: @client.id)` — URL param capturado por `set_client before_action` no ArtesController |
| `app/controllers/admin/clients_controller.rb` | `app/models/arte.rb` | `@client.artes.order(scheduled_on: :desc)` | WIRED | Linha 9 do show action; `Client has_many :artes` (model linha 5); `Arte belongs_to :client` (model linha 2) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `app/views/admin/clients/show.html.erb` (card Artes) | `@artes` | `@client.artes.order(scheduled_on: :desc)` em `ClientsController#show` | Sim — ActiveRecord query escopada ao cliente correto via associacao `has_many` | FLOWING |
| `app/views/admin/artes/_form.html.erb` | `f.hidden_field :client_id` | `arte.client_id` preenchido via `Arte.new(client: @client)` no `new` action; `@client` carregado por `Client.find_by(id: params[:client_id])` | Sim — flui de URL param -> before_action -> Arte instance -> hidden_field | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Suite de testes do ArtesController passa | `bin/rails test test/controllers/admin/artes_controller_test.rb` | 13 runs, 33 assertions, 0 failures, 0 errors, 0 skips | PASS |
| hidden_field :client_id presente no form | `grep -c "hidden_field :client_id" app/views/admin/artes/_form.html.erb` | 1 | PASS |
| Duplicate class attribute removido | `grep -c "class='hidden'" app/views/admin/artes/_form.html.erb` | 0 | PASS |
| Link Artes na sidebar | `grep -c "admin_artes_path" app/views/admin/shared/_sidebar.html.erb` | 1 | PASS |
| @artes carregado no controller | `grep -c "@artes = @client.artes" app/controllers/admin/clients_controller.rb` | 1 | PASS |
| Botao Nova Arte com client_id | `grep -c "new_admin_arte_path" app/views/admin/clients/show.html.erb` | 1 (com `client_id: @client.id`) | PASS |
| Todos os commits declarados existem no historico | `git log --oneline c1b6da0 4391127 51f0f84 62adfd8` | 4 commits encontrados | PASS |

---

### Requirements Coverage

| Requirement | Descricao em REQUIREMENTS.md | Status | Evidencia |
|-------------|------------------------------|--------|-----------|
| ARTE-01 | Admin pode criar uma arte associada a um cliente e a uma data especifica | SATISFIED | hidden_field :client_id + campo `scheduled_on` no form; `arte_params` permite ambos; fluxo de criacao testado |
| ARTE-02 | Admin pode fazer upload direto de arquivo (imagem ou video) para a arte | SATISFIED | Campo `f.file_field :media_file` no form; `:media_file` em `arte_params`; forma: `multipart: true` no form tag |
| ARTE-03 | Admin pode colar um link externo (Google Drive, Dropbox) como arquivo da arte | SATISFIED | Campo `f.url_field :external_url` no form; `:external_url` em `arte_params` |
| ARTE-04 | Admin pode definir a plataforma da arte (Instagram, Facebook ou LinkedIn) | SATISFIED | `f.select :platform, Arte.platforms.keys` no form; `:platform` em `arte_params` |
| ARTE-05 | Admin pode definir o formato da arte (imagem, video ou legenda) | SATISFIED | `f.select :media_type, Arte.media_types.keys` no form; `:media_type` em `arte_params` |
| ARTE-06 | Admin pode definir uma data limite de aprovacao para a arte | SATISFIED | `f.date_field :approval_deadline` no form; `:approval_deadline` em `arte_params` |
| ARTE-07 | Admin pode adicionar uma legenda/texto que acompanha a arte | SATISFIED | `f.text_area :caption` no form; `:caption` em `arte_params` |

**Todos os 7 requirements declarados no PLAN estao satisfeitos.**

Nota: ARTE-08 (editar arte) e ARTE-09 (excluir arte) estao mapeados para Phase 3 em REQUIREMENTS.md mas NAO foram declarados no frontmatter deste plano (escopo correto — eram funcionais antes desta fase). Eles nao sao gaps desta fase.

---

### Probe Execution

Step 7c: SKIPPED — nenhum probe `scripts/*/tests/probe-*.sh` declarado neste plano e nenhum encontrado na estrutura do projeto para esta fase.

---

### Anti-Patterns Found

| Arquivo | Linha | Padrao | Severidade | Impacto |
|---------|-------|--------|------------|---------|
| `app/views/admin/clients/show.html.erb` | 25, 104 | `"#{@client.name}"` interpolado em string passada para `raw(body)` no partial `_confirm_modal` | INFO (preexistente, nao introduzido por esta fase) | XSS potencial — detectado no REVIEW.md como CR-01; nao eh bloqueador desta fase pois o defito existe desde Phase 2 |
| `app/views/admin/clients/show.html.erb` | 153, 181 | `arte.scheduled_on.strftime` sem nil guard | WARNING (preexistente — linha 181 existe desde phase 6; linha 153 introduzida por esta fase) | Crash `NoMethodError` se `scheduled_on` for nil; modelo valida presenca, mas a linha 153 eh nova nesta fase |
| `app/views/admin/clients/show.html.erb` | 183 | `sort_by(&:created_at).last` sobre associacao com `order desc` | WARNING (linha nao modificada por esta fase) | Logica redundante/confusa — detectada no REVIEW.md WR-02 |

**Nenhum marcador TBD, FIXME ou XXX encontrado** nos 5 arquivos modificados.

Nenhum anti-pattern dos arquivos *modificados por esta fase* constitui BLOCKER para o goal. A linha 153 introduzida por esta fase tem o risco de nil-crash em `arte.scheduled_on.strftime` — modelado como WARNING porque `scheduled_on` tem validacao de presenca, mas nao eh um bloqueador de producao imediato.

---

### Human Verification Required

#### 1. Fluxo end-to-end de criacao de arte a partir do show do cliente

**Test:** Acessar o show de um cliente, clicar em "Nova Arte", verificar que o formulario abre com o campo hidden client_id preenchido (inspecionando o HTML), preencher todos os campos obrigatorios e submeter.
**Expected:** Arte salva no banco com `client_id` igual ao cliente de origem; redirect para `admin_arte_path` da arte criada; arte aparece na lista do card "Artes" na proxima visita ao show do cliente.
**Why human:** A conexao hidden_field -> arte_params -> ActiveRecord esta correta no codigo, mas a confirmacao final de que o valor nao eh nil no banco fecha o loop de producao.

#### 2. Comportamento do toggle de midia no browser

**Test:** Abrir o formulario de nova arte, selecionar o radio "Upload de arquivo" e confirmar que apenas o campo de arquivo fica visivel; em seguida selecionar "Link externo" e confirmar que apenas o campo de URL fica visivel.
**Expected:** Nunca dois campos de midia visiveis ao mesmo tempo; troca sem reload de pagina via Stimulus.
**Why human:** A classe ternaria CSS esta correta, mas o comportamento do `media-type-toggle` Stimulus controller (arquivo JS) nao foi verificado nesta fase — requer execucao no browser.

#### 3. Navegacao pela sidebar

**Test:** Clicar no link "Artes" na sidebar e confirmar que navega para `/admin/artes`.
**Expected:** Pagina de index de artes carrega com status 200.
**Why human:** Confirma que a rota `admin_artes_path` esta registrada e o layout renderiza corretamente em ambiente real.

---

### Gaps Summary

Nenhum gap bloqueador identificado. Todos os 5 must-haves verificados. Todos os 7 requirements satisfeitos com evidencia no codigo.

Os 3 itens de verificacao humana acima sao requeridos para fechar o ciclo de producao:
1. Confirmacao visual do fluxo client_id end-to-end
2. Teste do toggle de midia no browser (JavaScript/Stimulus)
3. Navegacao real pela sidebar

---

_Verified: 2026-05-27T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
