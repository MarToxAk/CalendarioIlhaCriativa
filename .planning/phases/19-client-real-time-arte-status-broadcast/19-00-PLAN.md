---
phase: 19-client-real-time-arte-status-broadcast
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/channels/client_calendar_channel_test.rb
  - test/models/arte_test.rb
  - app/channels/client_calendar_channel.rb
  - app/views/layouts/client.html.erb
  - app/views/client/home/index.html.erb
  - app/views/client/home/_month_calendar.html.erb
  - app/views/client/home/_arte_calendar_chip.html.erb
  - app/views/client/home/_calendar_summary.html.erb
  - app/views/client/shared/_arte_revised_toast.html.erb
autonomous: true
requirements:
  - RTUP-05
  - RTUP-06
  - RTUP-07
  - RTUP-01

must_haves:
  truths:
    - "Canal ClientCalendarChannel subscreve com stream_for current_client e rejeita sem current_client"
    - "Layout do cliente contém turbo_stream_from @client com guard if @client e div#client-toast-region"
    - "Partial _arte_calendar_chip.html.erb existe com id: dom_id(arte, 'calendar_chip') no link_to"
    - "Partial _calendar_summary.html.erb existe com id='calendar-summary' sempre no DOM (hidden quando total==0)"
    - "Partial _arte_revised_toast.html.erb existe com data-controller='toast' e botão data-action='click->toast#dismiss'"
    - "index.html.erb substitui div inline de resumo por render de _calendar_summary"
    - "_month_calendar.html.erb substitui loop link_to inline por render de _arte_calendar_chip"
    - "Testes RED escritos confirmam falha antes do callback no model existir"
  artifacts:
    - path: "app/channels/client_calendar_channel.rb"
      provides: "Canal ActionCable para cliente — stream_for current_client"
      contains: "class ClientCalendarChannel"
    - path: "app/views/client/home/_arte_calendar_chip.html.erb"
      provides: "Partial do chip da arte com DOM id para Turbo Stream replace"
      contains: "dom_id(arte, \"calendar_chip\")"
    - path: "app/views/client/home/_calendar_summary.html.erb"
      provides: "Partial da faixa de resumo com id='calendar-summary' sempre no DOM"
      contains: "calendar-summary"
    - path: "app/views/client/shared/_arte_revised_toast.html.erb"
      provides: "Toast do cliente — data-controller=toast, conteúdo revisada + link"
      contains: "data-controller=\"toast\""
    - path: "test/channels/client_calendar_channel_test.rb"
      provides: "Testes RED para subscrição e rejeição do canal"
      contains: "ClientCalendarChannelTest"
    - path: "test/models/arte_test.rb"
      provides: "Testes RED para broadcasts_revised_to_all — 3 streams cliente, 1 stream admin"
      contains: "broadcasts_revised_to_all"
  key_links:
    - from: "app/views/layouts/client.html.erb"
      to: "app/channels/client_calendar_channel.rb"
      via: "turbo_stream_from @client, channel: ClientCalendarChannel"
      pattern: "ClientCalendarChannel"
    - from: "app/views/client/home/index.html.erb"
      to: "app/views/client/home/_calendar_summary.html.erb"
      via: "render 'client/home/calendar_summary', summary: @summary"
      pattern: "calendar_summary"
    - from: "app/views/client/home/_month_calendar.html.erb"
      to: "app/views/client/home/_arte_calendar_chip.html.erb"
      via: "render 'client/home/arte_calendar_chip', arte: arte, client: client"
      pattern: "arte_calendar_chip"
---

<objective>
Instalar a infraestrutura de recepção real-time do cliente: canal ActionCable, layout com turbo_stream_from, partials com DOM IDs estáveis para Turbo Stream replace/append, e testes RED confirmando que o callback (a ser criado no plano 19-01) ainda não existe. Esta é a Wave 1 — tudo que 19-01 precisará já existirá quando ele rodar.

Purpose: Os Turbo Streams do broadcast precisam de targets com IDs estáveis no DOM do cliente. O canal precisa existir para que turbo_stream_from funcione. Os partials precisam existir para que o model possa renderizá-los em 19-01.

Output: Canal ClientCalendarChannel, 3 partials novos do cliente, 2 views modificadas (index + month_calendar), layout do cliente atualizado com turbo_stream_from + toast region, testes RED prontos para ficarem GREEN em 19-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-client-real-time-arte-status-broadcast/19-CONTEXT.md
@.planning/phases/19-client-real-time-arte-status-broadcast/19-RESEARCH.md
@.planning/phases/19-client-real-time-arte-status-broadcast/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED — ClientCalendarChannel e Arte#broadcasts_revised_to_all</name>
  <files>test/channels/client_calendar_channel_test.rb, test/models/arte_test.rb</files>
  <read_first>
    - test/channels/admin_notifications_channel_test.rb — template exato a adaptar (stub_connection, assert_has_stream_for)
    - test/models/arte_test.rb — setup existente (@client, Arte.create! patterns)
    - test/fixtures/clients.yml — confirmar que clients(:one) existe com access_token
    - app/models/approval_response.rb — padrão de stub com lambda (.stub(:broadcast_to, ->(c, content) { }))
  </read_first>
  <behavior>
    - Test canal subscribed: stub_connection(current_client: clients(:one)) → subscribe → assert subscription.confirmed? + assert_has_stream_for clients(:one)
    - Test canal rejected: stub_connection(current_user: nil, current_client: nil) → subscribe → assert subscription.rejected?
    - Test model broadcast (RED agora): Arte.create! com status :change_requested → arte.revised! → ClientCalendarChannel.broadcast_to chamado 1x, AdminNotificationsChannel.broadcast_to chamado 1x
    - Test model stream count (RED agora): client_calls.first contém exatamente 3 turbo-streams; admin_calls.first contém exatamente 1 turbo-stream
    - Test model no-op: Arte.create! com status :pending → arte.update!(title: "X") sem mudar status → ClientCalendarChannel.broadcast_to NÃO chamado (assert_empty client_calls)
  </behavior>
  <action>
    Criar test/channels/client_calendar_channel_test.rb como classe ClientCalendarChannelTest < ActionCable::Channel::TestCase com dois testes: "subscribes and streams for current client" (stub_connection com current_client: clients(:one), subscribe, assert confirmed?, assert_has_stream_for clients(:one)) e "rejects subscription without current client" (stub_connection com current_user: nil current_client: nil, subscribe, assert rejected?).

    Em test/models/arte_test.rb, adicionar ao final da classe (após o último test, antes do end) três testes novos:
    1. "revised! dispara broadcast para ClientCalendarChannel e AdminNotificationsChannel" — cria arte com platform: :instagram, media_type: :image, status: :change_requested, external_url, usa client_calls=[] e admin_calls=[] como capturas, stub duplo com lambda ->(c, content){calls << content}, chama arte.revised!, assert_equal 1, client_calls.length; assert_equal 1, admin_calls.length; assert_equal 3, client_calls.first.scan(/<turbo-stream/).count com mensagem "Cliente deve receber 3 turbo streams"; assert_equal 1, admin_calls.first.scan(/<turbo-stream/).count com mensagem "Admin deve receber 1 turbo stream".
    2. "revised! nao dispara broadcast quando update nao muda status para revised" — cria arte com status: :pending, stub ClientCalendarChannel.broadcast_to, chama arte.update!(title: "Novo titulo"), assert_empty client_calls.
    3. "revised! nao dispara broadcast quando status muda mas nao para revised" — cria arte com status: :change_requested, stub ClientCalendarChannel.broadcast_to, chama arte.approved!, assert_empty client_calls.

    Os testes do canal devem passar já (canal ainda não existe — RED). Os testes do model devem falhar com NameError ou NoMethodError (broadcast ainda não implementado) — confirmar RED antes de avançar.
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && rails test test/channels/client_calendar_channel_test.rb 2>&1 | tail -5</automated>
    Esperado: NameError ou LoadError (ClientCalendarChannel não existe) — RED confirmado.
    Após criar o canal na Task 2, reexecutar: rails test test/channels/client_calendar_channel_test.rb — deve passar (2 tests, 0 failures).
    rails test test/models/arte_test.rb 2>&1 | tail -5 — deve falhar com NoMethodError broadcasts_revised_to_all não definido — RED confirmado.
  </verify>
  <done>
    test/channels/client_calendar_channel_test.rb existe com 2 testes (subscribed + rejected).
    test/models/arte_test.rb tem 3 novos testes de broadcast.
    Testes do canal ficam GREEN após Task 2 criar o canal.
    Testes do model ficam RED até 19-01 implementar o callback.
  </done>
  <acceptance_criteria>
    - test/channels/client_calendar_channel_test.rb contém "class ClientCalendarChannelTest < ActionCable::Channel::TestCase"
    - test/channels/client_calendar_channel_test.rb contém "assert_has_stream_for clients(:one)"
    - test/channels/client_calendar_channel_test.rb contém "assert subscription.rejected?"
    - test/models/arte_test.rb contém "broadcasts_revised_to_all"
    - test/models/arte_test.rb contém "scan(/<turbo-stream/).count"
    - rails test test/channels/client_calendar_channel_test.rb retorna "2 runs, 0 failures" após Task 2 completar
    - rails test test/models/arte_test.rb retorna falha nos 3 novos testes até 19-01 rodar (RED confirmado)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ClientCalendarChannel + Layout do cliente com turbo_stream_from e toast region</name>
  <files>app/channels/client_calendar_channel.rb, app/views/layouts/client.html.erb</files>
  <read_first>
    - app/channels/admin_notifications_channel.rb — template exato (reject unless, stream_for) — copiar trocando current_user por current_client
    - app/channels/application_cable/connection.rb — confirmar que current_client está em identified_by
    - app/views/layouts/admin.html.erb — linhas 23-25 — padrão turbo_stream_from + toast region a replicar
    - app/views/layouts/client.html.erb — linha 14 (<body ...>) — ponto exato de inserção das duas linhas
  </read_first>
  <action>
    Criar app/channels/client_calendar_channel.rb com classe ClientCalendarChannel < ApplicationCable::Channel, método subscribed com duas linhas: (1) reject unless current_client e (2) stream_for current_client. Sem método unsubscribed nem lógica adicional — simétrico ao AdminNotificationsChannel.

    Em app/views/layouts/client.html.erb, inserir imediatamente após a tag de abertura body (linha 14, após "body class='min-h-screen bg-white flex flex-col'") duas novas linhas:
    (1) turbo_stream_from @client, channel: ClientCalendarChannel com guard if @client (erb tag)
    (2) div com id="client-toast-region" class="fixed bottom-4 right-4 z-50 flex flex-col gap-2 items-end" (div vazia, mesmo padrão do admin)

    Usar o guard "if @client" exatamente como o admin usa "if Current.user" — defesa em profundidade per D-08.
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && rails test test/channels/client_calendar_channel_test.rb</automated>
    Esperado: 2 runs, 0 failures, 0 errors.
  </verify>
  <done>
    app/channels/client_calendar_channel.rb existe e passa os 2 testes do canal.
    layouts/client.html.erb contém turbo_stream_from e client-toast-region imediatamente após body tag.
  </done>
  <acceptance_criteria>
    - app/channels/client_calendar_channel.rb contém "class ClientCalendarChannel < ApplicationCable::Channel"
    - app/channels/client_calendar_channel.rb contém "reject unless current_client"
    - app/channels/client_calendar_channel.rb contém "stream_for current_client"
    - app/views/layouts/client.html.erb contém "turbo_stream_from @client, channel: ClientCalendarChannel if @client"
    - app/views/layouts/client.html.erb contém 'id="client-toast-region"'
    - app/views/layouts/client.html.erb contém "fixed bottom-4 right-4 z-50"
    - rails test test/channels/client_calendar_channel_test.rb retorna "2 runs, 0 failures"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Partials do cliente com DOM IDs estáveis + refatoração das views</name>
  <files>
    app/views/client/home/_arte_calendar_chip.html.erb,
    app/views/client/home/_calendar_summary.html.erb,
    app/views/client/shared/_arte_revised_toast.html.erb,
    app/views/client/home/index.html.erb,
    app/views/client/home/_month_calendar.html.erb
  </files>
  <read_first>
    - app/views/client/home/_month_calendar.html.erb — linhas 32-38 — loop link_to que será extraído para _arte_calendar_chip
    - app/views/client/home/index.html.erb — linhas 25-40 — div de resumo que será extraída para _calendar_summary
    - app/views/admin/shared/_approval_toast.html.erb — template estrutural do toast a adaptar para _arte_revised_toast
    - app/views/client/shared/_arte_status_badge.html.erb — confirmar que aceita locals arte: e compact: true
    - app/views/client/shared/_platform_icon.html.erb — confirmar signature (arte:, size:)
    - .planning/phases/19-client-real-time-arte-status-broadcast/19-PATTERNS.md — padrões exatos dos 3 partials novos
  </read_first>
  <action>
    NOVO: app/views/client/home/_arte_calendar_chip.html.erb
    Locals: arte, client. Conteúdo: link_to client_arte_path(token: client.access_token, id: arte) com id: dom_id(arte, "calendar_chip") e class "mt-1 flex items-center gap-1 p-1 rounded bg-gray-50 hover:bg-orange-50 transition-colors". Dentro do bloco: render "client/shared/platform_icon" com arte: arte, size: 14 e render "client/shared/arte_status_badge" com arte: arte, compact: true. Idêntico ao link_to extraído de _month_calendar exceto pelo id: adicionado.

    NOVO: app/views/client/home/_calendar_summary.html.erb
    Locals: summary (Hash). ATENÇÃO ao Pitfall 3 do RESEARCH.md: o id="calendar-summary" deve ficar na div EXTERNA, sempre presente no DOM. Usar class com "hidden" quando summary[:total] == 0 em vez de omitir o elemento. Estrutura: div id="calendar-summary" role="status" aria-label="Resumo do mês" class="flex flex-wrap gap-2 mb-4 justify-center <%= 'hidden' if summary[:total] == 0 %>". Dentro: 4 spans (total bg-slate-100/text-slate-700, aprovadas bg-green-100/text-green-800, pendentes bg-amber-100/text-amber-800, pediu alteração bg-red-100/text-red-800) com valores de summary[:total], summary[:approved], summary[:pending], summary[:change_requested].

    NOVO: app/views/client/shared/_arte_revised_toast.html.erb
    Locals: arte, client. Estrutura idêntica ao _approval_toast.html.erb do admin: div data-controller="toast" class="bg-white border border-gray-200 shadow-lg rounded-lg px-4 py-3 flex items-start gap-3 w-80". Conteúdo: div flex-1 com p "Arte revisada" (text-sm font-semibold text-slate-900) e p com arte.title.presence || arte.platform.humanize + middot + l(arte.scheduled_on, format: :short) (text-xs text-slate-500 truncate). Link "Ver arte" apontando para client_arte_path(token: client.access_token, id: arte) class "text-xs text-[#0F7949] font-medium hover:underline shrink-0". Botão data-action="click->toast#dismiss" class "text-slate-400 hover:text-slate-600 shrink-0 leading-none" aria-label="Fechar notificação" com &times;.

    MODIFICAR app/views/client/home/index.html.erb:
    Substituir o bloco completo das linhas 25-40 (if @summary[:total] > 0 ... end + div role="status") por uma única linha: render "client/home/calendar_summary", summary: @summary

    MODIFICAR app/views/client/home/_month_calendar.html.erb:
    Substituir o loop interno nas linhas 32-38 (artes_do_dia.each do |arte| ... link_to ... end) por: artes_do_dia.each do |arte| seguido de render "client/home/arte_calendar_chip", arte: arte, client: client e end.
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && rails test test/models/arte_test.rb -n "/platform default/" && rails test test/models/arte_test.rb -n "/arte com external_url/"</automated>
    Testes existentes do arte_test.rb não devem quebrar.
    <automated>cd /home/bot/calendario_livia && grep -n "calendar_summary" app/views/client/home/index.html.erb</automated>
    Esperado: linha com render "client/home/calendar_summary".
    <automated>cd /home/bot/calendario_livia && grep -n "arte_calendar_chip" app/views/client/home/_month_calendar.html.erb</automated>
    Esperado: linha com render "client/home/arte_calendar_chip".
    <automated>cd /home/bot/calendario_livia && grep -n "calendar-summary" app/views/client/home/_calendar_summary.html.erb</automated>
    Esperado: linha com id="calendar-summary" fora de condicional Ruby.
  </verify>
  <done>
    3 partials novos criados com estrutura correta.
    index.html.erb e _month_calendar.html.erb usam renders dos novos partials.
    Testes existentes de modelo continuam passando.
    Testes RED de broadcast continuam falhando (callback ainda não existe — correto).
  </done>
  <acceptance_criteria>
    - app/views/client/home/_arte_calendar_chip.html.erb existe e contém 'dom_id(arte, "calendar_chip")'
    - app/views/client/home/_arte_calendar_chip.html.erb contém 'render "client/shared/arte_status_badge"'
    - app/views/client/home/_calendar_summary.html.erb existe e contém 'id="calendar-summary"'
    - app/views/client/home/_calendar_summary.html.erb contém "'hidden' if summary[:total] == 0" (hidden CSS, não condicional que omite o elemento)
    - app/views/client/shared/_arte_revised_toast.html.erb existe e contém 'data-controller="toast"'
    - app/views/client/shared/_arte_revised_toast.html.erb contém 'data-action="click->toast#dismiss"'
    - app/views/client/shared/_arte_revised_toast.html.erb contém 'Arte revisada'
    - app/views/client/shared/_arte_revised_toast.html.erb contém 'client_arte_path(token: client.access_token, id: arte)'
    - app/views/client/home/index.html.erb contém 'render "client/home/calendar_summary"' e NÃO contém 'role="status" aria-label="Resumo do mês"' inline
    - app/views/client/home/_month_calendar.html.erb contém 'render "client/home/arte_calendar_chip"' e NÃO contém link_to inline dentro do loop artes_do_dia
    - rails test retorna 0 failures nos testes pré-existentes (arte_test.rb testes originais)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token URL → ActionCable connection | access_token do cliente viaja como params[:token] no handshake WebSocket |
| ClientCalendarChannel → client streams | Canal scoped por current_client — impede cliente X de receber streams de cliente Y |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Spoofing | ClientCalendarChannel#subscribed | mitigate | `reject unless current_client` — rejeita conexão sem cliente autenticado; `stream_for current_client` — scope automático por record impede cross-client leakage |
| T-19-02 | Information Disclosure | turbo_stream_from no layout | mitigate | Guard `if @client` — não emite tag de subscrição quando @client é nil; sem exposição de canal sem autenticação |
| T-19-03 | Information Disclosure | _arte_revised_toast.html.erb | accept | Toast renderiza apenas title/platform/date da própria arte do cliente autenticado; sem dados sensíveis de outros clientes |
| T-19-04 | Tampering | dom_id(arte, "calendar_chip") target | accept | Target é gerado server-side; cliente não controla o target do Turbo Stream replace |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote externo — Package Legitimacy Gate não aplicável |
</threat_model>

<verification>
Após completar as 3 tarefas:

1. Canal testado: `rails test test/channels/client_calendar_channel_test.rb` → 2 runs, 0 failures
2. Partials existem: `ls app/views/client/home/_arte_calendar_chip.html.erb app/views/client/home/_calendar_summary.html.erb app/views/client/shared/_arte_revised_toast.html.erb`
3. Layout atualizado: `grep -n "client-toast-region\|ClientCalendarChannel" app/views/layouts/client.html.erb` → 2 matches
4. Views refatoradas: `grep -c "arte_calendar_chip" app/views/client/home/_month_calendar.html.erb` → >= 1; `grep -c "calendar_summary" app/views/client/home/index.html.erb` → >= 1
5. Testes RED confirmados: `rails test test/models/arte_test.rb` → os 3 novos testes de broadcast falham (broadcasts_revised_to_all não existe ainda)
6. Suite não quebrada: `rails test` → apenas os 3 novos testes de broadcast falham, todos os outros passam
</verification>

<success_criteria>
- ClientCalendarChannel subscreve/rejeita corretamente (2 testes verdes)
- Layout do cliente tem turbo_stream_from @client com guard if @client
- Layout do cliente tem div#client-toast-region com posicionamento fixed bottom-4 right-4
- Partial _arte_calendar_chip.html.erb tem id DOM estável via dom_id(arte, "calendar_chip")
- Partial _calendar_summary.html.erb tem id="calendar-summary" sempre no DOM (hidden via CSS quando total==0)
- Partial _arte_revised_toast.html.erb tem estrutura toast com auto-dismiss via toast_controller existente
- index.html.erb e _month_calendar.html.erb delegam para os novos partials
- 3 testes RED de broadcast escritos e confirmados falhando (esperando 19-01)
</success_criteria>

<output>
Criar `.planning/phases/19-client-real-time-arte-status-broadcast/19-00-SUMMARY.md` quando concluído.
</output>
