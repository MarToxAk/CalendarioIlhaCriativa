---
phase: 06-admin-feedback-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/controllers/admin/artes_controller.rb
  - app/views/admin/artes/show.html.erb
autonomous: true
requirements:
  - PAIN-05

must_haves:
  truths:
    - "Admin abre uma arte com status change_requested ou revised, vê o card 'Resposta interna ao comentário' e pode preencher e salvar uma nota"
    - "Após salvar, a nota persiste no banco e é exibida novamente no campo ao recarregar a página"
    - "O card de resposta interna NÃO aparece para artes com status pending ou approved"
    - "O campo admin_reply não é visível no portal do cliente"
  artifacts:
    - path: "app/controllers/admin/artes_controller.rb"
      provides: "arte_params expandido com :admin_reply no permit; action update retorna notice específico"
      contains: ":admin_reply"
    - path: "app/views/admin/artes/show.html.erb"
      provides: "Card de resposta interna com textarea e botão Salvar resposta"
      contains: "admin_reply"
  key_links:
    - from: "app/views/admin/artes/show.html.erb"
      to: "app/controllers/admin/artes_controller.rb#update"
      via: "form_with model: [:admin, @arte], method: :patch"
      pattern: "form_with.*:admin.*@arte.*patch"
---

<objective>
Este plano entrega a capacidade de o admin escrever uma resposta interna ao comentário do cliente na página de detalhe da arte (PAIN-05): adicionar :admin_reply ao strong parameters do ArtesController e adicionar o card de formulário na view show da arte.

É uma fatia vertical completa: strong params → PATCH update → textarea na view → admin preenche, salva e vê a nota persistida.

Purpose: Dar ao admin uma nota de trabalho interna associada ao pedido de alteração do cliente.
Output: arte_params com :admin_reply + card de formulário em artes/show para artes change_requested ou revised.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-feedback-panel/06-CONTEXT.md
@.planning/phases/06-admin-feedback-panel/06-RESEARCH.md
@.planning/phases/06-admin-feedback-panel/06-PATTERNS.md
@.planning/phases/06-admin-feedback-panel/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expandir arte_params com :admin_reply e fazer test_update_admin_reply passar</name>
  <files>
    app/controllers/admin/artes_controller.rb
  </files>
  <read_first>
    - app/controllers/admin/artes_controller.rb — linha 66-68: arte_params atual com permit; linha 34-40: action update existente com redirect_to e notice "Arte atualizada com sucesso."
    - test/controllers/admin/artes_controller_test.rb — localizar o teste test "update_admin_reply" criado no Plano 01; confirmar os params e asserts esperados
    - db/schema.rb — confirmar que coluna admin_reply existe na tabela artes após migração do Plano 01
  </read_first>
  <behavior>
    - PATCH /admin/artes/:id com params arte.admin_reply: "Nota do admin" persiste "Nota do admin" em arte.admin_reply
    - arte.reload.admin_reply retorna "Nota do admin" após o update
    - update com admin_reply vazio não apaga o valor existente (comportamento padrão de update — string vazia sobrescreve; nil não sobrescreve)
  </behavior>
  <action>
    Em app/controllers/admin/artes_controller.rb localizar a linha com def arte_params e o params.require(:arte).permit(...). Adicionar :admin_reply ao final da lista de campos permitidos, ficando: params.require(:arte).permit(:title, :caption, :scheduled_on, :approval_deadline, :external_url, :platform, :media_type, :client_id, :media_file, :admin_reply).

    A action update existente (linhas 34-40) já usa arte_params e já redireciona com notice — nenhuma mudança adicional na action update. O teste test_update_admin_reply criado no Plano 01 deve passar agora com esta única alteração.

    Não alterar nenhum outro método do controller.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/artes_controller_test.rb -n /update_admin_reply/</automated>
  </verify>
  <acceptance_criteria>
    - app/controllers/admin/artes_controller.rb linha do arte_params contém :admin_reply
    - bin/rails test test/controllers/admin/artes_controller_test.rb -n /update_admin_reply/ retorna 1 tests, 0 failures, 0 errors
    - PATCH /admin/artes/:id com params: { arte: { admin_reply: "Texto" } } redireciona para admin_arte_path (assert_redirected_to)
    - @arte.reload.admin_reply == "Texto" após o PATCH
  </acceptance_criteria>
  <done>:admin_reply no permit; teste test_update_admin_reply passa</done>
</task>

<task type="auto">
  <name>Task 2: Card de resposta interna na show da arte (PAIN-05)</name>
  <files>
    app/views/admin/artes/show.html.erb
  </files>
  <read_first>
    - app/views/admin/artes/show.html.erb — estrutura completa atual: card principal com informações da arte (linhas 1-28); bloco div.flex.gap-2 com botões de ações; link "Voltar"
    - .planning/phases/06-admin-feedback-panel/06-PATTERNS.md — seção "app/views/admin/artes/show.html.erb": bloco a adicionar (formulário admin_reply), nota sobre card separado do bloco de ações, nota sobre show apenas quando change_requested? ou revised?
    - app/views/admin/clients/show.html.erb — padrão de card (bg-white rounded-xl border border-gray-200 shadow-card p-6 max-w-2xl mt-4) para manter consistência visual
  </read_first>
  <action>
    Adicionar ao final de app/views/admin/artes/show.html.erb, após o fechamento da div do card principal (linha 28, após o </div> final), um segundo card separado — NUNCA dentro do card principal nem dentro do div.flex.gap-2.

    O novo card deve ser renderizado condicionalmente apenas quando @arte.change_requested? || @arte.revised? — artes pendentes ou aprovadas não precisam de resposta interna.

    Estrutura do card: div com classes bg-white rounded-xl border border-gray-200 shadow-card p-6 max-w-2xl mt-6. Dentro: h3 com "Resposta interna ao comentário" e classes text-sm font-semibold text-slate-900 border-b border-gray-100 pb-3 mb-4. Depois, form_with model: [:admin, @arte], method: :patch. Dentro do form: f.text_area :admin_reply com value: @arte.admin_reply, rows: 4, placeholder: "Escreva uma nota interna sobre o pedido de alteração do cliente...", class: "block w-full px-3 py-2 border border-gray-200 rounded-lg text-sm text-slate-700 focus:outline-none focus:ring-2 focus:ring-[#0F7949]/20 focus:border-[#0F7949]". Abaixo do textarea: div.mt-3 com f.submit "Salvar resposta" e class "h-9 px-4 bg-[#0F7949] hover:bg-[#0a5c37] text-white text-sm font-semibold rounded-lg transition-colors cursor-pointer".

    Nota crítica: o formulário de admin_reply é um form separado — não aninhar dentro do form existente nem dentro do div.flex.gap-2 de ações (Pitfall "Dois formulários PATCH aninhados" do RESEARCH.md).

    Se @arte.admin_reply.present?, exibir o valor salvo no textarea (o value: @arte.admin_reply garante isso).
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/artes_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/views/admin/artes/show.html.erb contém if @arte.change_requested? || @arte.revised?
    - app/views/admin/artes/show.html.erb contém form_with model: [:admin, @arte], method: :patch
    - app/views/admin/artes/show.html.erb contém f.text_area :admin_reply
    - app/views/admin/artes/show.html.erb contém "Resposta interna ao comentário"
    - app/views/admin/artes/show.html.erb NÃO contém o formulário de admin_reply dentro de outro form
    - bin/rails test test/controllers/admin/artes_controller_test.rb retorna 0 failures, 0 errors (todos os testes existentes continuam passando)
    - GET /admin/artes/:id de arte com status change_requested: page body contém "Resposta interna ao comentário"
    - GET /admin/artes/:id de arte com status pending: page body NÃO contém "Resposta interna ao comentário"
  </acceptance_criteria>
  <done>Card de resposta interna visível na show de artes change_requested/revised; formulário funcional; todos os testes do artes_controller passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→PATCH /admin/artes/:id | Admin envia texto livre no campo admin_reply via form PATCH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | XSS via admin_reply renderizado em textarea/show | mitigate | Rails escapa HTML por padrão em <%= %>; campo value: @arte.admin_reply em f.text_area usa escape automático do form helper; proibido usar raw() ou html_safe com admin_reply |
| T-06-04 | Tampering | Mass assignment de campos não autorizados via arte_params | mitigate | Strong Parameters — apenas :admin_reply adicionado explicitamente ao permit; nenhum outro campo novo |
| T-06-05 | Information Disclosure | admin_reply visível no portal do cliente | accept | Campo admin_reply está ausente das views client/ — não há render deste campo em nenhuma view do portal (D-10 per CONTEXT.md); verificar que nenhuma view client/ referencia admin_reply |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala pacotes externos — N/A |
</threat_model>

<verification>
Verificação geral do Plano 02:

1. bin/rails test test/controllers/admin/artes_controller_test.rb — 0 failures, 0 errors
2. GET /admin/artes/:id (arte com change_requested) via browser: card "Resposta interna ao comentário" visível com textarea e botão "Salvar resposta"
3. GET /admin/artes/:id (arte com pending) via browser: card de resposta interna NÃO aparece
4. PATCH /admin/artes/:id com admin_reply: "Teste" via browser: redireciona para a mesma página, recarregar mostra "Teste" no campo
5. grep -n "admin_reply" app/views/client/ — retorna 0 resultados (campo não exposto ao cliente)
</verification>

<success_criteria>
- Admin preenche "Resposta interna ao comentário" em arte com pedido de alteração e clica "Salvar resposta" — a resposta é salva e exibida ao recarregar (PAIN-05)
- Artes pending/approved não exibem o card de resposta interna
- O portal do cliente não exibe admin_reply em nenhuma circunstância (D-10)
- bin/rails test test/controllers/admin/artes_controller_test.rb: 0 failures, 0 errors
</success_criteria>

<output>
Criar `.planning/phases/06-admin-feedback-panel/06-02-SUMMARY.md` quando concluído.
</output>
