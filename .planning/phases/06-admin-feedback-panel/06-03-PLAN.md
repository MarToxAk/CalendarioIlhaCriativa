---
phase: 06-admin-feedback-panel
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/controllers/admin/clients_controller.rb
  - app/views/admin/clients/show.html.erb
autonomous: true
requirements:
  - CLIE-05

must_haves:
  truths:
    - "Admin acessa a página de um cliente e vê um card 'Histórico de aprovações' com todas as artes que receberam pelo menos uma resposta do cliente"
    - "Cada item do histórico mostra: título da arte, data agendada no formato DD/MM/AAAA, badge de status atual e link para a página da arte"
    - "Se o cliente tiver artes com comentário de pedido de alteração, o comentário aparece em itálico abaixo do título"
    - "Se o cliente não tiver nenhuma arte respondida, o card de histórico não aparece (seção condicional)"
    - "Artes com múltiplas respostas aparecem apenas uma vez na lista (sem duplicatas)"
  artifacts:
    - path: "app/controllers/admin/clients_controller.rb"
      provides: "action show com @artes_with_responses carregadas com joins + distinct"
      contains: "@artes_with_responses"
    - path: "app/views/admin/clients/show.html.erb"
      provides: "Terceiro card de histórico de aprovações"
      contains: "Histórico de aprovações"
  key_links:
    - from: "app/views/admin/clients/show.html.erb"
      to: "app/controllers/admin/clients_controller.rb"
      via: "@artes_with_responses renderizado no terceiro card"
      pattern: "@artes_with_responses"
    - from: "app/controllers/admin/clients_controller.rb"
      to: "approval_responses table via joins"
      via: "@client.artes.joins(:approval_responses).includes(:approval_responses).distinct"
      pattern: "joins.*approval_responses.*distinct"
---

<objective>
Este plano entrega o histórico completo de aprovações de um cliente específico na página admin/clients/show (CLIE-05): expandir ClientsController#show para carregar @artes_with_responses e adicionar o terceiro card de histórico na view.

É uma fatia vertical completa: query no controller → card na view → admin acessa a página do cliente e vê o histórico de artes respondidas com status, comentários e links.

Purpose: Dar ao admin visibilidade do histórico de aprovações de um cliente específico sem rota nova.
Output: ClientsController#show com @artes_with_responses + terceiro card em clients/show.html.erb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-feedback-panel/06-CONTEXT.md
@.planning/phases/06-admin-feedback-panel/06-RESEARCH.md
@.planning/phases/06-admin-feedback-panel/06-PATTERNS.md
@.planning/phases/06-admin-feedback-panel/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expandir ClientsController#show com @artes_with_responses e fazer test_show_inclui_historico passar</name>
  <files>
    app/controllers/admin/clients_controller.rb
  </files>
  <read_first>
    - app/controllers/admin/clients_controller.rb — action show atual (linhas 8-9, corpo vazio); before_action :set_client (linhas 55-57) que define @client = Client.find(params[:id]); estrutura completa do controller
    - app/models/arte.rb — confirmar que has_many :approval_responses existe; confirmar nomes dos campos (scheduled_on, status, title)
    - app/models/approval_response.rb — confirmar campos: decision (enum), comment, responded_at, arte_id
    - test/controllers/admin/clients_controller_test.rb — localizar o teste test "show_inclui_historico" criado no Plano 01; confirmar setup e asserts esperados; confirmar padrão de criação de Arte + ApprovalResponse inline
    - .planning/phases/06-admin-feedback-panel/06-PATTERNS.md — seção "app/controllers/admin/clients_controller.rb": query com joins + includes + distinct + order(scheduled_on: :desc)
    - .planning/phases/06-admin-feedback-panel/06-RESEARCH.md — Pitfall 5: joins sem distinct retorna linha por ApprovalResponse (artes duplicadas)
  </read_first>
  <behavior>
    - @artes_with_responses = @client.artes.joins(:approval_responses).includes(:approval_responses).distinct.order(scheduled_on: :desc)
    - Arte com múltiplas ApprovalResponses aparece UMA única vez (distinct)
    - Arte sem nenhuma ApprovalResponse NÃO aparece na lista
    - Resultados ordenados por scheduled_on decrescente (mais recente primeiro)
  </behavior>
  <action>
    Em app/controllers/admin/clients_controller.rb expandir a action show de corpo vazio para:

    def show
      @artes_with_responses = @client.artes
                                      .joins(:approval_responses)
                                      .includes(:approval_responses)
                                      .distinct
                                      .order(scheduled_on: :desc)
    end

    O @client já é definido pelo before_action :set_client — usar @client.artes em vez de Arte.where(client_id: ...) para respeitar o escopo de associação (padrão de segurança do projeto).

    O .distinct após .joins(:approval_responses) é obrigatório — sem ele artes com N respostas aparecem N vezes (Pitfall 5 do RESEARCH.md).

    O .includes(:approval_responses) junto com .joins(:approval_responses) pode parecer redundante mas garante eager loading ao renderizar comentários na view — evita N+1.

    Após esta mudança o teste test_show_inclui_historico do Plano 01 deve passar (o teste verifica apenas assert_response :success, que já passava — mas agora @artes_with_responses está disponível para a view).

    Atualizar o teste test_show_inclui_historico para uma asserção mais forte: criar uma Arte para @client + uma ApprovalResponse para essa arte, fazer GET admin_client_url(@client), e verificar que a response.body contém o título da arte. Isso garante que a view renderiza o card com dados reais.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/clients_controller_test.rb -n /show_inclui_historico/</automated>
  </verify>
  <acceptance_criteria>
    - app/controllers/admin/clients_controller.rb action show contém @artes_with_responses = @client.artes
    - app/controllers/admin/clients_controller.rb contém .joins(:approval_responses)
    - app/controllers/admin/clients_controller.rb contém .distinct
    - app/controllers/admin/clients_controller.rb contém .order(scheduled_on: :desc)
    - bin/rails test test/controllers/admin/clients_controller_test.rb -n /show_inclui_historico/ retorna 1 tests, 0 failures, 0 errors
  </acceptance_criteria>
  <done>ClientsController#show carrega @artes_with_responses com distinct; teste show_inclui_historico passa</done>
</task>

<task type="auto">
  <name>Task 2: Terceiro card de histórico em admin/clients/show.html.erb (CLIE-05)</name>
  <files>
    app/views/admin/clients/show.html.erb
  </files>
  <read_first>
    - app/views/admin/clients/show.html.erb — estrutura completa atual: dois cards existentes (Dados de acesso do portal, Informações); o terceiro card deve ser adicionado APÓS o último </div> (linha 132); padrão exato de card: div bg-white rounded-xl border border-gray-200 shadow-card p-6 max-w-2xl mt-4
    - app/views/client/shared/_arte_status_badge.html.erb — confirmar assinatura: render "client/shared/arte_status_badge", arte: arte, compact: true
    - .planning/phases/06-admin-feedback-panel/06-PATTERNS.md — seção "app/views/admin/clients/show.html.erb": terceiro card a adicionar com template completo incluindo iteração, título, data, badge, comentário, link
    - config/routes.rb — confirmar rota admin_arte_path (resources :artes no namespace admin)
  </read_first>
  <action>
    Adicionar ao final de app/views/admin/clients/show.html.erb, após o fechamento do segundo card "Informações" (após o </div> da linha 132), o terceiro card de histórico.

    O card deve ser envolvido em condição: se @artes_with_responses.any? renderizar o card; caso contrário nada (sem card vazio).

    Estrutura do card: div com classes bg-white rounded-xl border border-gray-200 shadow-card p-6 max-w-2xl mt-4. Dentro: h2 com "Histórico de aprovações" e classes text-sm font-semibold text-slate-900 border-b border-gray-100 pb-3 mb-4.

    Abaixo do h2: div.space-y-3 com iteração @artes_with_responses.each do |arte|. Para cada arte: div com flex items-center justify-between py-2 border-b border-gray-50 last:border-0. Lado esquerdo (div.flex-1.min-w-0): p com arte.title.presence || "Arte sem título" (text-sm font-medium text-slate-900 truncate); p com arte.scheduled_on.strftime("%d/%m/%Y") (text-xs text-slate-500 mt-0.5). Para mostrar o comentário do último pedido de alteração: localvar last_response = arte.approval_responses.sort_by(&:created_at).last — se last_response&.comment.present? exibir p com o comentário em itálico (text-xs text-slate-600 mt-1 italic) envolvido em aspas. Lado direito (div.ml-4.flex.items-center.gap-3.shrink-0): render "client/shared/arte_status_badge", arte: arte, compact: true; link_to "Ver", admin_arte_path(arte) com text-xs text-[#0F7949] hover:underline.

    Nota crítica: usar arte.approval_responses.sort_by(&:created_at).last (não .first) para pegar o comentário mais recente — approval_responses está eagerly loaded via includes. Se usar .order() em associação já loaded, Rails recarregará com nova query. Usar sort_by em Ruby sobre o array já carregado.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/clients_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/views/admin/clients/show.html.erb contém "Histórico de aprovações"
    - app/views/admin/clients/show.html.erb contém @artes_with_responses.any?
    - app/views/admin/clients/show.html.erb contém @artes_with_responses.each do |arte|
    - app/views/admin/clients/show.html.erb contém render "client/shared/arte_status_badge", arte: arte, compact: true
    - app/views/admin/clients/show.html.erb contém admin_arte_path(arte)
    - app/views/admin/clients/show.html.erb contém arte.scheduled_on.strftime
    - bin/rails test test/controllers/admin/clients_controller_test.rb retorna 0 failures, 0 errors
    - GET /admin/clients/:id de cliente com artes respondidas: page body contém "Histórico de aprovações" e o título da arte
    - GET /admin/clients/:id de cliente sem artes respondidas: page body NÃO contém "Histórico de aprovações" (condicional @artes_with_responses.any?)
  </acceptance_criteria>
  <done>Terceiro card de histórico de aprovações visível em admin/clients/show para clientes com artes respondidas; todos os testes de clients_controller passando</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→GET /admin/clients/:id | Admin autenticado acessa histórico de um cliente específico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Spoofing | GET /admin/clients/:id sem autenticação | mitigate | Admin::BaseController herda require_authentication — request sem sessão redireciona para /session/new; já coberto pelo before_action existente |
| T-06-07 | Information Disclosure | @client.artes sem escopo adequado | mitigate | Query usa @client.artes (scoped por associação) — nunca Arte.where(client_id: params[:id]); @client é definido pelo before_action :set_client que usa Client.find(params[:id]) sem escopo de usuário, o que é correto porque admin tem acesso total |
| T-06-08 | Tampering | XSS via arte.title ou approval_response.comment renderizados | mitigate | Rails escapa HTML por padrão em <%= %>; campos de texto do cliente (comment) são escapados automaticamente; não usar raw() ou html_safe com dados de usuário |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala pacotes externos — N/A |
</threat_model>

<verification>
Verificação geral do Plano 03:

1. bin/rails test test/controllers/admin/clients_controller_test.rb — 0 failures, 0 errors
2. GET /admin/clients/:id (cliente com artes respondidas) via browser: card "Histórico de aprovações" visível com itens mostrando título, data, badge de status e link "Ver"
3. GET /admin/clients/:id (cliente sem artes respondidas) via browser: card de histórico NÃO aparece
4. Arte com múltiplas ApprovalResponses aparece UMA vez na lista (testar criando 2 respostas para a mesma arte)
5. Comentário de pedido de alteração aparece em itálico abaixo do título
</verification>

<success_criteria>
- Admin abre a página de um cliente e vê o card "Histórico de aprovações" com todas as artes respondidas (CLIE-05)
- Cada item mostra: título, data DD/MM/AAAA, badge de status colorido, link "Ver" para admin_arte_path
- Comentários de pedido de alteração exibidos em itálico
- Artes sem nenhuma resposta não aparecem no histórico
- Clientes sem artes respondidas não exibem o card
- bin/rails test test/controllers/admin/clients_controller_test.rb: 0 failures, 0 errors
</success_criteria>

<output>
Criar `.planning/phases/06-admin-feedback-panel/06-03-SUMMARY.md` quando concluído.
</output>
