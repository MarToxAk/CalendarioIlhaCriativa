---
phase: 06-admin-feedback-panel
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/controllers/admin/artes_controller.rb
  - app/controllers/admin/dashboard_controller.rb
  - test/controllers/admin/artes_controller_test.rb
autonomous: true
gap_closure: true
requirements:
  - PAIN-05

must_haves:
  truths:
    - "Admin com arte change_requested preenche o card 'Resposta interna ao comentário' e salva — o PATCH não é bloqueado pelo check_editable"
    - "GET /admin?status=VALOR_INVALIDO retorna lista vazia sem crash e sem comportamento silencioso para valores não reconhecidos"
    - "test_update_admin_reply usa arte com status change_requested e verifica @arte.reload.admin_reply"
  artifacts:
    - path: "app/controllers/admin/artes_controller.rb"
      provides: "check_editable aceita pending?, revised? e change_requested?"
      contains: "change_requested?"
    - path: "app/controllers/admin/dashboard_controller.rb"
      provides: "whitelist de status antes do where"
      contains: "Arte.statuses.keys"
    - path: "test/controllers/admin/artes_controller_test.rb"
      provides: "teste update_admin_reply cobrindo o caso real de uso (change_requested)"
      contains: "change_requested"
---

<objective>
Fechar dois gaps identificados na verificação da fase 06:

Gap 1 (BLOCKER — PAIN-05): check_editable em artes_controller.rb bloqueia PATCH para artes com status change_requested, que é exatamente o status em que o formulário de admin_reply é exibido. A guarda atualmente permite somente pending? e revised?. O teste test_update_admin_reply usa status :pending (que nunca exibe o formulário na UI), mascarando o bug.

Gap 2 (WARNING — PAIN-03 parcial): dashboard_controller.rb passa params[:status] direto para where(status:) sem whitelist. No Rails 8.1, valores inválidos resultam em WHERE status IS NULL silencioso — lista vazia sem feedback ao admin.

Ambas as correções são cirúrgicas (1-3 linhas cada) e pertencem ao mesmo plano.

Purpose: Tornar o fluxo primário de PAIN-05 operacional e tornar o filtro de status robusto contra valores inválidos.
Output: check_editable corrigido + whitelist no dashboard + teste cobrindo o caso de uso real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-feedback-panel/06-VERIFICATION.md
@.planning/phases/06-admin-feedback-panel/06-02-PLAN.md
@.planning/phases/06-admin-feedback-panel/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir check_editable, whitelist de status no dashboard e teste update_admin_reply</name>
  <files>
    app/controllers/admin/artes_controller.rb,
    app/controllers/admin/dashboard_controller.rb,
    test/controllers/admin/artes_controller_test.rb
  </files>
  <read_first>
    - app/controllers/admin/artes_controller.rb linha 70-74: método check_editable atual — `unless @arte.pending? || @arte.revised?`
    - app/controllers/admin/dashboard_controller.rb linha 8: `scope = scope.where(status: params[:status]) if params[:status].present?` sem whitelist
    - test/controllers/admin/artes_controller_test.rb linhas 90-94: test "update_admin_reply persiste campo" — @arte criado no setup com status :pending; confirmar que nenhum outro teste usa :pending para cobrir change_requested
  </read_first>
  <behavior>
    - PATCH /admin/artes/:id para arte com status change_requested retorna redirect (302) em vez de redirect com alert "Edição bloqueada"
    - @arte.reload.admin_reply == "Nota interna do admin" após PATCH com params: { arte: { admin_reply: "Nota interna do admin" } } e arte com status change_requested
    - GET /admin?status=xyz não causa erro 500 e não exibe lista vazia silenciosa para valores inválidos — o where só é aplicado quando params[:status] é um valor reconhecido por Arte.statuses.keys
    - GET /admin?status=pending continua funcionando normalmente (filtro válido ainda aplicado)
    - test_update_admin_reply falha no RED com arte :pending bloqueada pelo check_editable e passa no GREEN com arte :change_requested
  </behavior>
  <action>
    Aplicar três alterações cirúrgicas:

    1. Em app/controllers/admin/artes_controller.rb, no método check_editable (linha 71), alterar a condição guard de:
       `unless @arte.pending? || @arte.revised?`
       para:
       `unless @arte.pending? || @arte.revised? || @arte.change_requested?`
    O método completo fica: redirect_to admin_arte_path(@arte), alert: "Edição bloqueada: só é possível editar artes pendentes, revisadas ou com pedido de alteração." Ajustar a mensagem do alert para refletir os três status agora permitidos.

    2. Em app/controllers/admin/dashboard_controller.rb, substituir a linha 8:
       `scope = scope.where(status: params[:status]) if params[:status].present?`
       por:
       `scope = scope.where(status: params[:status]) if params[:status].present? && Arte.statuses.keys.include?(params[:status].to_s)`
    Esta única linha adiciona o whitelist — nenhuma outra alteração no controller.

    3. Em test/controllers/admin/artes_controller_test.rb, no teste "update_admin_reply persiste campo" (linhas 90-94), alterar o @arte antes do PATCH para ter status :change_requested. O setup cria @arte com status :pending. Adicionar antes do patch:
       `@arte.update!(status: :change_requested)`
    O restante do teste permanece igual — assert_redirected_to admin_arte_url(@arte) e assert_equal "Nota interna do admin", @arte.reload.admin_reply continuam válidos.
    Não criar novos testes — apenas corrigir o teste existente.

    Não alterar nenhum outro método, rota, view ou model. Estas são correções cirúrgicas de 1-2 linhas cada.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/admin/artes_controller_test.rb test/controllers/admin/dashboard_controller_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/controllers/admin/artes_controller.rb contém `@arte.change_requested?` na guarda check_editable
    - app/controllers/admin/dashboard_controller.rb contém `Arte.statuses.keys.include?(params[:status].to_s)`
    - test/controllers/admin/artes_controller_test.rb no teste update_admin_reply contém `status: :change_requested` (ou `update!(status: :change_requested)`)
    - bin/rails test test/controllers/admin/artes_controller_test.rb: 0 failures, 0 errors
    - bin/rails test test/controllers/admin/dashboard_controller_test.rb: 0 failures, 0 errors
    - PATCH /admin/artes/:id com arte change_requested: retorna 302 redirect para admin_arte_path (não alert de "Edição bloqueada")
    - @arte.reload.admin_reply persiste o valor enviado via PATCH quando arte tem status change_requested
  </acceptance_criteria>
  <done>check_editable permite change_requested?; filtro de status tem whitelist; teste update_admin_reply cobre o caso de uso real; todos os testes admin passam</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→PATCH /admin/artes/:id | Admin envia texto no campo admin_reply; check_editable agora aceita change_requested? |
| browser→GET /admin?status=X | params[:status] agora validado contra whitelist antes de chegar ao where |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Mass assignment via arte_params | mitigate | Strong Parameters — :admin_reply já no permit do Plano 02; nenhum campo novo adicionado neste plano |
| T-06-05 | Information Disclosure | admin_reply visível no portal do cliente | accept | Nenhuma view client/ referencia admin_reply — verificado no Plano 02 |
| T-06-WL | Tampering | Injeção de enum inválido via params[:status] no dashboard | mitigate | Whitelist Arte.statuses.keys.include? antes do where — strings fora do enum são ignoradas |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote externo instalado neste plano — N/A |
</threat_model>

<verification>
Verificação geral do Plano 04 (gap closure):

1. bin/rails test test/controllers/admin/ — deve retornar 21+ runs, 0 failures, 0 errors
2. grep "change_requested?" app/controllers/admin/artes_controller.rb — deve aparecer na linha do check_editable
3. grep "Arte.statuses.keys" app/controllers/admin/dashboard_controller.rb — deve retornar resultado
4. grep "change_requested" test/controllers/admin/artes_controller_test.rb — deve aparecer dentro do bloco update_admin_reply
5. PATCH /admin/artes/:id com arte change_requested (via teste): retorna 302, não alert de bloqueio
</verification>

<success_criteria>
- check_editable permite pending?, revised? e change_requested? — o fluxo primário de PAIN-05 está operacional
- Filtro de status no dashboard valida contra Arte.statuses.keys antes de aplicar o where
- Teste update_admin_reply cobre o caso de uso real (arte change_requested) com assert_equal no admin_reply persistido
- bin/rails test test/controllers/admin/: 0 failures, 0 errors
</success_criteria>

<output>
Criar `.planning/phases/06-admin-feedback-panel/06-04-SUMMARY.md` quando concluído.
</output>
