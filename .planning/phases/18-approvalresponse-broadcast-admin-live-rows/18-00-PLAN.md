---
phase: 18-approvalresponse-broadcast-admin-live-rows
plan: "00"
type: auto
wave: 1
depends_on: []
files_modified:
  - test/models/approval_response_test.rb
autonomous: true
requirements: [RTUP-01, RTUP-02, RTUP-03, RTUP-04]

must_haves:
  truths:
    - "Testes RED existem para broadcasts_to_admin: falham com NameError/NoMethodError antes da implementação"
    - "Teste verifica que AdminNotificationsChannel.broadcast_to é chamado exatamente 1× após ApprovalResponse.create!"
    - "Teste verifica que change_requested gera 4 Turbo Streams; approved gera 3 (sem badge stream)"
    - "Teste verifica eager-load: Arte.includes(:client) é chamado, não Arte.find"
  artifacts:
    - path: test/models/approval_response_test.rb
      provides: "Testes RED para after_create_commit broadcasts_to_admin"
      contains: "broadcasts_to_admin"
  key_links:
    - from: test/models/approval_response_test.rb
      to: app/models/approval_response.rb
      via: "ActiveSupport::TestCase — stub de AdminNotificationsChannel.broadcast_to"
      pattern: "AdminNotificationsChannel.*stub|stub.*broadcast_to"
---

<objective>
Escrever os testes de modelo que falham (RED) validando o comportamento esperado de
`broadcasts_to_admin` no `ApprovalResponse`. Esses testes guiam a implementação do
Plan 02 e garantem que o broadcast não dispare N+1 queries.

Purpose: Estabelecer contrato testável para o after_create_commit antes de implementá-lo —
evita que Plan 02 entregue uma implementação que não pode ser verificada automaticamente.

Output: `test/models/approval_response_test.rb` com novos testes que falham assertivamente
(não por erro de sintaxe) até que Plan 02 adicione o método `broadcasts_to_admin`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes RED — callback e broadcast call</name>
  <files>test/models/approval_response_test.rb</files>
  <read_first>
    - test/models/approval_response_test.rb (ler COMPLETO — adicionar testes ao final, não sobrescrever)
    - app/models/approval_response.rb (estado atual sem broadcasts_to_admin — confirmar que o método NÃO existe)
    - app/channels/admin_notifications_channel.rb (assinatura de broadcast_to para montar stub correto)
  </read_first>
  <behavior>
    - Test A: `ApprovalResponse` não responde a `broadcasts_to_admin` ANTES de Plan 02 → NameError ao chamar diretamente (falha RED)
    - Test B: `after_create_commit` callbacks de `ApprovalResponse` incluem `:broadcasts_to_admin` → falha RED antes da implementação
    - Test C: `AdminNotificationsChannel.broadcast_to` é chamado exatamente 1 vez quando `ApprovalResponse.create!` com `decision: :change_requested`
    - Test D: `AdminNotificationsChannel.broadcast_to` é chamado exatamente 1 vez quando `decision: :approved`
  </behavior>
  <action>
    Adicionar ao final de `test/models/approval_response_test.rb` os seguintes testes (após os existentes):

    Test A — assert_respond_to privado: usar `assert @arte_pending.respond_to?(:broadcasts_to_admin, true)` — o `true` inclui métodos privados; DEVE falhar RED pois método não existe.

    Test B — assert callbacks registrados: verificar que `ApprovalResponse._commit_callbacks.map(&:filter)` inclui `:broadcasts_to_admin`. Usar `assert_includes ApprovalResponse._commit_callbacks.map(&:filter), :broadcasts_to_admin`.

    Test C e D — stub do canal: usar padrão `AdminNotificationsChannel.stub(:broadcast_to, ->(user, **kwargs) { @broadcast_calls << { user: user, kwargs: kwargs } }) do ... end`. Inicializar `@broadcast_calls = []` no bloco do teste. Chamar `ApprovalResponse.create!(arte: @arte_pending, decision: :change_requested)`. Asserção: `assert_equal 1, @broadcast_calls.length`.

    Para Test D: mesma estrutura, `decision: :approved`, `@arte_pending` deve estar em estado `revised?` (chamar `@arte_pending.revised!` antes de criar o approved response — ver fixtures ou criar arte revisada).

    IMPORTANTE: Não criar `setup` duplicado. Reutilizar `@arte_pending` do `setup` existente para Tests A/B/C. Para Test D, criar arte com `status: :revised` inline no teste.

    Cada teste é um método `test "..."` independente. Nomear:
    - `test "broadcasts_to_admin eh metodo privado definido no model"`
    - `test "after_create_commit registra broadcasts_to_admin"`
    - `test "change_requested cria approval e chama broadcast_to uma vez"`
    - `test "approved cria approval e chama broadcast_to uma vez"`
  </action>
  <verify>
    <automated>bin/rails test test/models/approval_response_test.rb 2>&1 | tail -20</automated>
  </verify>
  <done>
    Os 4 novos testes aparecem na saída do runner. Os testes sobre `broadcasts_to_admin` (Tests A, B) falham com `NoMethodError` ou `assert_includes` failure. Os testes de broadcast (C, D) falham com `NoMethodError` ou assertion failure.
    Os testes EXISTENTES (anteriores) continuam PASSANDO — não há regressão.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Testes RED — streams por decisão e eager-load</name>
  <files>test/models/approval_response_test.rb</files>
  <read_first>
    - test/models/approval_response_test.rb (estado após Tarefa 1 — adicionar mais testes ao final)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md (seção `<specifics>` — padrão do broadcasts_to_admin com streams.compact)
  </read_first>
  <behavior>
    - Test E: Para `change_requested` → kwargs[:turbo_stream] é Array com 4 elementos (toast append + badge replace + dashboard replace + approvals prepend)
    - Test F: Para `approved` → kwargs[:turbo_stream] é Array com 3 elementos (sem badge stream — D-12)
    - Test G: Sem N+1 — ao criar ApprovalResponse, número de queries inclui `Arte.includes(:client)` (uma query com JOIN), não duas queries separadas (Arte.find + client query)
  </behavior>
  <action>
    Adicionar ao final de `test/models/approval_response_test.rb` (após Tarefa 1):

    Test E — count de streams para change_requested:
    Usar mesmo padrão de stub com `@broadcast_calls = []`. Criar `ApprovalResponse.create!` com `decision: :change_requested`. Após o stub, asserção: `assert_equal 4, @broadcast_calls.first[:kwargs][:turbo_stream].length, "change_requested deve gerar 4 streams"`.

    Test F — count de streams para approved:
    `@arte_revised = Arte.create!(client: @client, scheduled_on: Date.current, platform: :instagram, media_type: :image, status: :revised, external_url: "https://drive.google.com/file/x")`.
    Criar response com `decision: :approved`. Asserção: `assert_equal 3, @broadcast_calls.first[:kwargs][:turbo_stream].length, "approved deve gerar 3 streams (sem badge)"`.

    Test G — eager-load sem N+1:
    Capturar queries via `queries = []` e `ActiveSupport::Notifications.subscribed(->(*, sql: **k) { queries << k[:sql] }, "sql.active_record")`.
    Dentro do bloco de subscription, stub do canal + create. Após: `arte_queries = queries.grep(/SELECT.*artes/)`. `client_queries = queries.grep(/SELECT.*clients/)`. Asserção: `assert(arte_queries.any? { |q| q.include?("JOIN") || q.include?("IN") }, "Arte deve ser carregada com includes(:client), não via N+1")`.

    Nomear:
    - `test "change_requested broadcast gera 4 turbo streams"`
    - `test "approved broadcast gera 3 turbo streams sem badge"`
    - `test "broadcasts_to_admin nao dispara N+1 para arte.client"`
  </action>
  <verify>
    <automated>bin/rails test test/models/approval_response_test.rb 2>&1 | grep -E "broadcasts_to_admin|turbo_stream|N+1|Failure|Error|pass|fail" | head -30</automated>
  </verify>
  <done>
    Os 3 novos testes aparecem na saída. Todos falham RED (NoMethodError ou assertion failure) pois `broadcasts_to_admin` não existe ainda.
    Total de testes no arquivo aumentou em 7 (4 da Tarefa 1 + 3 da Tarefa 2).
    Os testes existentes (antes desta fase) continuam PASSANDO.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → model | Testes chamam métodos privados via `respond_to?(:m, true)` — não há risco de exposição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-00-01 | Tampering | test/models/approval_response_test.rb | accept | Arquivo de test — sem superfície de ataque em produção |
| T-18-00-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
- `bin/rails test test/models/approval_response_test.rb` retorna falhas nos 7 novos testes
- Testes pré-existentes continuam verdes
- Nenhum teste usa watch-mode ou flags interativos
- Latência de feedback: &lt; 30s
</verification>

<success_criteria>
7 novos testes RED adicionados ao arquivo. Todos falham de forma assertiva (não por erro de sintaxe). Testes existentes permanecem verdes. O contrato do broadcasts_to_admin está documentado como testes antes de qualquer implementação.
</success_criteria>

<output>
Criar `.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-00-SUMMARY.md` ao concluir.
</output>
