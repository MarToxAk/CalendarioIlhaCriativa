---
phase: 18-approvalresponse-broadcast-admin-live-rows
plan: "02"
type: auto
wave: 2
depends_on: ["18-00", "18-01"]
files_modified:
  - app/views/admin/dashboard/_arte_dashboard_row.html.erb
  - app/views/admin/dashboard/index.html.erb
  - app/models/approval_response.rb
autonomous: true
requirements: [RTUP-01, RTUP-02, RTUP-03, RTUP-04]

must_haves:
  truths:
    - "Criar ApprovalResponse dispara exatamente 1 broadcast via AdminNotificationsChannel.broadcast_to(User.first, ...)"
    - "change_requested gera 4 Turbo Streams: append toast, replace sidebar-badge, replace arte dashboard row, prepend approvals-tbody"
    - "approved gera 3 Turbo Streams: append toast, replace arte dashboard row, prepend approvals-tbody (sem badge — D-12)"
    - "Arte eager-loaded com :client antes do broadcast — sem N+1 para arte.client.name no toast"
    - "Badge count recalculado server-authoritatively via Arte.change_requested.count a cada broadcast"
    - "Dashboard usa partial _arte_dashboard_row com id=dom_id(arte) em cada <tr>"
    - "7 testes RED do Plan 00 ficam VERDES após implementação"
  artifacts:
    - path: app/views/admin/dashboard/_arte_dashboard_row.html.erb
      provides: "Partial da linha da arte no dashboard com id dom_id(arte)"
      contains: "dom_id(arte)"
    - path: app/models/approval_response.rb
      provides: "after_create_commit :broadcasts_to_admin + método privado broadcasts_to_admin"
      contains: "broadcasts_to_admin"
  key_links:
    - from: app/models/approval_response.rb
      to: app/channels/admin_notifications_channel.rb
      via: "AdminNotificationsChannel.broadcast_to(User.first, turbo_stream: [...])"
      pattern: "AdminNotificationsChannel\\.broadcast_to"
    - from: app/models/approval_response.rb
      to: app/views/admin/shared/_approval_toast.html.erb
      via: "turbo_stream.append('admin-toast-region', partial: 'admin/shared/approval_toast', ...)"
      pattern: "admin/shared/approval_toast"
    - from: app/models/approval_response.rb
      to: app/views/admin/dashboard/_arte_dashboard_row.html.erb
      via: "turbo_stream.replace(dom_id(arte_with_client), partial: 'admin/dashboard/arte_dashboard_row', ...)"
      pattern: "arte_dashboard_row"
    - from: app/views/admin/dashboard/index.html.erb
      to: app/views/admin/dashboard/_arte_dashboard_row.html.erb
      via: "render 'admin/dashboard/arte_dashboard_row', arte: arte"
      pattern: "arte_dashboard_row"
---

<objective>
Criar o partial `_arte_dashboard_row.html.erb`, refatorar o dashboard para usá-lo, e
adicionar `after_create_commit :broadcasts_to_admin` ao model `ApprovalResponse` com o método
privado que dispara o único broadcast com múltiplos Turbo Streams.

Purpose: Este é o plano de entrega — nada de real-time funciona antes deste plano. Após
execução, criar um ApprovalResponse dispara automaticamente toast + badge (se change_requested)
+ dashboard row replace + approvals page prepend, tudo em uma única transmissão WebSocket.

Output: Dashboard refatorado com partial idêntico ao inline atual (apenas extraído).
`ApprovalResponse` com callback de broadcast. Todos os 7 testes RED do Plan 00 ficam VERDES.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-00-SUMMARY.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Partial _arte_dashboard_row + refatorar dashboard index</name>
  <files>
    app/views/admin/dashboard/_arte_dashboard_row.html.erb,
    app/views/admin/dashboard/index.html.erb
  </files>
  <read_first>
    - app/views/admin/dashboard/index.html.erb (ler COMPLETO — localizar bloco `<% artes.each do |arte| %> <tr class="hover:bg-slate-50">` dentro do tbody)
    - app/views/admin/artes/_arte_row.html.erb (analog de estrutura de `<tr>` com partial — confirmar padrão de colunas)
    - app/views/client/shared/_arte_status_badge.html.erb (confirmar que aceita `arte:` e `compact: true` como locals)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md (seção `_arte_dashboard_row.html.erb — CREATE`)
  </read_first>
  <action>
    PASSO 1 — Criar `app/views/admin/dashboard/_arte_dashboard_row.html.erb` (arquivo NOVO):
    O partial renderiza o `<tr>` da arte no dashboard com `id="<%= dom_id(arte) %>"`.
    Colunas obrigatórias (mesma ordem e classes do inline atual em dashboard/index.html.erb):
    1. `<td class="px-4 py-3 text-sm text-slate-900"><%= arte.title.presence || "Sem título" %></td>`
    2. `<td class="px-4 py-3 text-sm text-slate-600"><%= arte.scheduled_on.strftime("%d/%m/%Y") %></td>`
    3. `<td class="px-4 py-3"><%= render "client/shared/arte_status_badge", arte: arte, compact: true %></td>`
    4. `<td class="px-4 py-3"><%= link_to "Ver", admin_arte_path(arte), class: "inline-flex items-center h-8 px-3 border border-gray-200 rounded-lg text-xs text-slate-600 hover:bg-gray-50 transition-colors", data: { turbo_frame: "_top" } %></td>`
    Wrapper: `<tr id="<%= dom_id(arte) %>" class="hover:bg-slate-50">`.
    Adicionar comentário na linha 1: `<%# Partial para Turbo Stream replace (RTUP-03) — id=dom_id(arte) é o target %>`.
    Local esperada: `arte` (Arte — caller deve eager-load :client se necessário para badge).

    PASSO 2 — Modificar `app/views/admin/dashboard/index.html.erb`:
    Dentro do loop `<% artes.each do |arte| %>`, substituir o bloco `<tr class="hover:bg-slate-50"> ... </tr>` (toda a `<tr>` inline) por:
    `<%= render "admin/dashboard/arte_dashboard_row", arte: arte %>`.
    O `<tbody class="divide-y divide-gray-100">` e o `<% end %>` do loop permanecem — apenas o conteúdo do `<tr>` é substituído.
    VERIFICAR: a view tem múltiplos clientes com suas próprias tabelas (`@artes_by_client.each do |client, artes|`) — substituir O `<tr>` inline em CADA iteração da `artes.each`. Há apenas um único bloco `<tr>` inline — não há outros `<tr>` de arte a preservar.
  </action>
  <verify>
    <automated>grep -n "dom_id\|arte_dashboard_row\|hover:bg-slate-50" app/views/admin/dashboard/_arte_dashboard_row.html.erb app/views/admin/dashboard/index.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - `app/views/admin/dashboard/_arte_dashboard_row.html.erb` existe e contém `dom_id(arte)` no atributo `id` do `<tr>`
    - `app/views/admin/dashboard/_arte_dashboard_row.html.erb` contém `arte_status_badge` (reutiliza badge existente)
    - `app/views/admin/dashboard/_arte_dashboard_row.html.erb` contém `admin_arte_path(arte)` para o link "Ver"
    - `app/views/admin/dashboard/index.html.erb` contém `render "admin/dashboard/arte_dashboard_row", arte: arte`
    - `app/views/admin/dashboard/index.html.erb` NÃO contém mais `<tr class="hover:bg-slate-50">` inline (foi movido para o partial)
    - `bin/rails test test/controllers/admin/dashboard_controller_test.rb` passa (dashboard renderiza sem erro)
  </acceptance_criteria>
  <done>
    Dashboard refatorado: o `<tr>` da arte está em partial _arte_dashboard_row com id=dom_id(arte). O Turbo Stream replace do Plan 02 (callback do model) pode substituir a linha sem recarregar a página.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: after_create_commit broadcasts_to_admin no ApprovalResponse → GREEN</name>
  <files>app/models/approval_response.rb</files>
  <read_first>
    - app/models/approval_response.rb (ler COMPLETO — identificar posição de after_create :sync_arte_status para inserir after_create_commit logo após)
    - app/channels/admin_notifications_channel.rb (confirmar assinatura de broadcast_to — recebe user e kwargs)
    - app/models/arte.rb (confirmar que enum :status gera scope Arte.change_requested; confirmar que dom_id(arte) usa id e model name)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md (seção `<specifics>` — código esperado de broadcasts_to_admin com streams.compact)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md (seção `approval_response.rb — ADD after_create_commit`)
    - test/models/approval_response_test.rb (confirmar nomes e assertions dos 7 testes RED do Plan 00)
  </read_first>
  <behavior>
    - after_create_commit :broadcasts_to_admin registrado no model (após after_create :sync_arte_status)
    - broadcasts_to_admin: User.first como admin alvo; return se admin nil
    - broadcasts_to_admin: arte_with_client = Arte.includes(:client).find(arte_id) — eager-load D-15
    - broadcasts_to_admin: badge_count = Arte.change_requested.count — server-authoritative D-11
    - Streams para change_requested: [toast_append, badge_replace, dashboard_replace, approvals_prepend] — 4 streams
    - Streams para approved: [toast_append, dashboard_replace, approvals_prepend] — 3 streams (badge stream é nil, .compact remove)
    - AdminNotificationsChannel.broadcast_to(admin, turbo_stream: streams) — único broadcast D-02
  </behavior>
  <action>
    PASSO 1 — Adicionar callback ao `ApprovalResponse`:
    Após a linha `after_create :sync_arte_status`, adicionar:
    `after_create_commit :broadcasts_to_admin`
    IMPORTANTE: `after_create_commit` (não `after_create`) — garante broadcast fora da transação aberta (dados visíveis para outras conexões DB).

    PASSO 2 — Adicionar método privado `broadcasts_to_admin` no bloco `private`:
    O método segue a estrutura de `streams = [...].compact; AdminNotificationsChannel.broadcast_to(admin, turbo_stream: streams)`.

    Variáveis locais obrigatórias dentro do método:
    - `admin = User.first` — single-admin por ora; `return unless admin` como guard
    - `arte_with_client = Arte.includes(:client).find(arte_id)` — D-15: eager-load
    - `badge_count = Arte.change_requested.count` — D-11: server-authoritative

    Array de streams (`streams = [...].compact`):
    1. `turbo_stream.append("admin-toast-region", partial: "admin/shared/approval_toast", locals: { approval_response: self, arte: arte_with_client })` — sempre presente
    2. `(turbo_stream.replace("sidebar-badge", partial: "admin/shared/sidebar_badge", locals: { badge_count: badge_count }) if decision == "change_requested")` — nil para :approved; .compact remove — D-12
    3. `turbo_stream.replace(dom_id(arte_with_client), partial: "admin/dashboard/arte_dashboard_row", locals: { arte: arte_with_client })` — sempre presente
    4. `turbo_stream.prepend("approvals-tbody", partial: "admin/approvals/approval_row", locals: { approval_response: self })` — sempre presente

    Chamar: `AdminNotificationsChannel.broadcast_to(admin, turbo_stream: streams)`.

    PASSO 3 — Executar testes:
    `bin/rails test test/models/approval_response_test.rb`
    Os 7 testes RED do Plan 00 devem ficar VERDES. Os testes pré-existentes devem permanecer VERDES.
    Se algum teste falhar por detalhe de implementação (ex: `Arte.change_requested` vs `Arte.where(status: :change_requested)`), ajustar o método para satisfazer o contrato do teste — o teste é a fonte de verdade.

    NOTA: Se o ambiente de banco de dados de teste estiver inacessível (worktree), verificar a sintaxe Ruby via `ruby -c app/models/approval_response.rb`. Os testes passarão no ambiente com banco de dados funcional.
  </action>
  <verify>
    <automated>bin/rails test test/models/approval_response_test.rb 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/approval_response.rb` contém `after_create_commit :broadcasts_to_admin`
    - `app/models/approval_response.rb` NÃO tem `after_create :broadcasts_to_admin` (seria dentro da transação — errado)
    - `app/models/approval_response.rb` contém `Arte.includes(:client).find(arte_id)` no método privado
    - `app/models/approval_response.rb` contém `Arte.change_requested.count`
    - `app/models/approval_response.rb` contém `AdminNotificationsChannel.broadcast_to`
    - `app/models/approval_response.rb` contém `.compact` na construção do array de streams
    - `app/models/approval_response.rb` contém a guarda `if decision == "change_requested"` ou equivalente para badge stream condicional
    - `bin/rails test test/models/approval_response_test.rb` — todos os testes passam (incluindo os 7 RED do Plan 00)
    - `ruby -c app/models/approval_response.rb` retorna "Syntax OK"
  </acceptance_criteria>
  <done>
    broadcasts_to_admin implementado e testado. Criar um ApprovalResponse via Client::ResponsesController dispara automaticamente toast + badge (se change_requested) + dashboard row replace + approvals page prepend em uma única transmissão WebSocket ao admin logado. Todos os 7 testes do Plan 00 estão VERDES.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ApprovalResponse#after_create_commit → AdminNotificationsChannel | Model dispara broadcast após persistência — fora de qualquer request HTTP do cliente |
| AdminNotificationsChannel → admin browser | Turbo Streams injetados no DOM do admin via WebSocket |
| Arte.change_requested.count → badge_count | Query ao banco dentro do callback de model — sem input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Spoofing | ApprovalResponse#broadcasts_to_admin — User.first | accept | Single-admin por design; canal rejeita sem current_user (Phase 17 AdminNotificationsChannel#subscribed); broadcast apenas para admin autenticado via stream_for |
| T-18-02-02 | Information Disclosure | turbo_stream.append — approval_toast partial | accept | Canal WebSocket exige autenticação admin (Phase 17 connection.rb + channel reject); dado exposto (cliente, decisão, link) é dado legítimo para admin |
| T-18-02-03 | Denial of Service | Arte.change_requested.count + Arte.includes(:client).find | accept | Volume de 10–30 clientes; queries indexadas por PK e status enum; risco de DoS por volume negligível no contexto atual |
| T-18-02-04 | Tampering | after_create_commit dentro de transação | mitigate | Usar `after_create_commit` (não `after_create`) garante broadcast apenas após commit — evita transmitir dados em transação aberta que pode ser revertida |
| T-18-02-05 | Tampering | dom_id(arte_with_client) como target Turbo Stream | accept | dom_id gera `arte_N` com N da PK do banco — não manipulável via input externo |
| T-18-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
- `ruby -c app/models/approval_response.rb` → "Syntax OK"
- `grep -c "after_create_commit :broadcasts_to_admin" app/models/approval_response.rb` → 1
- `grep -c "Arte.includes(:client).find" app/models/approval_response.rb` → 1
- `grep -c "Arte.change_requested.count" app/models/approval_response.rb` → 1
- `bin/rails test test/models/approval_response_test.rb` → todos verdes (incluindo 7 do Plan 00)
- `bin/rails test` → suite completa verde sem regressões
</verification>

<success_criteria>
Dashboard refatorado com partial _arte_dashboard_row (id=dom_id(arte)). ApprovalResponse dispara broadcast via after_create_commit com 4 streams (change_requested) ou 3 streams (approved). Todos os 7 testes RED do Plan 00 estão VERDES. Fase 18 entregue: admin recebe toast + badge + live rows sem recarregar a página.
</success_criteria>

<output>
Criar `.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-02-SUMMARY.md` ao concluir.
</output>
