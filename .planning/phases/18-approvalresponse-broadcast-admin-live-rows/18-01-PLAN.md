---
phase: 18-approvalresponse-broadcast-admin-live-rows
plan: "01"
type: auto
wave: 1
depends_on: []
files_modified:
  - app/views/admin/shared/_sidebar.html.erb
  - app/views/admin/shared/_sidebar_badge.html.erb
  - app/views/admin/shared/_approval_toast.html.erb
  - app/views/admin/approvals/index.html.erb
  - app/views/admin/approvals/_approval_row.html.erb
autonomous: true
requirements: [RTUP-01, RTUP-02, RTUP-04]

must_haves:
  truths:
    - "span#sidebar-badge está SEMPRE presente no DOM do sidebar, com classe 'hidden' quando badge_count == 0"
    - "Turbo Stream replace de 'sidebar-badge' via partial admin/shared/sidebar_badge funciona sem elemento faltando na DOM"
    - "Toast partial renderiza nome do cliente, badge de decisão (via _decision_badge) e link 'Ver arte' com admin_arte_path"
    - "Tabela desktop da página Aprovações tem <tbody id='approvals-tbody'>"
    - "Cada <tr> em _approval_row.html.erb tem id='approval_response_N' via dom_id(approval_response)"
  artifacts:
    - path: app/views/admin/shared/_sidebar_badge.html.erb
      provides: "Partial isolado do span#sidebar-badge para Turbo Stream replace"
      contains: "sidebar-badge"
    - path: app/views/admin/shared/_approval_toast.html.erb
      provides: "Toast de notificação com cliente, decisão e link"
      contains: "admin-toast-region"
    - path: app/views/admin/shared/_sidebar.html.erb
      provides: "Sidebar sem conditional badge_count > 0"
  key_links:
    - from: app/views/admin/shared/_sidebar.html.erb
      to: app/views/admin/shared/_sidebar_badge.html.erb
      via: "render 'admin/shared/sidebar_badge', badge_count: badge_count"
      pattern: "render.*sidebar_badge"
    - from: app/views/admin/shared/_approval_toast.html.erb
      to: app/views/admin/approvals/_decision_badge.html.erb
      via: "render 'admin/approvals/decision_badge', approval_response: approval_response"
      pattern: "render.*decision_badge"
    - from: app/views/admin/approvals/_approval_row.html.erb
      to: "dom_id(approval_response)"
      via: "id atributo no <tr>"
      pattern: "dom_id.*approval_response"
---

<objective>
Preparar todas as views para receber os Turbo Stream broadcasts do Plan 02: (1) sidebar badge
sempre presente no DOM via partial isolado; (2) partial de toast pronto para append em
#admin-toast-region; (3) página Aprovações com tbody e rows identificáveis por dom_id.

Purpose: Sem esses IDs e partials, o broadcast do Plan 02 falharia silenciosamente — Turbo
Stream replace/prepend não encontraria os targets na DOM.

Output: 5 arquivos modificados/criados. Nenhuma funcionalidade real-time ativa ainda — apenas
a estrutura DOM que o Plan 02 precisará.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md
@.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md
@app/javascript/controllers/toast_controller.js
@app/views/admin/approvals/_decision_badge.html.erb
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Sidebar badge sempre no DOM + partial _sidebar_badge</name>
  <files>
    app/views/admin/shared/_sidebar.html.erb,
    app/views/admin/shared/_sidebar_badge.html.erb
  </files>
  <read_first>
    - app/views/admin/shared/_sidebar.html.erb (ler COMPLETO — localizar bloco `if item[:path] == admin_approvals_path && badge_count > 0` nas linhas ~28–34)
    - app/views/layouts/admin.html.erb (confirmar que sidebar é rendered via `render "admin/shared/sidebar"` — sem locals extras)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-CONTEXT.md (seção `<specifics>` — padrão exato do span com hidden class, D-10)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md (seção `_sidebar.html.erb — FIX badge condicional`)
  </read_first>
  <action>
    PASSO 1 — Criar `app/views/admin/shared/_sidebar_badge.html.erb` (arquivo NOVO):
    O partial deve renderizar SOMENTE o `<span id="sidebar-badge">` com toggle de hidden class.
    Atributos obrigatórios: `id="sidebar-badge"`, classes `ml-auto bg-red-500 text-white text-xs font-bold px-1.5 py-0.5 rounded-full` mais `hidden` condicional quando `badge_count == 0`.
    O partial espera a local `badge_count` (Integer). Renderiza `badge_count` como conteúdo do span.
    Padrão de hidden: `<%= 'hidden' if badge_count == 0 %>` interpolado nas classes.

    PASSO 2 — Modificar `app/views/admin/shared/_sidebar.html.erb`:
    Localizar o bloco condicional `<% if item[:path] == admin_approvals_path && badge_count > 0 %>`.
    Substituir APENAS esse bloco por:
    `<% if item[:path] == admin_approvals_path %>` (remover `&& badge_count > 0`)
    Dentro do bloco, substituir o `<span ...>` inline por:
    `<%= render "admin/shared/sidebar_badge", badge_count: badge_count %>`
    Remover o `<span id="sidebar-badge" ...>` inline e o `<% end %>` de fechamento original — manter apenas o novo `render` dentro do `if admin_approvals_path`.

    IMPORTANTE: O `badge_count` já está calculado antes do loop via `Arte.where(status: :change_requested).count` — NÃO duplicar a query. O partial herda o local implicitamente via `render "...", badge_count: badge_count`.
  </action>
  <verify>
    <automated>grep -n "sidebar-badge\|hidden\|badge_count" app/views/admin/shared/_sidebar.html.erb app/views/admin/shared/_sidebar_badge.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - `app/views/admin/shared/_sidebar_badge.html.erb` existe e contém `id="sidebar-badge"`
    - `app/views/admin/shared/_sidebar_badge.html.erb` contém a string `'hidden' if badge_count == 0`
    - `app/views/admin/shared/_sidebar.html.erb` NÃO contém mais `badge_count > 0` (condicional removido)
    - `app/views/admin/shared/_sidebar.html.erb` contém `render "admin/shared/sidebar_badge", badge_count: badge_count`
    - `app/views/admin/shared/_sidebar.html.erb` NÃO contém mais `<span id="sidebar-badge"` inline (foi movido para o partial)
    - `bin/rails test test/controllers/admin/dashboard_controller_test.rb` passa (sidebar renderiza sem erro)
  </acceptance_criteria>
  <done>
    span#sidebar-badge sempre presente no DOM (com `hidden` quando count=0). Partial _sidebar_badge.html.erb existe e é renderizado pelo sidebar. Turbo Stream pode fazer replace de "sidebar-badge" em qualquer momento sem risco de elemento ausente.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Toast partial + dom_id em _approval_row + id="approvals-tbody"</name>
  <files>
    app/views/admin/shared/_approval_toast.html.erb,
    app/views/admin/approvals/_approval_row.html.erb,
    app/views/admin/approvals/index.html.erb
  </files>
  <read_first>
    - app/views/admin/approvals/_decision_badge.html.erb (ler COMPLETO — partial reutilizado no toast; confirmar nome de local esperado)
    - app/javascript/controllers/toast_controller.js (ler COMPLETO — confirmar `data-controller="toast"` e `data-action="click->toast#dismiss"` para integração)
    - app/views/layouts/admin.html.erb (confirmar `id="admin-toast-region"` linha ~25 — target do turbo_stream.append)
    - app/views/admin/approvals/_approval_row.html.erb (ler COMPLETO — localizar abertura do `<tr class="hover:bg-gray-50...">`)
    - app/views/admin/approvals/index.html.erb (ler COMPLETO — localizar `<tbody>` da tabela DESKTOP, dentro de `.hidden.sm:block`)
    - .planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-PATTERNS.md (seção `_approval_toast.html.erb — CREATE` e `_approval_row.html.erb — ADD id`)
  </read_first>
  <action>
    PASSO 1 — Criar `app/views/admin/shared/_approval_toast.html.erb` (arquivo NOVO):
    Estrutura: `<div data-controller="toast" class="bg-white border border-gray-200 shadow-lg rounded-lg px-4 py-3 flex items-start gap-3 w-80">` como wrapper.
    Dentro: `<div class="flex-1 min-w-0">` com `<p class="text-sm font-semibold text-slate-900 truncate"><%= arte.client.name %></p>` e `<%= render "admin/approvals/decision_badge", approval_response: approval_response %>`.
    Botão Ver arte: `<%= link_to "Ver arte", admin_arte_path(arte), class: "text-xs text-[#0F7949] font-medium hover:underline shrink-0" %>`.
    Botão fechar: `<button data-action="click->toast#dismiss" class="text-slate-400 hover:text-slate-600 shrink-0 leading-none" aria-label="Fechar notificação">&times;</button>`.
    Locals esperados: `approval_response` (ApprovalResponse) e `arte` (Arte com :client eager-loaded via D-15).
    Adicionar comentário na linha 1: `<%# Locals: approval_response (ApprovalResponse), arte (Arte com :client eager-loaded) %>`.

    PASSO 2 — Modificar `app/views/admin/approvals/_approval_row.html.erb`:
    Localizar a primeira linha: `<tr class="hover:bg-gray-50 transition-colors border-b border-gray-100 last:border-0">`.
    Adicionar atributo `id`: `<tr id="<%= dom_id(approval_response) %>" class="hover:bg-gray-50 transition-colors border-b border-gray-100 last:border-0">`.
    APENAS essa linha muda — o restante do partial permanece idêntico.

    PASSO 3 — Modificar `app/views/admin/approvals/index.html.erb`:
    Localizar o `<tbody>` DENTRO da seção desktop (dentro de `<div class="... hidden sm:block">`).
    O `<tbody>` atual está na linha ~49 sem atributos: `<tbody>`.
    Substituir por: `<tbody id="approvals-tbody">`.
    NÃO tocar nos cards mobile (`<div class="block sm:hidden">`) — D-13: mobile não é target de broadcast.
  </action>
  <verify>
    <automated>grep -n "dom_id\|approvals-tbody\|data-controller.*toast\|admin-toast-region\|Ver arte" app/views/admin/shared/_approval_toast.html.erb app/views/admin/approvals/_approval_row.html.erb app/views/admin/approvals/index.html.erb</automated>
  </verify>
  <acceptance_criteria>
    - `app/views/admin/shared/_approval_toast.html.erb` existe e contém `data-controller="toast"`
    - `_approval_toast.html.erb` contém `click->toast#dismiss`
    - `_approval_toast.html.erb` contém `admin_arte_path(arte)`
    - `_approval_toast.html.erb` contém `render "admin/approvals/decision_badge"`
    - `_approval_toast.html.erb` contém `arte.client.name`
    - `app/views/admin/approvals/_approval_row.html.erb` contém `dom_id(approval_response)` no `<tr>`
    - `app/views/admin/approvals/index.html.erb` contém `id="approvals-tbody"` no `<tbody>` desktop
    - `app/views/admin/approvals/index.html.erb` NÃO contém `id="approvals-tbody"` na seção mobile (`.block.sm:hidden`)
    - `bin/rails test test/controllers/admin/approvals_controller_test.rb` passa (se o arquivo existir)
  </acceptance_criteria>
  <done>
    Toast partial pronto para append em #admin-toast-region. Approval rows têm ID único via dom_id. tbody desktop tem id="approvals-tbody" para prepend. Nenhum broadcast ativo ainda — só preparação DOM.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → view partial | `arte.client.name` e `approval_response` chegam do model ao partial — input de usuário potencialmente não sanitizado |
| broadcast → browser DOM | Turbo Stream injeta HTML no browser admin via WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Information Disclosure | _approval_toast.html.erb | accept | Toast visível apenas para admin autenticado via AdminNotificationsChannel (canal rejeita sem current_user — Phase 17) |
| T-18-01-02 | Tampering | _approval_toast.html.erb — arte.client.name | mitigate | Rails ERB auto-escapa output: `<%= arte.client.name %>` não executa HTML/JS do client name. XSS prevenido por padrão |
| T-18-01-03 | Tampering | _approval_row.html.erb — dom_id | accept | dom_id gera `approval_response_N` com N inteiro do banco — não interpolável de input externo |
| T-18-01-04 | Elevation of Privilege | approvals/index.html.erb | accept | Página já exige admin auth via before_action :require_admin em AdminController base |
| T-18-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
- `grep -c "sidebar-badge" app/views/admin/shared/_sidebar_badge.html.erb` retorna ≥ 1
- `grep "hidden.*badge_count == 0\|badge_count == 0.*hidden" app/views/admin/shared/_sidebar_badge.html.erb` retorna 1 linha
- `grep "badge_count > 0" app/views/admin/shared/_sidebar.html.erb` retorna 0 (condicional removido)
- `grep "approvals-tbody" app/views/admin/approvals/index.html.erb` retorna 1 linha
- `grep "dom_id(approval_response)" app/views/admin/approvals/_approval_row.html.erb` retorna 1 linha
- `bin/rails test test/controllers/admin/` passa sem regressões
</verification>

<success_criteria>
Todos os 5 arquivos criados/modificados conforme especificado. DOM pronto para receber broadcasts do Plan 02: sidebar badge sempre presente, toast partial criado, approvals tbody e rows identificáveis.
</success_criteria>

<output>
Criar `.planning/phases/18-approvalresponse-broadcast-admin-live-rows/18-01-SUMMARY.md` ao concluir.
</output>
