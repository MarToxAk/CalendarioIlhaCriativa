---
phase: 10-arte-form-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - app/views/admin/artes/new.html.erb
  - app/views/admin/artes/edit.html.erb
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "A página Nova Arte exibe o form dentro de um card branco com shadow-card e max-w-2xl"
    - "A página Nova Arte tem um back link visível apontando para admin_artes_path"
    - "A página Editar Arte exibe o form dentro de um card branco com shadow-card e max-w-2xl"
    - "A página Editar Arte tem um back link mostrando @arte.title apontando para admin_arte_path(@arte)"
    - "O render do partial em ambas as páginas passa button_label e cancel_path como locals"
  artifacts:
    - path: "app/views/admin/artes/new.html.erb"
      provides: "Página com back link e card wrapper para o form"
      contains: "shadow-card"
    - path: "app/views/admin/artes/edit.html.erb"
      provides: "Página com back link personalizado e card wrapper para o form"
      contains: "@arte.title"
  key_links:
    - from: "app/views/admin/artes/new.html.erb"
      to: "app/views/admin/artes/_form.html.erb"
      via: "render 'form' com locals arte: @arte, button_label:, cancel_path:"
      pattern: "button_label"
    - from: "app/views/admin/artes/edit.html.erb"
      to: "app/views/admin/artes/_form.html.erb"
      via: "render 'form' com locals arte: @arte, button_label:, cancel_path:"
      pattern: "cancel_path"
---

<objective>
Reescrever `new.html.erb` e `edit.html.erb` de artes para adicionar card wrapper (bg-white rounded-xl border border-gray-200 shadow-card p-8 max-w-2xl) e back link, seguindo o padrão exato das páginas de clients — e passar os locals `button_label` e `cancel_path` no render do partial.

Purpose: Atualmente ambas as páginas renderizam o form sem qualquer container visual — o formulário flutua sem card, sem link de voltar. A fase exige o mesmo padrão visual já estabelecido nas pages de clients.

Output: `new.html.erb` e `edit.html.erb` com estrutura idêntica ao padrão de clients (back link + card), adaptados para artes.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-arte-form-polish/10-CONTEXT.md
@.planning/phases/10-arte-form-polish/10-PATTERNS.md
@.planning/phases/10-arte-form-polish/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever new.html.erb com back link e card wrapper (PAGE-01)</name>

  <read_first>
    - `app/views/admin/artes/new.html.erb` — estado atual (apenas 3 linhas); confirmar conteúdo antes de sobrescrever
    - `app/views/admin/clients/new.html.erb` — padrão canônico EXATO a ser replicado; copiar estrutura completa
    - `.planning/phases/10-arte-form-polish/10-PATTERNS.md` — seção "new.html.erb" (linhas 22-72) com o resultado esperado linha por linha
  </read_first>

  <action>
Substituir o conteúdo completo de `app/views/admin/artes/new.html.erb` pela seguinte estrutura (replicando clients/new.html.erb com adaptações per D-01, D-02):

Estrutura da página:
  1. `<% content_for(:page_title) { "Nova Arte" } %>` — manter o page_title existente
  2. Div `mb-6` com `link_to` para `admin_artes_path` (não `admin_arte_path`):
     - `class:` do link: `"text-sm text-slate-600 hover:text-slate-900 transition-colors"`
     - `aria: { label: "Voltar para lista de artes" }`
     - Texto do link: `&larr; Voltar para Artes`
  3. Div `bg-white rounded-xl border border-gray-200 shadow-card p-8 max-w-2xl` com render do form:
     - `render "form", arte: @arte, button_label: "Criar arte", cancel_path: admin_artes_path`

Diferença crítica em relação ao analog de clients:
  - `max-w-2xl` (não `max-w-lg`) — per D-01
  - Path do back link: `admin_artes_path` (não `admin_clients_path`)
  - Locals do render: `arte: @arte, button_label: "Criar arte", cancel_path: admin_artes_path`
  </action>

  <verify>
    <automated>grep -c "shadow-card" app/views/admin/artes/new.html.erb && grep -c "admin_artes_path" app/views/admin/artes/new.html.erb && grep -c "button_label" app/views/admin/artes/new.html.erb</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "shadow-card" app/views/admin/artes/new.html.erb` retorna `1`
    - `grep -c "max-w-2xl" app/views/admin/artes/new.html.erb` retorna `1`
    - `grep -c "admin_artes_path" app/views/admin/artes/new.html.erb` retorna `2` (back link + cancel_path no render)
    - `grep -c "button_label" app/views/admin/artes/new.html.erb` retorna `1`
    - `grep -c "Criar arte" app/views/admin/artes/new.html.erb` retorna `1`
    - `grep -c "Voltar para Artes" app/views/admin/artes/new.html.erb` retorna `1`
    - `grep -c "max-w-lg" app/views/admin/artes/new.html.erb` retorna `0` (não usar max-w-lg)
    - Arquivo tem entre 10 e 16 linhas (estrutura similar ao analog de clients)
  </acceptance_criteria>

  <done>new.html.erb reescrita com back link apontando para admin_artes_path, card max-w-2xl e locals button_label/cancel_path passados ao partial.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever edit.html.erb com back link personalizado e card wrapper (PAGE-02)</name>

  <read_first>
    - `app/views/admin/artes/edit.html.erb` — estado atual (apenas 3 linhas); confirmar conteúdo antes de sobrescrever
    - `app/views/admin/clients/edit.html.erb` — padrão canônico EXATO a ser replicado; copiar estrutura completa
    - `.planning/phases/10-arte-form-polish/10-PATTERNS.md` — seção "edit.html.erb" (linhas 76-125) com o resultado esperado
  </read_first>

  <action>
Substituir o conteúdo completo de `app/views/admin/artes/edit.html.erb` pela seguinte estrutura (replicando clients/edit.html.erb com adaptações per D-01, D-03):

Estrutura da página:
  1. `<% content_for(:page_title) { "Editar Arte" } %>` — manter o page_title existente
  2. Div `mb-6` com `link_to` para `admin_arte_path(@arte)`:
     - `class:` do link: `"text-sm text-slate-600 hover:text-slate-900 transition-colors"`
     - `aria: { label: "Voltar para detalhes de #{@arte.title}" }`
     - Texto do link: `&larr; Voltar para <%= @arte.title %>`
  3. Div `bg-white rounded-xl border border-gray-200 shadow-card p-8 max-w-2xl` com render do form:
     - `render "form", arte: @arte, button_label: "Salvar alterações", cancel_path: admin_arte_path(@arte)`

Diferenças críticas em relação ao analog de clients:
  - `max-w-2xl` (não `max-w-lg`) — per D-01
  - Path do back link: `admin_arte_path(@arte)` (não `admin_client_path(@client)`)
  - Referência de nome: `@arte.title` (não `@client.name`)
  - Locals do render: `arte: @arte, button_label: "Salvar alterações", cancel_path: admin_arte_path(@arte)`
  </action>

  <verify>
    <automated>grep -c "shadow-card" app/views/admin/artes/edit.html.erb && grep -c "@arte.title" app/views/admin/artes/edit.html.erb && grep -c "cancel_path" app/views/admin/artes/edit.html.erb</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "shadow-card" app/views/admin/artes/edit.html.erb` retorna `1`
    - `grep -c "max-w-2xl" app/views/admin/artes/edit.html.erb` retorna `1`
    - `grep -c "@arte.title" app/views/admin/artes/edit.html.erb` retorna `2` (no aria label + no texto do link)
    - `grep -c "admin_arte_path(@arte)" app/views/admin/artes/edit.html.erb` retorna `2` (back link + cancel_path no render)
    - `grep -c "button_label" app/views/admin/artes/edit.html.erb` retorna `1`
    - `grep -c "Salvar alterações" app/views/admin/artes/edit.html.erb` retorna `1`
    - `grep -c "max-w-lg" app/views/admin/artes/edit.html.erb` retorna `0` (não usar max-w-lg)
    - Arquivo tem entre 10 e 16 linhas
  </acceptance_criteria>

  <done>edit.html.erb reescrita com back link mostrando @arte.title apontando para admin_arte_path(@arte), card max-w-2xl e locals button_label/cancel_path passados ao partial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → server-rendered views | @arte.title renderizado via ERB; proteção contra XSS é responsabilidade do Rails (auto-escape padrão) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | XSS (Tampering) | edit.html.erb — `<%= @arte.title %>` no back link | mitigate | Rails escapa automaticamente interpolações ERB com `<%= %>`. Não usar `html_safe` ou `raw` — manter escape padrão |
| T-10-02 | Information Disclosure | Views admin | accept | Páginas behind autenticação admin; não há dados expostos além do que já estava acessível |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Fase não instala nenhum pacote |
</threat_model>

<verification>
Após execução dos dois tasks:

```bash
grep -c "shadow-card" app/views/admin/artes/new.html.erb
# Esperado: 1

grep -c "max-w-2xl" app/views/admin/artes/new.html.erb
# Esperado: 1

grep -c "max-w-2xl" app/views/admin/artes/edit.html.erb
# Esperado: 1

grep -c "@arte.title" app/views/admin/artes/edit.html.erb
# Esperado: 2

grep -c "button_label" app/views/admin/artes/new.html.erb
# Esperado: 1

grep -c "button_label" app/views/admin/artes/edit.html.erb
# Esperado: 1
```
</verification>

<success_criteria>
- `new.html.erb`: back link para `admin_artes_path` + card `shadow-card max-w-2xl` + render com `button_label: "Criar arte"` e `cancel_path: admin_artes_path`
- `edit.html.erb`: back link mostrando `@arte.title` para `admin_arte_path(@arte)` + card `shadow-card max-w-2xl` + render com `button_label: "Salvar alterações"` e `cancel_path: admin_arte_path(@arte)`
- Nenhuma das duas páginas usa `max-w-lg`
- Nenhuma das duas páginas renderiza o form sem container (zero linhas com apenas `render "form"` sem locals)
</success_criteria>

<output>
Criar `.planning/phases/10-arte-form-polish/10-03-SUMMARY.md` ao concluir.
</output>
