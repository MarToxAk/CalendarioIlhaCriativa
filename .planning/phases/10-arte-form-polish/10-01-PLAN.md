---
phase: 10-arte-form-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/admin/artes/_form.html.erb
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03

must_haves:
  truths:
    - "Todos os campos de texto, data, url e select têm border, focus ring verde (#0F7949) e height h-11"
    - "O textarea de legenda tem min-h-[80px] com resize-y em vez de h-11"
    - "O file input usa pseudo-classes file: com estilo verde claro"
    - "Os radio buttons de Tipo de mídia estão em pills horizontais com labels como área de clique"
    - "O botão de submit é verde sólido (#0F7949) e o botão Cancelar é neutro com borda cinza"
    - "O form aceita locals button_label e cancel_path em vez de lógica inline persisted?"
  artifacts:
    - path: "app/views/admin/artes/_form.html.erb"
      provides: "Form partial estilizado com Tailwind puro, sem form-input/btn/btn-primary"
      contains: "focus:border-[#0F7949]"
    - path: "app/views/admin/artes/_form.html.erb"
      provides: "Assinatura com locals button_label e cancel_path"
      contains: "button_label"
  key_links:
    - from: "app/views/admin/artes/_form.html.erb"
      to: "media_type_toggle_controller.js"
      via: "data-media-type-toggle-target='uploadLabel' nos labels dos pills"
      pattern: "uploadLabel"
---

<objective>
Estilizar completamente o form partial de artes substituindo as classes placeholder `form-input`, `btn` e `btn-primary` por Tailwind puro, e refatorar o rodapé para aceitar locals `button_label` e `cancel_path`.

Purpose: Eliminar as classes CSS sem definição que tornam os campos invisuais/sem estilo, e preparar o partial para receber dados de new.html.erb e edit.html.erb via locals (plano 03).

Output: `app/views/admin/artes/_form.html.erb` totalmente estilizado com Tailwind inline, radio pills com targets Stimulus, sem nenhuma ocorrência de `form-input`, `btn` ou `btn-primary`.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-arte-form-polish/10-CONTEXT.md
@.planning/phases/10-arte-form-polish/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estilizar campos e labels do form partial (FORM-01)</name>

  <read_first>
    - `app/views/admin/artes/_form.html.erb` — arquivo COMPLETO a ser modificado; ler para mapear cada ocorrência de `form-input` e sua posição
    - `app/views/admin/clients/_form.html.erb` — padrão canônico; extrair classes exatas de label, text_field e submit antes de qualquer edição
  </read_first>

  <action>
Modificar `app/views/admin/artes/_form.html.erb`. Aplicar as seguintes substituições de classes em TODOS os campos:

Labels (todos os f.label e o label manual de "Tipo de mídia"):
Substituir `class: "block text-sm font-medium mb-1"` por `class: "block text-sm font-medium text-slate-900 mb-1.5"`.

Campos text_field (:title) e date_field (:scheduled_on, :approval_deadline) e url_field (:external_url):
Substituir `class: "form-input w-full"` por `class: "block w-full h-11 px-3 border border-gray-200 rounded-lg text-sm text-slate-900 bg-white focus:outline-none focus:border-[#0F7949] focus:ring-2 focus:ring-[#0F7949]/10 transition-colors placeholder-slate-400"`.

Textarea (:caption):
Substituir `class: "form-input w-full"` por `class: "block w-full min-h-[80px] resize-y px-3 py-2 border border-gray-200 rounded-lg text-sm text-slate-900 bg-white focus:outline-none focus:border-[#0F7949] focus:ring-2 focus:ring-[#0F7949]/10 transition-colors placeholder-slate-400"`. Diferença: sem `h-11`, adiciona `min-h-[80px] resize-y` e `py-2`.

Select (:platform, :media_type, :client_id):
Substituir `class: "form-input w-full"` por `class: "block w-full h-11 px-3 border border-gray-200 rounded-lg text-sm text-slate-900 bg-white focus:outline-none focus:border-[#0F7949] focus:ring-2 focus:ring-[#0F7949]/10 transition-colors"`. (sem `placeholder-slate-400` — selects não têm placeholder de texto.)

File field (:media_file):
Substituir `class: "form-input w-full"` por `class: "block w-full text-sm text-slate-900 border border-gray-200 rounded-lg cursor-pointer bg-white file:mr-4 file:py-2 file:px-4 file:border-0 file:text-sm file:font-semibold file:bg-green-50 file:text-green-700 hover:file:bg-green-100"`.

Após essas substituições, não deve existir NENHUMA ocorrência de `form-input` no arquivo.
  </action>

  <verify>
    <automated>grep -c "form-input" app/views/admin/artes/_form.html.erb || true</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "form-input" app/views/admin/artes/_form.html.erb` retorna `0`
    - `grep -c "focus:border-\[#0F7949\]" app/views/admin/artes/_form.html.erb` retorna `5` (title, caption, scheduled_on, approval_deadline, external_url — 5 campos text/date/url/textarea com focus ring)
    - `grep -c "file:bg-green-50" app/views/admin/artes/_form.html.erb` retorna `1`
    - `grep -c "min-h-\[80px\]" app/views/admin/artes/_form.html.erb` retorna `1`
    - `grep -c "text-slate-900 mb-1.5" app/views/admin/artes/_form.html.erb` retorna igual ao número de labels no arquivo (verificar manualmente que cada label tem a nova classe)
  </acceptance_criteria>

  <done>Todos os campos do form têm Tailwind puro; zero ocorrências de `form-input` no arquivo.</done>
</task>

<task type="auto">
  <name>Task 2: Radio pills, botões e locals do rodapé (FORM-02, FORM-03)</name>

  <read_first>
    - `app/views/admin/artes/_form.html.erb` — estado pós-Task 1; verificar linhas 34-60 (bloco dos radio buttons e rodapé)
    - `.planning/phases/10-arte-form-polish/10-PATTERNS.md` — seções "Radio pill pattern" e "Botão submit pattern" para os valores exatos das classes
  </read_first>

  <action>
Ainda em `app/views/admin/artes/_form.html.erb`, fazer três grupos de mudanças:

GRUPO 1 — Radio pills (substituir o bloco `<div class="flex gap-4">` nas linhas ~36-39):
Substituir o div completo com os dois labels de radio por pills interativos. Cada pill é um `<label>` com:
  - `data-media-type-toggle-target="uploadLabel"` (ou `"linkLabel"`)
  - `class: "cursor-pointer flex items-center gap-2 px-4 py-2 rounded-lg border border-gray-200 text-sm font-medium transition-colors"` como classes base
  - O `f.radio_button` fica DENTRO do label com `class: "sr-only"` para ficar oculto visualmente mas acessível
  - Targets Stimulus mantidos: `data: { action: "media-type-toggle#selectUpload", media_type_toggle_target: "uploadRadio" }` no radio de upload; `data: { action: "media-type-toggle#selectLink", media_type_toggle_target: "linkRadio" }` no de link
  - Texto do label de upload: "Upload de arquivo"; texto do label de link: "Link externo"
  - O label manual `<label class="block text-sm font-medium mb-1">Tipo de mídia</label>` deve usar a nova classe: `class: "block text-sm font-medium text-slate-900 mb-1.5"`
  - O `<div class="flex gap-4">` vira `<div class="flex gap-3">`

GRUPO 2 — Rodapé de botões (substituir linhas ~57-60):
  - Wrapper: substituir `<div class="flex gap-2">` por `<div class="flex justify-end gap-3 pt-2">`
  - Submit: substituir `class: "btn btn-primary"` por `class: "h-10 px-4 bg-[#0F7949] hover:bg-[#0a5c37] active:scale-[0.99] text-white text-sm font-semibold rounded-lg transition-colors cursor-pointer"`
  - Submit text: substituir `arte.persisted? ? "Atualizar" : "Criar"` pelo local `button_label`
  - Cancelar: substituir `class: "btn"` por `class: "inline-flex items-center h-10 px-4 border border-gray-200 rounded-lg text-sm text-slate-600 hover:text-slate-900 hover:bg-gray-50 transition-colors"`
  - Cancelar path: substituir `arte.persisted? ? admin_arte_path(arte) : admin_artes_path` pelo local `cancel_path`
  - Cancelar texto: manter "Voltar sem salvar" (padrão do analog de clients)

GRUPO 3 — Garantir que o partial suporte locals:
  Adicionar na primeira linha do arquivo (após o comentário `<%# ... %>`) a declaração dos locals:
  `<% button_label ||= arte.persisted? ? "Salvar alterações" : "Criar" %>`
  `<% cancel_path ||= arte.persisted? ? admin_arte_path(arte) : admin_artes_path %>`
  Isso garante fallback para chamadas sem locals (compatibilidade retroativa se necessário).
  </action>

  <verify>
    <automated>grep -cE "btn|btn-primary" app/views/admin/artes/_form.html.erb || true</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '"btn"|"btn-primary"' app/views/admin/artes/_form.html.erb` retorna `0`
    - `grep -c "justify-end gap-3 pt-2" app/views/admin/artes/_form.html.erb` retorna `1`
    - `grep -c "uploadLabel" app/views/admin/artes/_form.html.erb` retorna `1`
    - `grep -c "linkLabel" app/views/admin/artes/_form.html.erb` retorna `1`
    - `grep -c "sr-only" app/views/admin/artes/_form.html.erb` retorna `2` (um por radio button)
    - `grep -c "button_label" app/views/admin/artes/_form.html.erb` retorna `2` (declaração do local + uso no submit)
    - `grep -c "cancel_path" app/views/admin/artes/_form.html.erb` retorna `2` (declaração do local + uso no link)
    - `grep -c "bg-\[#0F7949\]" app/views/admin/artes/_form.html.erb` retorna `1` (botão submit)
    - `grep -c "Voltar sem salvar" app/views/admin/artes/_form.html.erb` retorna `1`
  </acceptance_criteria>

  <done>Radio pills interativos implementados com targets Stimulus; botões com Tailwind puro; locals button_label e cancel_path declarados com fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Rails views | Modificações de view são server-rendered — sem input de usuário no escopo desta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | _form.html.erb — locals button_label/cancel_path | mitigate | Locals são definidos server-side via ||= fallback; nunca expostos a input do usuário — só admin autenticado acessa as actions new/edit |
| T-10-02 | Information Disclosure | Estilização pura de views | accept | Nenhum dado sensível exposto; mudanças são somente CSS classes em markup já existente |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Esta fase não instala nenhum pacote — zero risco de supply chain |
</threat_model>

<verification>
Após execução dos dois tasks:

```bash
grep -c "form-input" app/views/admin/artes/_form.html.erb
# Esperado: 0

grep -cE '"btn"|"btn-primary"' app/views/admin/artes/_form.html.erb
# Esperado: 0

grep -c "focus:border-\[#0F7949\]" app/views/admin/artes/_form.html.erb
# Esperado: >= 5

grep -c "uploadLabel" app/views/admin/artes/_form.html.erb
# Esperado: 1

grep -c "sr-only" app/views/admin/artes/_form.html.erb
# Esperado: 2
```
</verification>

<success_criteria>
- Nenhuma ocorrência de `form-input`, `btn`, `btn-primary` em `_form.html.erb`
- Todos os 7 campos estilizados com Tailwind (title, caption, scheduled_on, approval_deadline, platform, media_type, client_id, media_file, external_url)
- Radio pills com targets `uploadLabel` e `linkLabel` prontos para o Stimulus controller (Plano 02)
- Locals `button_label` e `cancel_path` funcionando com fallback
- Botão submit verde #0F7949 e cancelar neutro
</success_criteria>

<output>
Criar `.planning/phases/10-arte-form-polish/10-01-SUMMARY.md` ao concluir.
</output>
