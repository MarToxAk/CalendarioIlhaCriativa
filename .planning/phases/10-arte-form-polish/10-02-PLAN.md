---
phase: 10-arte-form-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/javascript/controllers/media_type_toggle_controller.js
autonomous: true
requirements:
  - FORM-03

must_haves:
  truths:
    - "O controller Stimulus gerencia o estilo visual dos pills além da visibilidade dos campos"
    - "Na carga da página o pill ativo (Upload ou Link) aparece destacado em verde #0F7949"
    - "Ao clicar em um pill, o destaque visual migra para o pill clicado imediatamente"
    - "Os targets uploadLabel e linkLabel existem no static targets do controller"
  artifacts:
    - path: "app/javascript/controllers/media_type_toggle_controller.js"
      provides: "Controller Stimulus extendido com togglePills() e novos targets"
      contains: "uploadLabel"
  key_links:
    - from: "app/javascript/controllers/media_type_toggle_controller.js"
      to: "app/views/admin/artes/_form.html.erb"
      via: "data-media-type-toggle-target='uploadLabel' nos labels dos pills"
      pattern: "uploadLabelTarget"
---

<objective>
Estender o Stimulus controller `media_type_toggle_controller.js` com o método `togglePills()` e os targets `uploadLabel`/`linkLabel` para gerenciar o estado visual ativo/inativo dos radio pills.

Purpose: Sem esta extensão, os pills do Plano 01 renderizam sem destaque visual — o estado ativo (verde) não é aplicado nem na carga inicial nem ao trocar de opção.

Output: `media_type_toggle_controller.js` com `static targets` incluindo `uploadLabel` e `linkLabel`, e método `togglePills()` chamado dentro de `toggleFields()`.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-arte-form-polish/10-CONTEXT.md
@.planning/phases/10-arte-form-polish/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar togglePills() e targets de label ao Stimulus controller (FORM-03)</name>

  <read_first>
    - `app/javascript/controllers/media_type_toggle_controller.js` — arquivo COMPLETO a ser modificado; ler o estado atual para garantir que os targets existentes (`uploadField`, `linkField`, `uploadRadio`, `linkRadio`) e os métodos (`connect`, `toggleFields`, `selectUpload`, `selectLink`) são preservados
    - `.planning/phases/10-arte-form-polish/10-PATTERNS.md` — seção do controller (linhas 258-376) com o resultado final esperado e o padrão exato do método `togglePills()`
  </read_first>

  <action>
Modificar `app/javascript/controllers/media_type_toggle_controller.js` com as seguintes mudanças cirúrgicas:

1. Linha `static targets`: adicionar `"uploadLabel"` e `"linkLabel"` ao array existente.
   Resultado: `static targets = ["uploadField", "linkField", "uploadRadio", "linkRadio", "uploadLabel", "linkLabel"]`

2. Dentro do método `toggleFields()`, adicionar `this.togglePills()` como ÚLTIMA linha do método, após a lógica de show/hide existente.

3. Adicionar o método `togglePills()` entre `toggleFields()` e `selectUpload()`:
   - Declarar `const activeClasses = ["border-[#0F7949]", "bg-green-50", "text-[#0F7949]"]`
   - Declarar `const inactiveClasses = ["border-gray-200", "text-slate-700"]`
   - Se `this.uploadRadioTarget.checked`:
     - `this.uploadLabelTarget.classList.add(...activeClasses)` e `.remove(...inactiveClasses)`
     - `this.linkLabelTarget.classList.remove(...activeClasses)` e `.add(...inactiveClasses)`
   - Caso contrário (`else`):
     - `this.linkLabelTarget.classList.add(...activeClasses)` e `.remove(...inactiveClasses)`
     - `this.uploadLabelTarget.classList.remove(...activeClasses)` e `.add(...inactiveClasses)`

NÃO alterar: a lógica de `connect()`, `selectUpload()`, `selectLink()` — apenas adicionar `this.togglePills()` dentro de `toggleFields()`.
  </action>

  <verify>
    <automated>grep -c "uploadLabel" app/javascript/controllers/media_type_toggle_controller.js && grep -c "togglePills" app/javascript/controllers/media_type_toggle_controller.js && grep -c "0F7949" app/javascript/controllers/media_type_toggle_controller.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "uploadLabel" app/javascript/controllers/media_type_toggle_controller.js` retorna `2` (um na declaração static targets, um no uso dentro de togglePills)
    - `grep -c "linkLabel" app/javascript/controllers/media_type_toggle_controller.js` retorna `2` (idem)
    - `grep -c "togglePills" app/javascript/controllers/media_type_toggle_controller.js` retorna `2` (definição do método + chamada dentro de toggleFields)
    - `grep -c "0F7949" app/javascript/controllers/media_type_toggle_controller.js` retorna `2` (presente em activeClasses: border e text)
    - `grep -c "bg-green-50" app/javascript/controllers/media_type_toggle_controller.js` retorna `1`
    - O array `static targets` contém exatamente 6 strings: `uploadField`, `linkField`, `uploadRadio`, `linkRadio`, `uploadLabel`, `linkLabel`
    - O método `selectUpload()` ainda chama apenas `this.uploadRadioTarget.checked = true` e `this.toggleFields()` — sem outras alterações
    - O método `selectLink()` ainda chama apenas `this.linkRadioTarget.checked = true` e `this.toggleFields()` — sem outras alterações
  </acceptance_criteria>

  <done>Controller extendido: togglePills() implementado, targets uploadLabel e linkLabel declarados, toggleFields() chama togglePills() ao final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → Stimulus controller | Manipulação de classList apenas em elementos do DOM controlados pelo servidor; sem input externo processado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | media_type_toggle_controller.js — classList manipulation | accept | Operações aplicadas apenas em elementos dentro do `data-controller` scope; sem eval, sem dados de usuário, sem XSS path |
| T-10-02 | Information Disclosure | JS client-side | accept | Nenhum dado sensível exposto — apenas classes CSS de UI |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Fase não instala nenhum pacote — zero risco de supply chain |
</threat_model>

<verification>
Após execução do task:

```bash
grep "static targets" app/javascript/controllers/media_type_toggle_controller.js
# Esperado: inclui uploadLabel e linkLabel

grep -c "togglePills" app/javascript/controllers/media_type_toggle_controller.js
# Esperado: 2 (definição + chamada)

grep -c "0F7949" app/javascript/controllers/media_type_toggle_controller.js
# Esperado: 2
```
</verification>

<success_criteria>
- `static targets` contém 6 strings incluindo `uploadLabel` e `linkLabel`
- Método `togglePills()` presente e chamado dentro de `toggleFields()`
- Classes ativas `border-[#0F7949] bg-green-50 text-[#0F7949]` e inativas `border-gray-200 text-slate-700` declaradas no método
- Comportamentos existentes (toggleFields, selectUpload, selectLink) preservados sem alteração de lógica
</success_criteria>

<output>
Criar `.planning/phases/10-arte-form-polish/10-02-SUMMARY.md` ao concluir.
</output>
