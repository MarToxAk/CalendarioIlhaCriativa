---
phase: 04-client-calendar-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/layouts/client.html.erb
  - app/controllers/client_controller.rb
  - app/views/client/sessions/new.html.erb
  - config/locales/pt-BR.yml
  - config/routes.rb
autonomous: true
requirements:
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "O portal do cliente usa layout dedicado client.html.erb com header (marca, nome do cliente, botão Sair)"
    - "ClientController declara layout 'client' — todos os controllers filhos herdam"
    - "A tela de login não contém DOCTYPE, head nem body — só o card de formulário"
    - "Nomes de meses aparecem em português (Maio, Junho...) via locale pt-BR"
    - "Rota client_arte_path existe (resources :artes, only: [:show] no scope /c/:token)"
  artifacts:
    - path: "app/views/layouts/client.html.erb"
      provides: "Layout dedicado do portal do cliente"
      contains: "layout 'client'"
    - path: "config/locales/pt-BR.yml"
      provides: "Tradução de nomes de meses para português"
      contains: "Janeiro"
    - path: "config/routes.rb"
      provides: "Rota client_arte_path"
      contains: "resources :artes"
  key_links:
    - from: "app/controllers/client_controller.rb"
      to: "app/views/layouts/client.html.erb"
      via: "layout 'client'"
      pattern: "layout 'client'"
    - from: "app/views/client/sessions/new.html.erb"
      to: "app/views/layouts/client.html.erb"
      via: "herança de layout Rails (Client::SessionsController < ClientController)"
      pattern: "DOCTYPE|<html|<head|<body"
---

<objective>
Criar a infraestrutura visual do portal do cliente: layout dedicado, locale pt-BR e rota de arte.

Purpose: Planos 02 e 03 dependem do layout client.html.erb, da rota client_arte_path e dos nomes de meses em PT-BR. Este plano resolve toda essa fundação sem tocar em lógica de calendário.

Output:
- app/views/layouts/client.html.erb — layout com header (D-01, D-02, D-04)
- ClientController declara layout 'client' (D-01)
- app/views/client/sessions/new.html.erb — só o card de login, sem HTML duplicado (D-03)
- config/locales/pt-BR.yml — date.month_names em PT-BR para I18n.l()
- config/routes.rb — resources :artes, only: [:show] no scope /c/:token (D-09)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-client-calendar-portal/04-CONTEXT.md
@.planning/phases/04-client-calendar-portal/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Layout client.html.erb + ClientController layout + locale pt-BR</name>
  <files>
    app/views/layouts/client.html.erb
    app/controllers/client_controller.rb
    config/locales/pt-BR.yml
  </files>
  <read_first>
    - app/controllers/client_controller.rb — adicionar layout 'client'; não alterar load_client_from_token nem require_client_auth
    - app/views/layouts/admin.html.erb — estrutura de referência (head, flash, main yield)
    - .planning/phases/04-client-calendar-portal/04-RESEARCH.md Pattern 6 — template exato do layout client
  </read_first>
  <action>
    Criar app/views/layouts/client.html.erb seguindo Pattern 6 do RESEARCH.md (D-01, D-02, D-04):

    - DOCTYPE html, lang="pt-BR", head idêntico ao admin.html.erb (csrf_meta_tags, csp_meta_tag, yield :head, Inter font, stylesheet_link_tag :app, javascript_importmap_tags)
    - title: content_for(:title) || "Ilha Criativa · Calendário"
    - body com class "min-h-screen bg-white flex flex-col" (D-04: bg-white, diferente do bg-gray-50 do admin)
    - header sticky top-0 bg-white border-b border-gray-100 z-10 com px-4 sm:px-6 py-4 flex items-center justify-between:
      - Esquerda: span "Ilha Criativa · Bom Custo" com font-semibold text-[#0F7949]
      - Centro/direita: span com @client&.name (safe navigation — @client pode ser nil em erros antes de load_client_from_token)
      - Botão "Sair": button_to "Sair", client_session_path(token: @client&.access_token), method: :delete, class text-sm text-slate-500 hover:text-slate-900 transition-colors
    - Flash messages: notice (bg-green-50, text-green-700) e alert (bg-red-50, text-red-600 font-medium) com mx-4 sm:mx-6 mt-4 px-4 py-3 rounded-lg border
    - main.flex-1.px-4.sm:px-6.py-6.sm:py-8 com yield

    Em app/controllers/client_controller.rb adicionar APENAS a linha `layout 'client'` logo após `class ClientController < ApplicationController` — preservar os before_actions e métodos privados sem nenhuma alteração.

    Criar config/locales/pt-BR.yml com (per D-13/Pattern 5 do RESEARCH.md — Opção A recomendada):
    - pt-BR.date.month_names: [~, Janeiro, Fevereiro, Março, Abril, Maio, Junho, Julho, Agosto, Setembro, Outubro, Novembro, Dezembro]
    - pt-BR.date.abbr_day_names: [Dom, Seg, Ter, Qua, Qui, Sex, Sáb]
    - pt-BR.date.day_names: [Domingo, Segunda-feira, Terça-feira, Quarta-feira, Quinta-feira, Sexta-feira, Sábado]

    Após criar o locale, garantir que config/application.rb define config.i18n.default_locale = :'pt-BR' (ler o arquivo antes de editar — se já existir, não duplicar; se não existir, adicionar dentro do bloco class Application).
  </action>
  <verify>
    <automated>bundle exec rails runner "puts I18n.l(Date.new(2026, 5, 1), format: '%B %Y')" 2>&1 | grep -i "maio"</automated>
  </verify>
  <acceptance_criteria>
    - app/views/layouts/client.html.erb existe e contém "bg-white" no body, "@client&.name", "Sair", "client_session_path"
    - app/controllers/client_controller.rb contém a linha `layout 'client'` antes do `skip_before_action`
    - config/locales/pt-BR.yml contém "Janeiro" e "Maio" na chave date.month_names
    - `bundle exec rails runner "puts I18n.l(Date.new(2026, 5, 1), format: '%B %Y')"` imprime "Maio 2026"
    - config/application.rb contém `config.i18n.default_locale` definido como pt-BR
  </acceptance_criteria>
  <done>Layout client.html.erb criado, ClientController com layout 'client', locale pt-BR ativo com I18n.l() retornando meses em português</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refatorar sessions/new + adicionar rota client_arte_path</name>
  <files>
    app/views/client/sessions/new.html.erb
    config/routes.rb
  </files>
  <read_first>
    - app/views/client/sessions/new.html.erb — HTML COMPLETO atual (DOCTYPE, head, body inline) que será substituído pelo card isolado
    - config/routes.rb — scope /c/:token atual; adicionar resources :artes sem quebrar rotas existentes
    - .planning/phases/04-client-calendar-portal/04-RESEARCH.md Pattern 4 — rota exata a adicionar
    - .planning/phases/04-client-calendar-portal/04-RESEARCH.md exemplo de refatoração do login (sessão "Exemplo — refatoração do login view")
  </read_first>
  <action>
    Refatorar app/views/client/sessions/new.html.erb (D-03): remover COMPLETAMENTE o DOCTYPE, html, head, body e header que existem atualmente — a view passa a conter apenas o conteúdo do card de login. O layout client.html.erb (criado na Task 1) provê o shell HTML.

    O conteúdo resultante da view deve ser um div externo com flex-1 flex items-center justify-center px-4 py-8, contendo o card div.bg-white.rounded-xl.shadow-sm.border.border-gray-100.max-w-sm.w-full.p-8 com:
    - h1 "Bem-vindo(a)!" text-xl font-semibold text-slate-900 mb-1
    - p "Digite sua senha para acessar os conteúdos preparados para você." text-slate-500 text-sm mb-6
    - Bloco flash[:alert] com role="alert" (manter o existente, adaptado)
    - form_with url: client_session_path(token: params[:token]), method: :post com:
      - label "Senha de acesso" + password_field com id "senha-cliente", autocomplete "current-password"
      - Botão de toggle de senha convertido para Stimulus: data-controller="password-toggle" no div wrapper, data-password-toggle-target="field" no input, data-action="click->password-toggle#toggle" no button (substituir o onclick="toggleSenha(this)" inline e remover o bloco <script> inteiramente)
      - submit "Acessar calendário" com as classes bg-[#EA580C] hover:bg-[#C2410C] existentes
    - Preservar o id "senha-cliente", autocomplete, aria-required, classes CSS do input idênticos ao atual

    Em config/routes.rb, dentro do bloco `scope "/c/:token", as: :client`, adicionar após `resource :session`:
    `resources :artes, only: [ :show ], controller: "client/artes"`
    Isso gera o helper client_arte_path(token:, id:) usado no Plano 02 e 03.
  </action>
  <verify>
    <automated>bundle exec rails routes | grep "client_arte"</automated>
  </verify>
  <acceptance_criteria>
    - app/views/client/sessions/new.html.erb NÃO contém "DOCTYPE", "&lt;html", "&lt;head", "&lt;body" (grep deve retornar 0 matches)
    - app/views/client/sessions/new.html.erb contém "data-controller=\"password-toggle\"" (Stimulus) e NÃO contém "onclick" nem bloco &lt;script&gt;
    - `bundle exec rails routes | grep client_arte` retorna linha contendo "GET /c/:token/artes/:id" e "client/artes#show"
    - `bundle exec rails test test/controllers/client/sessions_controller_test.rb` continua verde (nenhum teste quebrado pela refatoração)
  </acceptance_criteria>
  <done>sessions/new.html.erb é apenas o card de login (sem HTML inline duplicado), usa Stimulus para toggle de senha, e client_arte_path(:token, :id) existe nas rotas</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → layout client.html.erb | @client pode ser nil se load_client_from_token falhar antes do layout ser renderizado |
| routes → client/artes#show | Nova rota exposta; requer autenticação via ClientController before_actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | layouts/client.html.erb — @client.name | mitigate | Usar @client&.name (safe navigation) — nil retorna string vazia, sem NoMethodError |
| T-04-01-02 | Tampering | config/locales/pt-BR.yml | accept | Arquivo de tradução estático; sem input do usuário; risco zero de injeção |
| T-04-01-03 | Elevation of Privilege | Nova rota client/artes#show | mitigate | Rota herda ClientController before_actions (load_client_from_token + require_client_auth) — acesso sem sessão redireciona para login |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacotes neste plano — sem risco de supply chain |
</threat_model>

<verification>
Verificações de fase ao final do Plano 01:

1. `bundle exec rails runner "puts I18n.l(Date.new(2026, 5, 1), format: '%B %Y')"` → "Maio 2026"
2. `bundle exec rails routes | grep client_arte` → rota GET /c/:token/artes/:id presente
3. `grep -c "DOCTYPE\|<html\|<head\|<body" app/views/client/sessions/new.html.erb` → 0
4. `grep "layout 'client'" app/controllers/client_controller.rb` → 1 match
5. `bundle exec rails test test/controllers/client/sessions_controller_test.rb` → verde
</verification>

<success_criteria>
- Layout client.html.erb criado com header correto (marca, nome do cliente, botão Sair com DELETE)
- ClientController declara layout 'client'
- sessions/new.html.erb é apenas o card de login sem HTML inline
- Stimulus password-toggle substitui onclick inline no login
- client_arte_path helper disponível nas rotas
- Locale pt-BR com nomes de meses em português
- Nenhum teste existente quebrado
</success_criteria>

<output>
Criar `.planning/phases/04-client-calendar-portal/04-01-SUMMARY.md` quando concluído
</output>
