---
phase: 05-approval-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/views/admin/artes/show.html.erb
  - test/controllers/admin/artes_controller_test.rb
autonomous: true
requirements:
  - APRO-03

must_haves:
  truths:
    - "Admin vê botão 'Marcar como Revisada' na página de show de uma arte com status change_requested"
    - "Admin clica o botão, arte muda para status revised, flash 'Arte marcada como revisada.' aparece"
    - "Botão 'Marcar como Revisada' NÃO aparece para artes com status pending, approved ou revised"
    - "Após mark_revised, cliente consegue aprovar a arte novamente (revised? aceito pelo validator)"
  artifacts:
    - path: app/views/admin/artes/show.html.erb
      provides: "Botão mark_revised condicional ao status change_requested"
      contains: "mark_revised"
    - path: test/controllers/admin/artes_controller_test.rb
      provides: "Testes para mark_revised action"
      contains: "mark_revised"
  key_links:
    - from: "app/views/admin/artes/show.html.erb"
      to: "Admin::ArtesController#mark_revised"
      via: "button_to mark_revised_admin_arte_path"
      pattern: "mark_revised_admin_arte_path"
---

<objective>
Expor o botão "Marcar como Revisada" na view de show do admin para artes com status change_requested (D-09, D-10, APRO-03). Fechar o ciclo de re-aprovação: após o admin marcar como revised, o cliente volta a ver os botões de aprovação.

Purpose: Sem esta action, o fluxo de re-aprovação (APRO-03) está incompleto — artes com change_requested ficam presas sem que o admin possa devolvê-las ao cliente.

Output: Botão na admin show view, testes verdes para mark_revised, suite completa verde.
</objective>

<execution_context>
@/home/bot/.claude/get-shit-done/workflows/execute-plan.md
@/home/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bot/calendario_livia/.planning/PROJECT.md
@/home/bot/calendario_livia/.planning/phases/05-approval-flow/05-CONTEXT.md
@/home/bot/calendario_livia/.planning/phases/05-approval-flow/05-01-SUMMARY.md
@/home/bot/calendario_livia/.planning/phases/05-approval-flow/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Botão mark_revised na admin show view + testes</name>
  <read_first>
    - app/views/admin/artes/show.html.erb (estrutura atual do div de botões — linha 16-21; approval_response singular deve ter sido corrigido em 05-01)
    - app/controllers/admin/artes_controller.rb (mark_revised action adicionada em 05-01 — confirmar que existe e está no before_action :set_arte)
    - test/controllers/admin/artes_controller_test.rb (testes mark_revised adicionados em 05-01 Task 2 — confirmar cobertura antes de adicionar mais)
    - config/routes.rb (confirmar mark_revised_admin_arte_path existe após 05-01)
  </read_first>
  <files>
    app/views/admin/artes/show.html.erb,
    test/controllers/admin/artes_controller_test.rb
  </files>
  <action>
    1. Atualizar app/views/admin/artes/show.html.erb:
       a. No div de botões (`.mt-4.flex.gap-2`), corrigir a condição do botão Excluir: substituir `@arte.approval_response.nil?` por `@arte.approval_responses.none?` caso ainda esteja singular (verificar antes de editar).
       b. Adicionar o botão "Marcar como Revisada" condicionalmente:
          ```erb
          <% if @arte.change_requested? %>
            <%= button_to "Marcar como Revisada",
                          mark_revised_admin_arte_path(@arte),
                          method: :patch,
                          class: "btn btn-secondary",
                          form: { data: { turbo_confirm: "Confirmar: marcar esta arte como revisada?" } } %>
          <% end %>
          ```
       c. Verificar visualmente que o botão aparece apenas quando `change_requested?` — usar o condicional diretamente, sem variável intermediária.

    2. Verificar test/controllers/admin/artes_controller_test.rb:
       Se os testes "mark_revised muda status para revised quando change_requested" e "mark_revised rejeita arte nao change_requested" já existem (adicionados em 05-01 Task 2), apenas confirmar que passam.
       Se NÃO existirem (executor de 05-01 os pulou), adicioná-los agora:
       - Setup: criar admin user via sign_in helper existente, criar Cliente, criar Arte com status pending
       - Test A: criar ApprovalResponse com decision: :change_requested (callback sincroniza arte para change_requested) → PATCH mark_revised_admin_arte_path(@arte) → assert_redirected_to admin_arte_path(@arte) + assert @arte.reload.revised? + assert_equal "Arte marcada como revisada.", flash[:notice]
       - Test B: arte ainda pending → PATCH mark_revised_admin_arte_path(@arte) → assert redirect com flash[:alert] "Ação inválida para o status atual."
       - Test C (ciclo completo APRO-03): arte revised → criar ApprovalResponse com decision: :approved → assert @arte.reload.approved? (validator aceita revised?)

    3. Rodar suite completa para validar que nenhum teste existente quebrou.
  </action>
  <verify>
    <automated>cd /home/bot/calendario_livia && bin/rails test test/controllers/admin/artes_controller_test.rb && bin/rails test</automated>
  </verify>
  <done>
    - app/views/admin/artes/show.html.erb contém mark_revised_admin_arte_path dentro de condicional @arte.change_requested?
    - Botão usa button_to com method: :patch
    - Nenhuma referência a approval_response singular restante na view
    - Testes admin passam (incluindo mark_revised e ciclo completo)
    - bin/rails test: suite completa verde
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Admin::ArtesController#mark_revised | Admin autenticado via Admin::BaseController (require_authentication); Arte.find por ID admin-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | Admin::ArtesController#mark_revised | accept | Protegido por Admin::BaseController#require_authentication; acesso a qualquer arte é intencional para admin |
| T-05-03-02 | Tampering | Arte status via mark_revised | mitigate | Guarda `if @arte.change_requested?` no controller rejeita transição inválida com redirect+alert; sem raw SQL ou bypass de enum |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
bin/rails test — suite completa verde
grep "mark_revised_admin_arte_path" app/views/admin/artes/show.html.erb — deve retornar 1 linha
grep -v "approval_response\b" app/views/admin/artes/show.html.erb || grep "approval_response" app/views/admin/artes/show.html.erb | grep "approval_responses" — todos os usos devem ser plurais
bin/rails routes | grep mark_revised — deve mostrar PATCH /admin/artes/:id/mark_revised
</verification>

<success_criteria>
1. Admin acessa show de arte com change_requested → botão "Marcar como Revisada" visível
2. Admin acessa show de arte com pending, approved ou revised → botão NÃO aparece
3. PATCH /admin/artes/:id/mark_revised com arte change_requested → status revised, flash notice, redirect para admin_arte_path
4. Após mark_revised, cliente pode POST /c/:token/artes/:arte_id/responses com decision: approved → APRO-03 completo
5. bin/rails test: suite completa verde (todos os testes de todas as fases)
</success_criteria>

<output>
Criar `.planning/phases/05-approval-flow/05-03-SUMMARY.md` ao concluir.

Após esta SUMMARY, a Fase 5 está pronta para `/gsd-verify-work`.
</output>
